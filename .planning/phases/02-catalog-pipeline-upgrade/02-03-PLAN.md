---
phase: 02-catalog-pipeline-upgrade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - vscode-extension/src/catalog/types.ts
autonomous: true
requirements: [EXT-01]
must_haves:
  truths:
    - "Il tipo CatalogStep dell'extension include app, area, status, replacedBy, requester, assignee"
    - "Tutti i nuovi campi sono opzionali (?) — un catalog senza questi campi compila senza errori"
    - "L'extension compila senza errori dopo l'aggiunta (npx tsc --noEmit verde)"
    - "Nessun consumer esistente (CompletionProvider, FsLoader) viene rotto dai nuovi campi"
  artifacts:
    - path: "vscode-extension/src/catalog/types.ts"
      provides: "Interfaccia CatalogStep allineata allo schema arricchito, retrocompatibile"
      contains: "status?:"
  key_links:
    - from: "vscode-extension/src/catalog/types.ts"
      to: "vscode-extension/src/**"
      via: "interfaccia condivisa CatalogStep consumata dai provider"
      pattern: "status\\?:.*implemented.*wanted.*deprecated"
---

<objective>
Allineare l'interfaccia `CatalogStep` in `vscode-extension/src/catalog/types.ts` al nuovo schema del catalog, aggiungendo i 6 campi (`app`, `area`, `status`, `replacedBy`, `requester`, `assignee`) come opzionali per garantire la retrocompatibilità con catalog vecchi [EXT-01, D-08]. Nessuna modifica ai consumer: i provider esistenti non leggono questi campi.

Purpose: L'extension è il canale primario di authoring. Senza i tipi allineati, i provider Phase 4/5 (Completion, Diagnostics) non potranno accedere a status/app/area in modo type-safe. Questo plan prepara il contratto di tipo. È indipendente dagli script (file diverso, repo extension separato).
Output: `types.ts` con `CatalogStep` arricchito di 6 campi opzionali, compilazione verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md
@.planning/phases/02-catalog-pipeline-upgrade/02-RESEARCH.md

<interfaces>
<!-- Interfaccia CatalogStep corrente nell'extension (types.ts:16-29). -->
<!-- L'executor aggiunge 6 campi opzionali; NON rimuove né rinomina i campi esistenti. -->

CatalogStep corrente (vscode-extension/src/catalog/types.ts:16-29):
```typescript
export interface CatalogStep {
  expression: string;
  parameters: string[];
  domain: string;
  page?: string;
  sourceRef: string;
  doc: StepDoc;
  documented: boolean;
}
```

Verifica RESEARCH (Pattern 5): grep su vscode-extension/src/ NON mostra accessi a status/app/area
nei consumer esistenti → l'aggiunta di campi opzionali non rompe alcun provider.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere i 6 campi opzionali a CatalogStep in types.ts</name>
  <files>vscode-extension/src/catalog/types.ts</files>
  <read_first>
    - vscode-extension/src/catalog/types.ts (stato corrente — l'executor estende l'interfaccia esistente)
    - .planning/phases/02-catalog-pipeline-upgrade/02-RESEARCH.md (Pattern 5 con codice esatto)
    - .planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md (D-01, D-08 retrocompatibilità)
  </read_first>
  <action>
Modificare l'interfaccia `CatalogStep` in `vscode-extension/src/catalog/types.ts` (righe 16-29), aggiungendo i 6 nuovi campi TUTTI opzionali (D-08). NON rimuovere né rinominare i campi esistenti. NON modificare `StepDoc` né `Catalog` né `CatalogLoader`. NON toccare alcun consumer (CompletionProvider, FsLoader): non vanno modificati perché non leggono questi campi.

Sostituire l'interfaccia con:
```typescript
export interface CatalogStep {
  /** Cucumber expression con placeholder, es: 'I am logged in as a {string} user' */
  expression: string;
  /** Lista dei placeholder nell'ordine in cui appaiono, es: ['{string}'] */
  parameters: string[];
  /** Dominio dello step nel formato <app>/<area>, es. 'app-a/orders', 'common' */
  domain: string;
  /** App di appartenenza, primo segmento del path (es. 'app-a'). Opzionale per retrocompatibilità. */
  app?: string;
  /** Area funzionale, secondo segmento del path (es. 'orders'). Opzionale per retrocompatibilità. */
  area?: string;
  /** Stato lifecycle dello step. undefined è trattato come 'implemented' dal consumer. */
  status?: 'implemented' | 'wanted' | 'deprecated';
  /** Espressione del sostituto, presente solo se status === 'deprecated'. */
  replacedBy?: string;
  /** ID del richiedente, presente solo se status === 'wanted'. */
  requester?: string;
  /** SDET incaricato, presente solo se status === 'wanted'. */
  assignee?: string;
  /** Page Object associato allo step (opzionale, emesso da extract-steps.ts) */
  page?: string;
  /** Riferimento al sorgente, es: 'src/steps/app-a/orders/orders.steps.ts:15' */
  sourceRef: string;
  doc: StepDoc;
  documented: boolean;
}
```

Il commento di `domain` va aggiornato al nuovo formato `<app>/<area>` come mostrato sopra.
  </action>
  <verify>
    <automated>cd vscode-extension && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contiene `app?: string;`
    - types.ts contiene `area?: string;`
    - types.ts contiene `status?: 'implemented' | 'wanted' | 'deprecated';`
    - types.ts contiene `replacedBy?: string;`
    - types.ts contiene `requester?: string;`
    - types.ts contiene `assignee?: string;`
    - i campi esistenti `expression`, `parameters`, `domain`, `page?`, `sourceRef`, `doc`, `documented` sono ancora presenti
    - `cd vscode-extension && npx tsc --noEmit` esce 0 (nessun consumer rotto)
  </acceptance_criteria>
  <done>CatalogStep nell'extension include i 6 nuovi campi opzionali; tsc --noEmit dell'extension verde; nessun consumer modificato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nessun trust boundary. Modifica di sole definizioni di tipo TypeScript in un file dell'extension. Nessun codice eseguibile aggiunto, nessun input, nessun endpoint.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | vscode-extension/src/catalog/types.ts (definizioni di tipo) | accept | Solo type-level, nessun comportamento runtime. I tipi opzionali non introducono percorsi eseguibili. Catalog malformato è già gestito da FsLoader (RESEARCH: ritorna catalog vuoto su JSON corrotto). |

Riferimento: 02-RESEARCH.md §Security Domain conferma assenza di surface (V2-V6 ASVS tutte "no").
</threat_model>

<verification>
- `cd vscode-extension && npx tsc --noEmit` esce 0
- CatalogStep contiene i 6 campi opzionali con i tipi corretti
- Nessun file consumer modificato (solo types.ts in files_modified)
</verification>

<success_criteria>
- EXT-01: CatalogStep include status, replacedBy, requester, assignee, app, area, tutti opzionali e retrocompatibili (catalog senza questi campi compila e non causa errori runtime)
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-catalog-pipeline-upgrade/02-03-SUMMARY.md`
</output>
