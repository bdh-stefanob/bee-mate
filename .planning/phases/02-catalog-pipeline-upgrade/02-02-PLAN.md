---
phase: 02-catalog-pipeline-upgrade
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/render-markdown.ts
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "STEP_CATALOG.md mostra il badge 🔧 davanti agli step wanted e ⛔ davanti ai deprecated"
    - "Gli step implemented non hanno badge (default silenzioso)"
    - "L'header mostra il breakdown 'Total: N steps (X implemented, Y wanted, Z deprecated)'"
    - "Lo step wanted mostra @intent + riga Requester/Assignee"
    - "Lo step deprecated mostra @intent + riga 'Sostituito da: <espressione>'"
    - "Il domain di sezione riflette il nuovo formato <app>/<area>"
  artifacts:
    - path: "scripts/render-markdown.ts"
      provides: "Renderer Markdown con badge status, breakdown header, righe lifecycle"
      contains: "statusBadge"
    - path: "STEP_CATALOG.md"
      provides: "Catalog Markdown rigenerato con badge"
      contains: "🔧"
  key_links:
    - from: "scripts/render-markdown.ts"
      to: "STEP_CATALOG.md"
      via: "concatenazione condizionale con badge emoji"
      pattern: "🔧|⛔|implemented.*wanted.*deprecated"
---

<objective>
Aggiornare `scripts/render-markdown.ts` per rendere visibile lo `status` di ogni step nel `STEP_CATALOG.md`: badge emoji inline (`🔧` wanted, `⛔` deprecated, nessuno per implemented) [D-04], righe condizionali Requester/Assignee per gli step wanted [D-05] e "Sostituito da" per i deprecated [D-06], e un header con breakdown per status [D-07]. Completa la parte di output umano-leggibile di INFRA-03.

Purpose: Il `STEP_CATALOG.md` è il canale di consultazione per i QA non-tecnici. Senza i badge, lo status resta invisibile nel Markdown anche se presente nel JSON. Questo plan è la proiezione leggibile dello schema arricchito da Plan 01.
Output: `render-markdown.ts` esteso, `STEP_CATALOG.md` rigenerato con badge e breakdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md
@.planning/phases/02-catalog-pipeline-upgrade/02-RESEARCH.md

<interfaces>
<!-- render-markdown.ts ha la PROPRIA interfaccia CatalogStep locale (righe 14-27), -->
<!-- distinta da quella di extract-steps.ts e types.ts. Va aggiornata o i badge non escono (Pitfall 2). -->

Interfaccia CatalogStep locale corrente (render-markdown.ts:20-27):
```typescript
interface CatalogStep {
  expression: string;
  parameters: string[];
  domain: string;
  sourceRef: string;
  doc: StepDoc;
  documented: boolean;
}
```

Header Total corrente (render-markdown.ts:43-44):
```typescript
md += `Total steps: **${catalog.totalSteps}** `;
md += `(${catalog.documentedSteps} documented, ${catalog.undocumentedSteps} undocumented)\n\n`;
```

Loop step corrente (render-markdown.ts:55-69):
```typescript
for (const s of list) {
  const flag = s.documented ? "" : " ⚠️ _undocumented_";
  md += `### \`${s.expression}\`${flag}\n\n`;
  if (s.doc.intent) md += `${s.doc.intent}\n\n`;
  // ... params, pre, post, sourceRef
}
```

Schema JSON prodotto da Plan 01 (campi disponibili su ogni step):
```typescript
// app, area, domain (es. "app-a/orders"), status ('implemented'|'wanted'|'deprecated'),
// replacedBy?, requester?, assignee?  — tutti top-level su ogni elemento di catalog.steps
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere badge status, breakdown header e righe lifecycle in render-markdown.ts</name>
  <files>scripts/render-markdown.ts</files>
  <read_first>
    - scripts/render-markdown.ts (stato corrente — l'executor estende l'esistente, NON riscrive)
    - .planning/phases/02-catalog-pipeline-upgrade/02-RESEARCH.md (Pattern 4 + Pitfall 2 con codice esatto)
    - .planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md (D-04, D-05, D-06, D-07)
    - step-catalog.json (output di Plan 01 — verificare che app/area/status/requester/assignee siano presenti)
  </read_first>
  <action>
Modificare `scripts/render-markdown.ts` in 3 punti. NON toccare `extract-steps.ts` né `types.ts`.

**1. Interfaccia `CatalogStep` locale (righe 20-27)** — aggiungere i campi lifecycle opzionali, altrimenti TypeScript compila ma i badge non vengono emessi (Pitfall 2). `status` opzionale per retrocompatibilità con catalog vecchi:
```typescript
interface CatalogStep {
  expression: string;
  parameters: string[];
  app?: string;
  area?: string;
  domain: string;
  status?: 'implemented' | 'wanted' | 'deprecated';
  replacedBy?: string;
  requester?: string;
  assignee?: string;
  sourceRef: string;
  doc: StepDoc;
  documented: boolean;
}
```

**2. Header breakdown (righe 43-44)** — sostituire le 2 righe `Total steps` con il breakdown per status (D-07). Il filtro `!s.status || s.status === 'implemented'` gestisce anche i catalog vecchi senza `status`:
```typescript
const impl = steps.filter((s) => !s.status || s.status === 'implemented').length;
const want = steps.filter((s) => s.status === 'wanted').length;
const depr = steps.filter((s) => s.status === 'deprecated').length;
md += `Total: **${catalog.totalSteps}** steps `;
md += `(${impl} implemented, ${want} wanted, ${depr} deprecated)\n\n`;
```

**3. Loop step (righe 55-69)** — aggiungere il badge davanti all'expression (D-04) e le righe condizionali lifecycle (D-05, D-06). Il badge status e il flag undocumented coesistono senza conflitto. Sostituire il corpo del `for (const s of list)`:
```typescript
for (const s of list) {
  const statusBadge =
    s.status === 'wanted' ? '🔧 ' :
    s.status === 'deprecated' ? '⛔ ' : '';
  const undocFlag = s.documented ? "" : " ⚠️ _undocumented_";
  md += `### ${statusBadge}\`${s.expression}\`${undocFlag}\n\n`;
  if (s.doc.intent) md += `${s.doc.intent}\n\n`;
  if (s.status === 'wanted' && (s.requester || s.assignee)) {
    md += `_Requester: ${s.requester ?? '—'} — Assignee: ${s.assignee ?? '—'}_\n\n`;
  }
  if (s.status === 'deprecated' && s.replacedBy) {
    md += `**Sostituito da:** \`${s.replacedBy}\`\n\n`;
  }
  if (Object.keys(s.doc.params).length) {
    md += `**Parameters:**\n`;
    for (const [name, desc] of Object.entries(s.doc.params)) {
      md += `- \`${name}\` — ${desc}\n`;
    }
    md += `\n`;
  }
  if (s.doc.pre) md += `**Pre:** ${s.doc.pre}\n\n`;
  if (s.doc.post) md += `**Post:** ${s.doc.post}\n\n`;
  md += `_Implementation:_ \`${s.sourceRef}\`\n\n`;
}
```

Le righe Requester/Assignee e Sostituito-da vanno PRIMA dei Parameters per dare priorità all'informazione di lifecycle. Le sezioni `## Domain:` usano già `s.domain` raggruppato, quindi riflettono automaticamente il nuovo formato `app-a/orders` prodotto da Plan 01 — nessuna modifica necessaria al raggruppamento.
  </action>
  <verify>
    <automated>npm run catalog && node -e "const md=require('fs').readFileSync('STEP_CATALOG.md','utf-8'); if(!md.includes('🔧'))throw new Error('badge wanted mancante'); if(!/\\(\\d+ implemented, \\d+ wanted, \\d+ deprecated\\)/.test(md))throw new Error('breakdown header mancante'); if(!md.includes('Requester: DEMO-001'))throw new Error('riga requester mancante'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - render-markdown.ts contiene `const statusBadge =`
    - render-markdown.ts contiene `s.status === 'wanted' ? '🔧 '`
    - render-markdown.ts contiene `s.status === 'deprecated' ? '⛔ '`
    - render-markdown.ts contiene `(${impl} implemented, ${want} wanted, ${depr} deprecated)`
    - render-markdown.ts contiene `**Sostituito da:**`
    - render-markdown.ts contiene `_Requester: ${s.requester`
    - `npx tsc --noEmit -p tsconfig.json` esce 0
    - dopo `npm run catalog`: STEP_CATALOG.md contiene `🔧`
    - dopo `npm run catalog`: STEP_CATALOG.md header matcha `(N implemented, N wanted, N deprecated)`
    - dopo `npm run catalog`: STEP_CATALOG.md contiene `Requester: DEMO-001`
    - dopo `npm run catalog`: STEP_CATALOG.md contiene una sezione `## Domain: \`app-a/orders\``
  </acceptance_criteria>
  <done>STEP_CATALOG.md mostra il badge 🔧 sullo step demo wanted, l'header con breakdown per status, e la riga Requester/Assignee. Domain di sezione in formato <app>/<area>. tsc verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nessun trust boundary. `render-markdown.ts` legge `step-catalog.json` (artifact di build prodotto dal repo stesso) e produce `STEP_CATALOG.md` (documentazione). Nessun input utente, nessun endpoint, nessuna autenticazione.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | STEP_CATALOG.md (documentazione generata) | accept | File rigenerato deterministicamente da `npm run catalog`; CLAUDE.md vieta l'edit manuale. Manomissione viene sovrascritta alla rigenerazione successiva. |

Riferimento: 02-RESEARCH.md §Security Domain conferma assenza di surface (V2-V6 ASVS tutte "no").
</threat_model>

<verification>
- `npm run catalog` esce 0 e rigenera STEP_CATALOG.md
- Il Markdown contiene il badge 🔧 sullo step wanted demo
- L'header mostra il breakdown per status
- Le sezioni domain usano il formato <app>/<area>
- `npx tsc --noEmit -p tsconfig.json` verde
</verification>

<success_criteria>
- INFRA-03 (parte rendering): STEP_CATALOG.md rende visibili status e domain aggiornato, incluso lo step @wanted demo con badge 🔧 e breakdown header
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-catalog-pipeline-upgrade/02-02-SUMMARY.md`
</output>
