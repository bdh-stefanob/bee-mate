---
phase: 02-catalog-pipeline-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract-steps.ts
  - src/steps/app-a/orders/orders.steps.ts
autonomous: true
requirements: [INFRA-02, INFRA-03]
must_haves:
  truths:
    - "Ogni step nel catalog JSON ha campi app, area, domain (come <app>/<area>) e status"
    - "Uno step con @wanted produce status: 'wanted' nel JSON anche se ha @intent"
    - "Uno step con @deprecated produce status: 'deprecated' e replacedBy se presente"
    - "Lo step demo @wanted esiste in app-a/orders ed è censito nel catalog"
    - "npm run test:dry continua a passare dopo le modifiche"
  artifacts:
    - path: "scripts/extract-steps.ts"
      provides: "Parser JSDoc esteso con tag lifecycle + derivazione app/area/domain"
      contains: "status"
    - path: "src/steps/app-a/orders/orders.steps.ts"
      provides: "Step demo @wanted con requester/assignee"
      contains: "@wanted"
    - path: "step-catalog.json"
      provides: "Catalog rigenerato con schema arricchito"
      contains: "\"status\""
  key_links:
    - from: "scripts/extract-steps.ts"
      to: "step-catalog.json"
      via: "fs.writeFileSync con nuovi campi app/area/status"
      pattern: "app.*area.*status|status.*replacedBy"
    - from: "src/steps/app-a/orders/orders.steps.ts"
      to: "step-catalog.json"
      via: "dry-run cattura lo step @wanted"
      pattern: "status.*wanted"
---

<objective>
Estendere `scripts/extract-steps.ts` per (1) derivare `app` e `area` dal path del file step e comporre `domain` come `<app>/<area>` [INFRA-02], e (2) riconoscere i tag JSDoc lifecycle `@wanted`, `@deprecated`, `@replacedBy`, `@requester`, `@assignee` producendo il campo `status` top-level su ogni `CatalogStep` [INFRA-03]. Aggiungere uno step demo `@wanted` in `app-a/orders` per popolare il catalog con almeno uno step di status non-implemented.

Purpose: Il `step-catalog.json` è il contratto di integrazione di tutta la fase. Render-markdown (Plan 02) e l'extension (Plan 03) consumano questo schema. Senza questo plan il catalog resta al formato Phase 1.
Output: `extract-steps.ts` esteso, step demo `@wanted` in orders.steps.ts, `step-catalog.json` rigenerato con i nuovi campi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md
@.planning/phases/02-catalog-pipeline-upgrade/02-RESEARCH.md
@WORKFLOW.md

<interfaces>
<!-- Schema corrente in extract-steps.ts (righe 23-39). L'executor lo estende, NON lo riscrive da zero. -->

StepDoc interno corrente (extract-steps.ts:23-29):
```typescript
interface StepDoc {
  intent?: string;
  params: Record<string, string>;
  pre?: string;
  post?: string;
  page?: string;
}
```

CatalogStep interno corrente (extract-steps.ts:31-39):
```typescript
interface CatalogStep {
  expression: string;
  parameters: string[];
  domain: string;
  page?: string;
  sourceRef: string;
  doc: StepDoc;
  documented: boolean;
}
```

Parser tag corrente (extract-steps.ts:89-102) — usa `/^@(\w+)\s+(.*)$/` che NON matcha i tag bare:
```typescript
for (const line of clean) {
  const m = line.match(/^@(\w+)\s+(.*)$/);
  if (!m) continue;
  const tag = m[1];
  const rest = m[2].trim();
  if (tag === "intent") doc.intent = rest;
  // ... pre, post, page, param
}
```

Derivazione domain corrente (extract-steps.ts:133-134):
```typescript
const domainMatch = uri.match(/steps[/\\]([^/\\]+)[/\\]/);
const domain = domainMatch ? domainMatch[1] : "common";
```

Push step corrente (extract-steps.ts:141):
```typescript
steps.push({ expression, parameters, domain, page: doc.page, sourceRef, doc, documented });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estendere extract-steps.ts con derivazione app/area/domain e parser tag lifecycle</name>
  <files>scripts/extract-steps.ts</files>
  <read_first>
    - scripts/extract-steps.ts (stato corrente — l'executor estende l'esistente)
    - .planning/phases/02-catalog-pipeline-upgrade/02-RESEARCH.md (Pattern 1, 2, 3 + Pitfall 1, 3, 4 con codice esatto)
    - .planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md (D-01, D-02, D-03)
    - WORKFLOW.md §2 (tabella tag JSDoc @wanted/@deprecated/@replacedBy/@requester/@assignee con esempi)
  </read_first>
  <behavior>
    - Test INFRA-02: dopo `npm run catalog`, lo step in orders ha app==='app-a', area==='orders', domain==='app-a/orders'
    - Test edge case common: lo step in src/steps/common/ ha app==='common', area==='common', domain==='common'
    - Test INFRA-03 wanted: uno step con @wanted ha status==='wanted'
    - Test INFRA-03 deprecated: uno step con @deprecated (e senza @wanted) ha status==='deprecated'
    - Test default: uno step senza @wanted né @deprecated ha status==='implemented'
    - Test conflitto: @wanted + @deprecated insieme → status==='wanted' (wanted vince, D-02)
  </behavior>
  <action>
Modificare `scripts/extract-steps.ts` in 5 punti precisi.

**1. Interfaccia `StepDoc` locale (riga 23-29)** — aggiungere i 5 campi lifecycle interni:
```typescript
interface StepDoc {
  intent?: string;
  params: Record<string, string>;
  pre?: string;
  post?: string;
  page?: string;
  wanted?: boolean;
  deprecated?: boolean;
  replacedBy?: string;
  requester?: string;
  assignee?: string;
}
```

**2. Interfaccia `CatalogStep` locale (riga 31-39)** — aggiungere i campi top-level. `domain` resta `string`. I campi lifecycle vivono top-level, NON dentro `doc` (D-03):
```typescript
interface CatalogStep {
  expression: string;
  parameters: string[];
  app: string;
  area: string;
  domain: string;
  status: 'implemented' | 'wanted' | 'deprecated';
  replacedBy?: string;
  requester?: string;
  assignee?: string;
  page?: string;
  sourceRef: string;
  doc: StepDoc;
  documented: boolean;
}
```

**3. Loop parser tag in `extractDoc()` (righe 89-102)** — sostituire la regex con una che gestisce i tag bare (Pitfall 1: `@wanted`/`@deprecated` non hanno argomento). Rendere il gruppo valore opzionale e aggiungere i 5 case:
```typescript
for (const line of clean) {
  const m = line.match(/^@(\w+)(?:\s+(.*))?$/);
  if (!m) continue;
  const tag = m[1];
  const rest = (m[2] ?? "").trim();
  if (tag === "intent") doc.intent = rest;
  else if (tag === "pre") doc.pre = rest;
  else if (tag === "post") doc.post = rest;
  else if (tag === "page") doc.page = rest;
  else if (tag === "wanted") doc.wanted = true;
  else if (tag === "deprecated") doc.deprecated = true;
  else if (tag === "replacedBy") doc.replacedBy = rest;
  else if (tag === "requester") doc.requester = rest;
  else if (tag === "assignee") doc.assignee = rest;
  else if (tag === "param") {
    const pm = rest.match(/^(\S+)\s+(.*)$/);
    if (pm) doc.params[pm[1]] = pm[2];
  }
}
```

**4. Derivazione `domain` (righe 133-134)** — estendere per catturare app + area. Fallback per path a 1 livello (`steps/common/`): `area = app`, `domain = app`:
```typescript
const appMatch = uri.match(/steps[/\\]([^/\\]+)[/\\]/);
const areaMatch = uri.match(/steps[/\\][^/\\]+[/\\]([^/\\]+)[/\\]/);
const app = appMatch ? appMatch[1] : "common";
const area = areaMatch ? areaMatch[1] : app;
const domain = areaMatch ? `${app}/${area}` : app;
```

**5. Push dello step (riga 141)** — calcolare `status` con priorità wanted > deprecated > implemented (D-02) e aggiungere i nuovi campi. Usare spread condizionale per i campi opzionali:
```typescript
const status: 'implemented' | 'wanted' | 'deprecated' =
  doc.wanted ? 'wanted' :
  doc.deprecated ? 'deprecated' :
  'implemented';

steps.push({
  expression,
  parameters,
  app,
  area,
  domain,
  status,
  ...(doc.replacedBy ? { replacedBy: doc.replacedBy } : {}),
  ...(doc.requester ? { requester: doc.requester } : {}),
  ...(doc.assignee ? { assignee: doc.assignee } : {}),
  page: doc.page,
  sourceRef,
  doc,
  documented,
});
```

NON modificare il JSON di output per annidare i campi lifecycle dentro `doc` (Anti-Pattern research): `doc` conserva il suo schema, i campi lifecycle sono top-level.
NON toccare `render-markdown.ts` né `types.ts` (altri plan).
  </action>
  <verify>
    <automated>npm run catalog && node -e "const c=require('./step-catalog.json'); const s=c.steps.find(x=>x.sourceRef.includes('orders')); if(!(s&&s.app==='app-a'&&s.area==='orders'&&s.domain==='app-a/orders'))throw new Error('INFRA-02 fail'); const cm=c.steps.find(x=>x.domain==='common'); if(cm&&cm.area!=='common')throw new Error('edge common fail'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - extract-steps.ts contiene la stringa `/^@(\w+)(?:\s+(.*))?$/`
    - extract-steps.ts contiene `else if (tag === "wanted") doc.wanted = true`
    - extract-steps.ts contiene `else if (tag === "deprecated") doc.deprecated = true`
    - extract-steps.ts contiene `const areaMatch = uri.match(`
    - extract-steps.ts contiene `status: 'implemented' | 'wanted' | 'deprecated'`
    - `npx tsc --noEmit -p tsconfig.json` esce 0 (nessun errore di tipo)
    - dopo `npm run catalog`: lo step orders ha app==='app-a', area==='orders', domain==='app-a/orders'
    - dopo `npm run catalog`: lo step in common ha area==='common' (edge case)
    - `npm run test:dry` esce 0
  </acceptance_criteria>
  <done>extract-steps.ts deriva app/area/domain a 2 segmenti, riconosce i 5 tag lifecycle (inclusi i bare @wanted/@deprecated), e ogni entry del JSON ha status. tsc verde, test:dry verde.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiungere step demo @wanted in app-a/orders</name>
  <files>src/steps/app-a/orders/orders.steps.ts</files>
  <read_first>
    - src/steps/app-a/orders/orders.steps.ts (stato corrente — l'executor aggiunge in coda)
    - .planning/phases/02-catalog-pipeline-upgrade/02-CONTEXT.md (Claude's Discretion: demo @wanted step)
    - WORKFLOW.md §2 (formato JSDoc di uno step @wanted con @requester/@assignee)
  </read_first>
  <action>
Aggiungere in coda al file `src/steps/app-a/orders/orders.steps.ts` (dopo la riga 70, ultimo step esistente) un nuovo step demo `@wanted`. NON aggiungere import di pages o actions (lo stub non li usa). NON referenziare questo step in alcun `.feature` (Pitfall 5: farebbe fallire `npm test`).

Lo step DEVE rispettare l'architettura 4 layer (CLAUDE.md): lo step glue non contiene selettori. Lo stub fa `throw new Error('NOT IMPLEMENTED')` perché l'action non esiste ancora (è uno step richiesto, non implementato).

Aggiungere esattamente:
```typescript
/**
 * @intent    Searches the catalog for a product by name.
 * @param     product  The product name to search for.
 * @wanted
 * @requester DEMO-001
 * @assignee  steve
 */
When(
  "I search for the product {string}",
  async function (this: CustomWorld, product: string) {
    throw new Error('NOT IMPLEMENTED');
  }
);
```

Nota: `product` è volutamente inutilizzato nello stub; non rimuoverlo dalla firma perché la cucumber expression ha `{string}` e la firma deve combaciare.
  </action>
  <verify>
    <automated>npm run catalog && node -e "const c=require('./step-catalog.json'); const s=c.steps.find(x=>x.status==='wanted'); if(!s)throw new Error('nessuno step wanted'); if(s.requester!=='DEMO-001')throw new Error('requester mancante'); if(s.assignee!=='steve')throw new Error('assignee mancante'); if(s.app!=='app-a'||s.area!=='orders')throw new Error('app/area errati'); console.log('OK wanted:',s.expression)"</automated>
  </verify>
  <acceptance_criteria>
    - orders.steps.ts contiene `@wanted`
    - orders.steps.ts contiene `@requester DEMO-001`
    - orders.steps.ts contiene `@assignee  steve`
    - orders.steps.ts contiene `"I search for the product {string}"`
    - orders.steps.ts contiene `throw new Error('NOT IMPLEMENTED')`
    - nessun file in src/features/ contiene la stringa `I search for the product` (grep deve fallire)
    - dopo `npm run catalog`: esiste uno step con status==='wanted', requester==='DEMO-001', assignee==='steve'
    - `step-catalog.json` è stato rigenerato e contiene i nuovi campi (verifica: `node -e "const c=require('./step-catalog.json'); if(!c.steps.some(s=>'status' in s && 'app' in s && 'area' in s))throw new Error('catalog non aggiornato')"`) — il file deve essere staged/committato come deliverable di questo task (CLAUDE.md regola 5: step-catalog.json è la SoT committata)
    - `npm run test:dry` esce 0
    - `npm test` esce 0 (lo step wanted non è invocato da nessuno scenario)
  </acceptance_criteria>
  <done>Esiste uno step demo @wanted in orders.steps.ts con requester/assignee compilati; il catalog lo mostra come status wanted; test:dry e test full passano (lo stub non è invocato).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nessun trust boundary in questa fase. Lo script `extract-steps.ts` legge solo file sorgente TypeScript del repository (controllati dallo sviluppatore) e produce artifact di build (`step-catalog.json`). Nessun endpoint HTTP, nessun input utente a runtime, nessuna autenticazione.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | step-catalog.json (build artifact) | accept | Artifact rigenerabile da codice via `npm run catalog`; non è dato operativo. Manomissione non ha valore — il file è derivato deterministicamente dal sorgente. |
| T-02-02 | Information Disclosure | extract-steps.ts (legge sorgenti) | accept | Legge solo file del repo già accessibili allo sviluppatore. Nessun segreto coinvolto (RESEARCH §Security Domain: V5 Input Validation non applicabile — input = file sorgente repo). |

Riferimento: 02-RESEARCH.md §Security Domain conferma assenza di surface (V2-V6 ASVS tutte "no").
</threat_model>

<verification>
- `npm run catalog` esce 0 e rigenera step-catalog.json
- Lo schema JSON contiene app, area, domain (formato <app>/<area>), status per ogni step
- Almeno uno step ha status==='wanted' con requester/assignee
- `step-catalog.json` (prodotto da `npm run catalog`) è la SoT machine-readable committata (CLAUDE.md regola 5): va staged/committato insieme alle modifiche al codice. Il workflow GSD esegue il commit automatico al termine di ogni task — non serve un task di commit separato, ma il file rigenerato DEVE risultare tra i file modificati.
- `npx tsc --noEmit -p tsconfig.json` verde
- `npm run test:dry` verde, `npm test` verde
</verification>

<success_criteria>
- INFRA-02: step-catalog.json ha app, area, domain (come <app>/<area>) per ogni step
- INFRA-03: extract-steps.ts riconosce i 5 tag lifecycle e produce status; esiste lo step demo @wanted
- I test esistenti continuano a passare (nessuna regressione)
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-catalog-pipeline-upgrade/02-01-SUMMARY.md`
</output>
</content>
</invoke>
