---
phase: 01-multi-app-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/app-a/auth/login.feature
  - src/features/app-a/orders/place-order.feature
  - src/features/app-b/.gitkeep
  - src/steps/app-a/auth/auth.steps.ts
  - src/steps/app-a/orders/orders.steps.ts
  - src/steps/app-b/.gitkeep
  - src/steps/common/common.steps.ts
  - src/actions/app-a/auth.actions.ts
  - src/actions/app-a/orders.actions.ts
  - src/actions/app-b/.gitkeep
  - src/pages/app-a/login.page.ts
  - src/pages/app-a/cart.page.ts
  - src/pages/app-b/.gitkeep
  - scripts/extract-steps.ts
  - step-catalog.json
  - STEP_CATALOG.md
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "Tutto il codice esistente vive sotto src/<layer>/app-a/<area>/ (D-01)"
    - "Esistono directory placeholder app-b con .gitkeep in tutti e 4 i layer (D-03)"
    - "npm test passa 5 scenari / 18 step / 0 undefined sui nuovi path (D-06.1)"
    - "tsc --noEmit ritorna 0 errori dopo il move (D-06.3)"
    - "npm run catalog rigenera step-catalog.json senza errori con domain 'app-a' (D-06.2)"
    - "Nessun path legacy (src/steps/auth, src/actions/auth.actions.ts a 2 livelli, ecc.) rimane nel repo (D-06.4)"
  artifacts:
    - path: "src/steps/app-a/auth/auth.steps.ts"
      provides: "Step definitions auth migrate con import aggiornati"
      contains: "../../../actions/app-a/auth.actions"
    - path: "src/steps/app-a/orders/orders.steps.ts"
      provides: "Step definitions orders migrate con import aggiornati"
      contains: "../../../actions/app-a/orders.actions"
    - path: "src/actions/app-a/auth.actions.ts"
      provides: "Action class auth migrata con import page aggiornato"
      contains: "../../pages/app-a/login.page"
    - path: "src/actions/app-a/orders.actions.ts"
      provides: "Action class orders migrata con import page aggiornato"
      contains: "../../pages/app-a/cart.page"
    - path: "src/steps/common/common.steps.ts"
      provides: "Step common (posizione invariata) con import action aggiornato"
      contains: "../../actions/app-a/auth.actions"
    - path: "src/features/app-b/.gitkeep"
      provides: "Placeholder app-b per il layer features"
    - path: "step-catalog.json"
      provides: "Catalog rigenerato con domain app-a e sourceRef sui nuovi path"
      contains: "app-a"
  key_links:
    - from: "src/steps/app-a/auth/auth.steps.ts"
      to: "src/actions/app-a/auth.actions.ts"
      via: "import relativo a 3 livelli"
      pattern: "\\.\\./\\.\\./\\.\\./actions/app-a/auth\\.actions"
    - from: "src/steps/common/common.steps.ts"
      to: "src/actions/app-a/auth.actions.ts"
      via: "import relativo a 2 livelli (file non spostato, target spostato)"
      pattern: "\\.\\./\\.\\./actions/app-a/auth\\.actions"
    - from: "scripts/extract-steps.ts"
      to: "step-catalog.json"
      via: "regex domain derivation riga 128 produce 'app-a'"
      pattern: "steps\\\\?/\\(\\[\\^/\\]\\+\\)"
---

<objective>
Ristrutturare il layout `src/` da mono-app (`src/<layer>/<area>/`) a multi-app
(`src/<layer>/app-a/<area>/`), migrando tutto il codice esistente sotto `app-a`,
creando i placeholder `app-b` vuoti in tutti e 4 i layer, aggiornando gli import
relativi, e rigenerando il catalog. Tutti i test continuano a passare (5 scenari /
18 step / 0 undefined).

Purpose: INFRA-01 è propedeutico a tutte le fasi successive — i path-based requirement
(INFRA-02 domain `<app>/<area>`, EXT-07 stub in `src/steps/<app>/<area>/`) dipendono
da questa struttura. È un refactor strutturale puro: nessuna nuova funzionalità,
nessuna modifica al comportamento.
Output: struttura multi-app `app-a`/`app-b`, import aggiornati, catalog rigenerato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-app-scaffold/01-CONTEXT.md
@.planning/phases/01-multi-app-scaffold/01-RESEARCH.md
@CONTRIBUTING.md

<interfaces>
<!-- Import map verificato sul codebase reale. L'executor usa questi valori direttamente. -->

Mapping file move (D-01):
  src/features/auth/          → src/features/app-a/auth/
  src/features/orders/        → src/features/app-a/orders/
  src/steps/auth/             → src/steps/app-a/auth/
  src/steps/orders/           → src/steps/app-a/orders/
  src/actions/auth.actions.ts → src/actions/app-a/auth.actions.ts
  src/actions/orders.actions.ts → src/actions/app-a/orders.actions.ts
  src/pages/login.page.ts     → src/pages/app-a/login.page.ts
  src/pages/cart.page.ts      → src/pages/app-a/cart.page.ts

NON si spostano (D-02 + Open Question RESEARCH):
  src/steps/common/common.steps.ts  (resta in src/steps/common/)
  src/support/world.ts
  src/support/hooks.ts

Import da aggiornare (valori esatti — VERIFIED su codebase):

  src/steps/app-a/auth/auth.steps.ts (era src/steps/auth/auth.steps.ts):
    riga 8: "../../support/world"          → "../../../support/world"
    riga 9: "../../actions/auth.actions"   → "../../../actions/app-a/auth.actions"

  src/steps/app-a/orders/orders.steps.ts (era src/steps/orders/orders.steps.ts):
    riga 8: "../../support/world"           → "../../../support/world"
    riga 9: "../../actions/orders.actions"  → "../../../actions/app-a/orders.actions"

  src/steps/common/common.steps.ts (NON si sposta, ma import action cambia):
    riga 7: "../../support/world"          → "../../support/world"   (INVARIATO)
    riga 8: "../../actions/auth.actions"   → "../../actions/app-a/auth.actions"  (CAMBIA)

  src/actions/app-a/auth.actions.ts (era src/actions/auth.actions.ts):
    riga 7: "../pages/login.page"          → "../../pages/app-a/login.page"

  src/actions/app-a/orders.actions.ts (era src/actions/orders.actions.ts):
    riga 6: "../pages/cart.page"           → "../../pages/app-a/cart.page"

  src/pages/app-a/login.page.ts, src/pages/app-a/cart.page.ts:
    nessun import relativo "../" → NESSUNA modifica import

Regex domain in scripts/extract-steps.ts riga 128 (VERIFIED — già corretta):
  const domainMatch = uri.match(/steps\/([^/]+)\//);
  Con uri "src/steps/app-a/auth/auth.steps.ts" → domain = "app-a"  ✓
  Con uri "src/steps/common/common.steps.ts"   → domain = "common" ✓
  → NESSUNA modifica funzionale alla regex. Solo verifica + eventuale commento.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migra il codice sotto app-a, crea placeholder app-b, aggiorna gli import</name>
  <files>src/features/app-a/auth/login.feature, src/features/app-a/orders/place-order.feature, src/steps/app-a/auth/auth.steps.ts, src/steps/app-a/orders/orders.steps.ts, src/steps/common/common.steps.ts, src/actions/app-a/auth.actions.ts, src/actions/app-a/orders.actions.ts, src/pages/app-a/login.page.ts, src/pages/app-a/cart.page.ts, src/features/app-b/.gitkeep, src/steps/app-b/.gitkeep, src/actions/app-b/.gitkeep, src/pages/app-b/.gitkeep</files>
  <read_first>
    - src/steps/auth/auth.steps.ts (stato attuale, import a riga 8-9)
    - src/steps/orders/orders.steps.ts (stato attuale, import a riga 8-9)
    - src/steps/common/common.steps.ts (NON si sposta — import action da aggiornare a riga 8)
    - src/actions/auth.actions.ts (import page a riga 7)
    - src/actions/orders.actions.ts (import page a riga 6)
    - src/pages/login.page.ts (verifica assenza import "../")
    - src/pages/cart.page.ts (verifica assenza import "../")
    - cucumber.js (verifica glob ricorsivi — NON modificare)
    - tsconfig.json (verifica include src/**/*.ts — NON modificare)
  </read_first>
  <action>
Esegui il refactor strutturale nell'ordine ESATTO (la sequenza previene errori transitori — RESEARCH Pitfall 2):

PASSO 1 — Move con git mv (preserva la storia, D Claude's Discretion):
```
git mv src/features/auth src/features/app-a/auth
git mv src/features/orders src/features/app-a/orders
git mv src/steps/auth src/steps/app-a/auth
git mv src/steps/orders src/steps/app-a/orders
git mv src/actions/auth.actions.ts src/actions/app-a/auth.actions.ts
git mv src/actions/orders.actions.ts src/actions/app-a/orders.actions.ts
git mv src/pages/login.page.ts src/pages/app-a/login.page.ts
git mv src/pages/cart.page.ts src/pages/app-a/cart.page.ts
```
Crea prima le directory target se git mv lo richiede (`mkdir`/`New-Item`). NON spostare
`src/steps/common/`, `src/support/`. Su Windows PowerShell git mv funziona; se una directory
target intermedia manca, crearla prima.

PASSO 2 — Aggiorna gli import (valori ESATTI, NON "allineare" genericamente):
- src/steps/app-a/auth/auth.steps.ts:
    "../../support/world"          → "../../../support/world"
    "../../actions/auth.actions"   → "../../../actions/app-a/auth.actions"
- src/steps/app-a/orders/orders.steps.ts:
    "../../support/world"          → "../../../support/world"
    "../../actions/orders.actions" → "../../../actions/app-a/orders.actions"
- src/steps/common/common.steps.ts (file NON spostato — solo l'import action cambia):
    "../../actions/auth.actions"   → "../../actions/app-a/auth.actions"
    (lascia "../../support/world" INVARIATO)
- src/actions/app-a/auth.actions.ts:
    "../pages/login.page"          → "../../pages/app-a/login.page"
- src/actions/app-a/orders.actions.ts:
    "../pages/cart.page"           → "../../pages/app-a/cart.page"
- src/pages/app-a/login.page.ts, src/pages/app-a/cart.page.ts: NESSUN import "../" → non toccare.

NON modificare i JSDoc (@intent/@page/@pre/@post): vanno preservati invariati.
NON rimuovere le credenziali hardcoded in auth.actions.ts (known issue, fuori scope Phase 1).

PASSO 3 — Crea i placeholder app-b vuoti (D-03), un file .gitkeep per layer:
    src/features/app-b/.gitkeep
    src/steps/app-b/.gitkeep
    src/actions/app-b/.gitkeep
    src/pages/app-b/.gitkeep
(file vuoti, nessun contenuto).

PASSO 4 — Gate compile: esegui `npx tsc --noEmit`. Deve dare 0 errori.
Se segnala "Cannot find module" in common.steps.ts → l'import a riga 8 non è stato aggiornato.

PASSO 5 — Gate test: esegui `npm test`. Deve dare 5 scenari / 18 step / 0 undefined.

NON modificare cucumber.js né tsconfig.json (glob già ricorsivi — RESEARCH Anti-Pattern).
NON modificare src/support/.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` esce con codice 0 (nessun errore TypeScript)
    - `npm test` output contiene "5 scenarios" e "18 steps" e NON contiene "undefined"
    - File src/steps/app-a/auth/auth.steps.ts esiste e contiene "../../../actions/app-a/auth.actions"
    - File src/steps/app-a/orders/orders.steps.ts esiste e contiene "../../../actions/app-a/orders.actions"
    - File src/steps/common/common.steps.ts contiene "../../actions/app-a/auth.actions"
    - File src/actions/app-a/auth.actions.ts contiene "../../pages/app-a/login.page"
    - File src/actions/app-a/orders.actions.ts contiene "../../pages/app-a/cart.page"
    - Esistono i 4 file: src/features/app-b/.gitkeep, src/steps/app-b/.gitkeep, src/actions/app-b/.gitkeep, src/pages/app-b/.gitkeep
    - NON esistono più: src/features/auth/, src/features/orders/, src/steps/auth/, src/steps/orders/, src/actions/auth.actions.ts, src/actions/orders.actions.ts, src/pages/login.page.ts, src/pages/cart.page.ts
    - src/support/world.ts e src/support/hooks.ts NON modificati (git diff vuoto su quei file)
    - cucumber.js e tsconfig.json NON modificati
  </acceptance_criteria>
  <done>Tutto il codice è sotto app-a, common.steps.ts ha l'import aggiornato, esistono i placeholder app-b, tsc passa con 0 errori, npm test passa 5/18/0.</done>
</task>

<task type="auto">
  <name>Task 2: Verifica regex domain, rigenera il catalog, conferma assenza di path legacy</name>
  <files>scripts/extract-steps.ts, step-catalog.json, STEP_CATALOG.md</files>
  <read_first>
    - scripts/extract-steps.ts (riga 128-129: regex domain derivation)
    - step-catalog.json (stato attuale — sourceRef con path legacy da sovrascrivere)
    - CLAUDE.md (regola: STEP_CATALOG.md non si scrive a mano, solo npm run catalog)
  </read_first>
  <action>
PASSO 1 — Verifica regex domain (D-04, RESEARCH Pitfall 4):
La regex a riga 128 di scripts/extract-steps.ts è:
    const domainMatch = uri.match(/steps\/([^/]+)\//);
Questa regex È GIÀ CORRETTA per i nuovi path: con uri "src/steps/app-a/auth/auth.steps.ts"
cattura "app-a" (primo segmento dopo steps/), con "src/steps/common/common.steps.ts" cattura
"common". NON modificare la regex funzionalmente — sarebbe una regressione inutile.
Se nel file c'è un commento che descrive la regex come produttrice di "auth"/<area> a 2 livelli,
aggiornalo per documentare che produce il primo segmento app (es. "app-a") con path a 3 livelli,
retrocompatibile con path a 2 livelli ("common"). Questo è l'unico tocco ammesso a extract-steps.ts.

PASSO 2 — Rigenera il catalog (criterio D-06.2):
Esegui `npm run catalog`. Deve completare senza errori e riscrivere step-catalog.json e
STEP_CATALOG.md. NON editare a mano questi due file.

PASSO 3 — Verifica output catalog:
In step-catalog.json, il campo "domain" deve valere "app-a" per gli step di auth/orders
(non più "auth"/"orders") e "common" per gli step common. Il campo "sourceRef" deve puntare
ai nuovi path (es. "src/steps/app-a/auth/auth.steps.ts:NN"), non a path legacy.

PASSO 4 — Verifica assenza path legacy (criterio D-06.4):
Cerca nel repo (src/ e step-catalog.json) qualsiasi riferimento a path legacy:
"src/steps/auth", "src/steps/orders", "src/actions/auth.actions", "src/actions/orders.actions",
"src/pages/login.page", "src/pages/cart.page" (senza app-a/). Devono dare 0 risultati nei
file sorgente e nel catalog rigenerato.
  </action>
  <verify>
    <automated>npm run catalog</automated>
    <automated>node -e "const c=require('./step-catalog.json'); const ds=[...new Set(c.steps.map(s=>s.domain))].sort(); if(!ds.includes('app-a')) throw new Error('domain app-a mancante: '+ds); const legacy=c.steps.filter(s=>/src\/(steps\/(auth|orders)\/|actions\/(auth|orders)\.actions|pages\/(login|cart)\.page)/.test(s.sourceRef)); if(legacy.length) throw new Error('sourceRef legacy: '+JSON.stringify(legacy.map(s=>s.sourceRef))); console.log('OK domains:',ds.join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run catalog` esce con codice 0 senza errori
    - step-catalog.json contiene almeno uno step con "domain": "app-a"
    - Nessuno step in step-catalog.json ha "sourceRef" che corrisponde a path legacy (src/steps/auth/, src/steps/orders/, src/actions/auth.actions.ts, src/actions/orders.actions.ts, src/pages/login.page.ts, src/pages/cart.page.ts senza app-a/)
    - La regex a riga 128 di scripts/extract-steps.ts resta `uri.match(/steps\/([^/]+)\//)` (nessuna modifica funzionale)
    - STEP_CATALOG.md è stato rigenerato (mtime aggiornato, non editato a mano)
    - Grep di "src/steps/auth" e "src/steps/orders" su src/ dà 0 risultati
  </acceptance_criteria>
  <done>La regex domain è confermata corretta, il catalog è rigenerato con domain "app-a" e sourceRef sui nuovi path, nessun path legacy resta nel repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nessuna | Phase 1 è un refactor file-system puro: nessun input utente, nessun endpoint, nessuna credenziale nuova, nessuna superficie di rete attraversata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | step-catalog.json (sourceRef obsoleti dopo move) | mitigate | Task 2 rigenera il catalog via `npm run catalog` e verifica via assertion node che nessun sourceRef punti a path legacy. La VS Code extension (Phase 4) userà sourceRef per go-to-definition: path obsoleti = navigazione rotta. |
| T-01-02 | Information Disclosure | credenziali hardcoded in auth.actions.ts | accept | Known issue ereditato (STATE.md). Fuori scope Phase 1 — il move non introduce né rimuove credenziali. Da risolvere in fase dedicata. Rischio basso: repo personale, valori placeholder ("test.user@example.com"). |
| T-01-03 | Denial of Service | suite test rotta da import non aggiornati | mitigate | Gate `tsc --noEmit` + `npm test` in Task 1 bloccano il completamento se un import resta rotto (incluso il caso common.steps.ts, RESEARCH Pitfall 1). |
</threat_model>

<verification>
Gate complessivi della phase (tutti devono essere verdi):
1. `npx tsc --noEmit` → 0 errori (D-06.3)
2. `npm test` → 5 scenari / 18 step / 0 undefined (D-06.1)
3. `npm run catalog` → 0 errori, step-catalog.json con domain "app-a" (D-06.2)
4. Grep di path legacy in src/ → 0 risultati (D-06.4)
</verification>

<success_criteria>
- Struttura `src/<layer>/app-a/<area>/` per tutto il codice esistente (D-01)
- Placeholder app-b con .gitkeep nei 4 layer (D-03)
- `src/support/` e `src/steps/common/` (posizione) invariati (D-02)
- Tutti e 4 i gate D-06 verdi
- Import relativi aggiornati ai valori esatti documentati in `<interfaces>` (D-05, nessun path alias)
- Catalog rigenerato (mai editato a mano)
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-app-scaffold/01-01-SUMMARY.md`
</output>
