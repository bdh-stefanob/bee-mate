---
phase: 999.17-tag-driven-placement-movimentazione-feature
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/src/lib/feature-tags.ts
  - web-ui/src/components/FeaturePlacementDialog.tsx
  - web-ui/src/components/FeatureImportDialog.tsx
autonomous: true
requirements: [TAG-PLACE-01]
must_haves:
  truths:
    - "Esiste un helper che legge e scrive i tag @app @flow nel contenuto di un feature: getFeatureTags(content) e setFeatureTags(content, app, flow)"
    - "setFeatureTags sostituisce la riga tag esistente (prima riga @...) se presente, altrimenti la inserisce prima di Feature: (o in cima); preserva il resto del contenuto verbatim"
    - "Esiste un componente FeaturePlacementDialog riusabile che chiede App+Flow (autocomplete su valori esistenti), mostra l'anteprima del path e una guardia anti-overwrite, e restituisce la scelta via callback onConfirm(app, flow) senza decidere l'azione (save/move)"
    - "FeatureImportDialog continua a funzionare riusando FeaturePlacementDialog (o la stessa logica), senza regressioni sull'import esistente"
  artifacts:
    - path: "web-ui/src/lib/feature-tags.ts"
      provides: "getFeatureTags / setFeatureTags — i tag @app @flow come fonte di verità nel contenuto"
      contains: "export function setFeatureTags"
    - path: "web-ui/src/components/FeaturePlacementDialog.tsx"
      provides: "Dialog generico App/Flow con autocomplete, anteprima path, anti-overwrite, callback onConfirm"
      contains: "onConfirm"
  key_links:
    - from: "web-ui/src/components/FeatureImportDialog.tsx"
      to: "web-ui/src/components/FeaturePlacementDialog.tsx"
      via: "riuso del dialog generico per l'import"
      pattern: "FeaturePlacementDialog"
---

<objective>
Creare le **fondamenta** per il placement tag-driven: (1) un helper che tratta i tag `@app @flow` nel contenuto come fonte di verità (lettura + scrittura/aggiornamento), e (2) un **dialog generico di placement** riusabile (App/Flow + autocomplete + anteprima path + anti-overwrite), separando la SCELTA della collocazione dall'AZIONE (save/move). Rifattorizzare `FeatureImportDialog` perché lo riusi.

Purpose: i piani 02 (definisci tag al Save/+) e 03/04 (movimentazione) hanno bisogno di un unico punto per scegliere app/flow e per scrivere i tag nel contenuto. Centralizzarlo evita duplicazione e mantiene i tag come unica fonte di verità (decisione utente).

Output: `feature-tags.ts` (helper), `FeaturePlacementDialog.tsx` (dialog generico), `FeatureImportDialog` rifattorizzato per riusarlo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Derivazione tag attuale (pattern già usato in _content.tsx handleSave ~riga 350):
```typescript
const tagLine = content.match(/^(@\S+(?:\s+@\S+)*)/m);
const tags = tagLine?.[1].match(/@(\S+)/g)?.map(t => t.slice(1)) ?? [];
// tags[0] = app, tags[1] = flow
```
`slugify` da `@/lib/repo`.

FeatureImportDialog attuale (web-ui/src/components/FeatureImportDialog.tsx): props `{ open, fileName, content, existingApps, existingFlows, existingPaths, onCancel, onSaved }`. Deriva app/flow dai tag + page marker (extractPageMarkers), datalist, anteprima path `src/features/{app}/{flow}/{file}.feature`, guardia anti-overwrite (confronto con existingPaths), POST /api/features con content VERBATIM, poi onSaved(path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper feature-tags (get/set)</name>
  <files>web-ui/src/lib/feature-tags.ts</files>
  <read_first>
    - web-ui/src/app/editor/_content.tsx (handleSave: come deriva i tag oggi)
    - web-ui/src/lib/repo.ts (slugify)
  </read_first>
  <action>
Crea `web-ui/src/lib/feature-tags.ts`:
```typescript
export function getFeatureTags(content: string): { app: string | null; flow: string | null } {
  const tagLine = content.match(/^(@\S+(?:\s+@\S+)*)/m);
  const tags = tagLine?.[1].match(/@(\S+)/g)?.map(t => t.slice(1)) ?? [];
  return { app: tags[0] ?? null, flow: tags[1] ?? null };
}

/** Inserisce o sostituisce la riga tag @app @flow in cima al feature, preservando il resto. */
export function setFeatureTags(content: string, app: string, flow: string): string {
  const tagLine = `@${app} @${flow}`;
  const lines = content.split('\n');
  // Trova la prima riga non vuota
  const firstIdx = lines.findIndex(l => l.trim() !== '');
  if (firstIdx >= 0 && /^\s*@/.test(lines[firstIdx])) {
    lines[firstIdx] = tagLine;               // sostituisci la tag line esistente
    return lines.join('\n');
  }
  // Nessuna tag line: inserisci prima di Feature: se c'è, altrimenti in cima
  const featureIdx = lines.findIndex(l => /^\s*Feature:/i.test(l));
  const insertAt = featureIdx >= 0 ? featureIdx : 0;
  lines.splice(insertAt, 0, tagLine, '');
  return lines.join('\n');
}
```
NOTA: i valori app/flow passati dovrebbero essere già slugificati dal chiamante (o slugificali qui per sicurezza). Non alterare i `# #PAGINA` né il titolo.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "feature-tags" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/src/lib/feature-tags.ts` esiste ed esporta `getFeatureTags` e `setFeatureTags`
    - setFeatureTags sostituisce una tag line esistente e, se assente, inserisce prima di `Feature:`; preserva il resto verbatim
    - `cd web-ui && npx tsc --noEmit` senza errori
  </acceptance_criteria>
  <done>Helper per leggere/scrivere i tag @app @flow nel contenuto, tag come fonte di verità.</done>
</task>

<task type="auto">
  <name>Task 2: FeaturePlacementDialog generico + refactor FeatureImportDialog</name>
  <files>web-ui/src/components/FeaturePlacementDialog.tsx, web-ui/src/components/FeatureImportDialog.tsx</files>
  <read_first>
    - web-ui/src/components/FeatureImportDialog.tsx (logica attuale: autocomplete, anteprima, anti-overwrite)
    - web-ui/src/lib/feature-tags.ts (dal Task 1)
    - web-ui/src/lib/page-markers.ts (extractPageMarkers, usato per suggerire flow)
  </read_first>
  <action>
**FeaturePlacementDialog.tsx** (nuovo, `'use client'`): estrai la UI generica di scelta collocazione.
```typescript
interface FeaturePlacementDialogProps {
  open: boolean;
  title: string;                  // titolo dialog (es. "Importa feature", "Sposta feature", "Salva feature")
  fileBaseName: string;           // nome file senza dir (per anteprima path)
  suggestedApp?: string;
  suggestedFlow?: string;
  detectedPages?: string[];       // opzionale: "Pagine rilevate" (per import)
  existingApps: string[];
  existingFlows: string[];
  existingPaths: string[];        // per guardia anti-overwrite
  confirmLabel?: string;          // es. "Salva", "Sposta"
  onCancel: () => void;
  onConfirm: (app: string, flow: string, targetRel: string) => void | Promise<void>;
}
```
- Stato `app`/`flow` inizializzati dai suggested; input con `<datalist>` da existingApps/existingFlows; anteprima `src/features/{slugify(app)}/{slugify(flow)}/{fileBaseName}`; guardia anti-overwrite (confronto coda `{app}/{flow}/{file}` con existingPaths, checkbox di conferma); "Conferma" disabilitato se app/flow vuoti.
- onConfirm riceve app/flow slugificati e il `targetRel`. Il dialog NON fa fetch: l'azione (save/move) la decide il chiamante.

**FeatureImportDialog.tsx**: rifattorizza per usare `FeaturePlacementDialog` internamente (deriva suggested app/flow dai tag via getFeatureTags + fallback page marker; detectedPages da extractPageMarkers; onConfirm → POST /api/features con content verbatim → onSaved). Mantieni la firma esterna invariata (open, fileName, content, existing*, onCancel, onSaved) così la pagina Features non cambia.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "FeaturePlacementDialog|FeatureImportDialog" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `FeaturePlacementDialog.tsx` esiste, generico, con `onConfirm(app, flow, targetRel)` e nessun fetch interno
    - `FeatureImportDialog` riusa `FeaturePlacementDialog` e mantiene la firma esterna (nessuna modifica richiesta in features/page.tsx)
    - Anti-overwrite e anteprima path presenti nel dialog generico
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Dialog di placement generico riusabile; import rifattorizzato senza regressioni.</done>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- L'import dalla pagina Features funziona come prima (riusa il dialog generico)
- setFeatureTags preserva il contenuto e gestisce tag presenti/assenti
</verification>

<success_criteria>
- Helper get/set tag nel contenuto (fonte di verità)
- Dialog di placement generico riusabile da import/save/move
- Nessuna regressione sull'import
</success_criteria>

<output>
After completion, create `.planning/phases/999.17-tag-driven-placement-movimentazione-feature/999.17-01-SUMMARY.md`
</output>
