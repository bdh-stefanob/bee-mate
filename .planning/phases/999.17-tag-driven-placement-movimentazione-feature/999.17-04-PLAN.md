---
phase: 999.17-tag-driven-placement-movimentazione-feature
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - web-ui/src/app/features/page.tsx
autonomous: false
requirements: [MOVE-01]
must_haves:
  truths:
    - "Nella pagina Features i feature sono trascinabili (HTML5 drag&drop) e i nodi flow (App→Flow) sono drop target"
    - "Trascinando un feature su un nodo flow diverso, il feature viene spostato in quell'app/flow tramite POST /api/features/move (stessa logica del bottone Sposta: tag aggiornati, anti-overwrite, tab/localStorage aggiornati, albero rinfrescato)"
    - "C'è un feedback visivo sul drop target durante il dragover; il drop sulla stessa posizione è un no-op"
    - "Non sono introdotte nuove dipendenze (solo HTML5 DnD nativo)"
  artifacts:
    - path: "web-ui/src/app/features/page.tsx"
      provides: "Drag&drop dei feature sui nodi flow dell'albero, riusando handleMove"
      contains: "onDrop"
  key_links: []
---

<objective>
Aggiungere la **movimentazione via drag&drop**: trascinare un feature su un nodo flow dell'albero per spostarlo lì, riusando l'endpoint `/api/features/move` e la logica `handleMove` del piano 03. Include il **checkpoint di verifica visiva** dell'intera fase 999.17.

Purpose: completare la movimentazione con l'interazione più immediata (drag&drop), oltre al bottone "Sposta". Decisione utente: entrambe le UX.

Output: drag&drop nativo (nessuna dipendenza) nella pagina Features; il drop su un flow chiama lo stesso move. Checkpoint finale.

NOTA: l'ultima task è un checkpoint `human-verify` → il piano è `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Pagina Features (web-ui/src/app/features/page.tsx) dopo il piano 03:
- `buildTree` → `tree: { app, flows: { flow, features }[] }[]`. Render: nodo app (button) → nodi flow (button) → `renderFeatureRow(f, indent)`.
- `handleMove(file, app, flow)` (o equivalente del piano 03): POST /api/features/move, gestione 409, aggiornamento localStorage tab + `loadFeatures()`. Riusalo per il drop.
- `FeatureSummary`: `file`, `app`, `flow`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drag&drop dei feature sui nodi flow</name>
  <files>web-ui/src/app/features/page.tsx</files>
  <read_first>
    - web-ui/src/app/features/page.tsx (renderFeatureRow, render nodi flow, handleMove dal piano 03)
  </read_first>
  <action>
1. Stato `const [dragOverFlow, setDragOverFlow] = useState<string | null>(null)` (chiave `${app}/${flow}`) per il feedback visivo.
2. In `renderFeatureRow`: rendi il contenitore `draggable`, `onDragStart={e => e.dataTransfer.setData('text/plain', f.file)}` (e setta `effectAllowed='move'`).
3. Sui **nodi flow**: `onDragOver={e => { e.preventDefault(); setDragOverFlow(flowKey); }}`, `onDragLeave={() => setDragOverFlow(null)}`, `onDrop={e => { e.preventDefault(); const file = e.dataTransfer.getData('text/plain'); setDragOverFlow(null); if (file) onDropFeature(file, app, flow); }}`. Evidenzia il nodo flow quando `dragOverFlow === flowKey` (es. ring/bg teal).
4. `onDropFeature(file, app, flow)`: se il feature è già in quell'app/flow (confronta con `features.find(f=>f.file===file)`), no-op; altrimenti chiama la stessa logica di `handleMove(file, app, flow)` del piano 03 (riusa la funzione, non duplicare il fetch).
5. Nessuna nuova dipendenza: solo HTML5 DnD. Mantieni intatti i bottoni Edit/Sposta e il resto.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "features/page" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "draggable|onDrop|onDragOver" web-ui/src/app/features/page.tsx` mostra il DnD su righe feature e nodi flow
    - Il drop su un flow riusa la logica di move del piano 03 (tag-aware, anti-overwrite, tab/localStorage, refresh)
    - Feedback visivo sul drop target; no-op se stessa posizione
    - Nessuna nuova dipendenza in package.json
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Drag&drop dei feature sui nodi flow, riusando l'endpoint/handler move.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifica visiva fase 999.17</name>
  <files>(verifica manuale — nessun file modificato)</files>
  <action>Avviare la web-ui e verificare placement tag-driven + movimentazione (dialog e drag&drop). Pausa per conferma utente.</action>
  <verify>Verifica manuale dell'utente</verify>
  <done>L'utente conferma definizione tag e movimentazione funzionanti</done>
  <what-built>
    Fase 999.17: (01) helper feature-tags + dialog placement riusabile; (02) Save tag-driven (dialog se ambiguo) + bottone "Cartella" nell'editor; (03) endpoint /api/features/move + bottone "Sposta"; (04) drag&drop dei feature sui nodi flow.
  </what-built>
  <how-to-verify>
1. `cd web-ui && npm run dev` → http://localhost:3000
2. **Definisci tag al Save (02):** /editor, crea/edita un feature SENZA tag @app @flow e premi Save → si apre il dialog placement; definisci App/Flow → salva → il file compare nel nodo App→Flow giusto (non in "ALTRO"). Con tag già presenti, Save è diretto. Prova anche il bottone "Cartella" nell'header per impostare i tag senza salvare.
3. **Sposta (dialog) (03):** /features → su un feature, bottone "Sposta" → scegli un nuovo App/Flow → il feature si sposta nell'albero; apri il file e verifica che i tag `@app @flow` nel contenuto siano aggiornati. Prova uno spostamento che collide → avviso (no overwrite).
4. **Drag&drop (04):** /features → trascina un feature su un nodo flow diverso → si sposta lì (feedback visivo durante il drag). Le cartelle vuote spariscono.
5. Console browser: nessun errore React/runtime.
  </how-to-verify>
  <resume-signal>Scrivi "approvato" o descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- Drag&drop sposta il feature nel flow target, tag aggiornati
- Checkpoint umano (Task 2) approvato
</verification>

<success_criteria>
- Movimentazione via drag&drop oltre al bottone Sposta, riusando l'endpoint move
- Tag come fonte di verità coerenti dopo lo spostamento
- Verifica visiva dell'intera fase approvata dall'utente
</success_criteria>

<output>
After completion, create `.planning/phases/999.17-tag-driven-placement-movimentazione-feature/999.17-04-SUMMARY.md`
</output>
