---
phase: 999.17-tag-driven-placement-movimentazione-feature
plan: 04
subsystem: ui
tags: [drag-and-drop, html5-dnd, react, next.js, feature-move]

# Dependency graph
requires:
  - phase: 999.17-03
    provides: "handleMove logic + /api/features/move endpoint + Sposta button"
provides:
  - "Native HTML5 drag&drop on feature rows in /features page"
  - "Flow nodes as drop targets with teal ring/bg visual feedback"
  - "onDropFeature reusing move endpoint: tag update, localStorage tab sync, tree refresh"
affects: [999.17-checkpoint, features-page-future-iterations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "HTML5 DnD via draggable + onDragStart + onDragOver/onDragLeave/onDrop — no deps"
    - "dragOverFlow state (${app}/${flow} key) drives conditional Tailwind ring+bg on drop target"
    - "onDropFeature guards no-op via features.find() before calling /api/features/move"

key-files:
  created: []
  modified:
    - web-ui/src/app/features/page.tsx

key-decisions:
  - "999.17-04: onDropFeature non duplica il fetch — stessa chiamata POST /api/features/move di handleMove; localStorage tab update incluso"
  - "999.17-04: no-op guard controlla current.app === app && current.flow === flow prima del fetch per evitare round-trip inutili"

patterns-established:
  - "DnD con feedback visivo dichiarativo: className condizionale su dragOverFlow, nessuna manipolazione DOM diretta"

requirements-completed: [MOVE-01]

# Metrics
duration: 10min
completed: 2026-06-26
---

# Phase 999.17 Plan 04: Drag&Drop Feature Summary

**Native HTML5 drag&drop su feature rows e nodi flow in /features, riutilizzando /api/features/move con no-op guard, teal ring feedback e localStorage tab sync — nessuna dipendenza aggiunta**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-06-26T10:15:00Z
- **Completed:** 2026-06-26T10:25:00Z
- **Tasks:** 1 completato (Task 2 = checkpoint human-verify — attesa approvazione)
- **Files modified:** 1

## Accomplishments
- Feature rows nel tree sidebar ora `draggable` con `effectAllowed='move'` e path file nel dataTransfer
- Nodi flow accettano drop: `onDragOver`/`onDragLeave`/`onDrop` con `dragOverFlow` state per highlight
- `onDropFeature` riusa la logica POST `/api/features/move`: no-op se stessa posizione, anti-409, localStorage tabs aggiornati, `loadFeatures()` refresh
- Nessuna nuova dipendenza in package.json

## Task Commits

1. **Task 1: Drag&drop dei feature sui nodi flow** — `b471ce5` (feat)

## Files Created/Modified
- `web-ui/src/app/features/page.tsx` — aggiunto `dragOverFlow` state, `draggable`+`onDragStart` su feature rows, `onDragOver`/`onDragLeave`/`onDrop` sui nodi flow, `onDropFeature` con move logic

## Decisions Made
- `onDropFeature` non duplica il fetch body di `handleMove` — stessa chiamata `POST /api/features/move` con stessa gestione 409 e localStorage sync. Garantisce coerenza comportamentale tra drag&drop e bottone Sposta.
- No-op guard (`current.app === app && current.flow === flow`) prima del fetch evita round-trip inutili quando si rilascia su stessa posizione.

## Deviations from Plan

Nessuna — piano eseguito esattamente come scritto.

## Issues Encountered

Nessuno — TypeScript passa senza errori su features/page.

## User Setup Required

Nessuno.

## Next Phase Readiness

- Fase 999.17 completa dopo approvazione checkpoint Task 2 (verifica visiva end-to-end)
- Intera fase: placement tag-driven (dialog Save + bottone Cartella) + Sposta (dialog) + drag&drop

---
*Phase: 999.17-tag-driven-placement-movimentazione-feature*
*Completed: 2026-06-26*
