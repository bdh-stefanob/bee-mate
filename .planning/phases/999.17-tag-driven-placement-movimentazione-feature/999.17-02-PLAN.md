---
phase: 999.17-tag-driven-placement-movimentazione-feature
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web-ui/src/app/editor/_content.tsx
autonomous: true
requirements: [TAG-PLACE-01]
must_haves:
  truths:
    - "Salvando dall'editor un feature con placement AMBIGUO (mancano app/flow nei tag), si apre FeaturePlacementDialog per definire App/Flow; alla conferma i tag vengono scritti nel contenuto (setFeatureTags) e il file salvato in src/features/{app}/{flow}/{slug}.feature"
    - "Se i tag @app @flow sono già presenti, il Save resta diretto e veloce come ora (nessun modale forzato su ogni Ctrl+S)"
    - "Esiste un'azione esplicita nell'editor (es. bottone 'Cartella'/'Imposta tag') che apre il dialog di placement e applica i tag al contenuto del tab corrente — utile dopo aver creato un nuovo feature col +"
    - "Il dialog usa App/Flow esistenti (da GET /api/features) per autocomplete e anti-overwrite"
  artifacts:
    - path: "web-ui/src/app/editor/_content.tsx"
      provides: "Save tag-driven (dialog se ambiguo) + azione esplicita di placement, riusando FeaturePlacementDialog e setFeatureTags"
      contains: "FeaturePlacementDialog"
  key_links:
    - from: "web-ui/src/app/editor/_content.tsx"
      to: "web-ui/src/lib/feature-tags.ts"
      via: "setFeatureTags per scrivere i tag nel contenuto prima del salvataggio"
      pattern: "setFeatureTags"
---

<objective>
Rendere il salvataggio dall'editor **tag-driven** e dare un modo esplicito di definire i tag (cartella) del feature: al Save con placement ambiguo si apre il `FeaturePlacementDialog` (dal piano 01); un'azione "Imposta cartella/tag" lo apre on-demand (utile dopo il "+"). I tag scelti vengono scritti nel contenuto via `setFeatureTags` (tag = fonte di verità).

Purpose: oggi `handleSave` deriva il path dai tag e, se mancano, salva flat in "ALTRO" silenziosamente (es. `feature-prova` con `@import`). Con questa modifica il QA definisce App/Flow quando serve, e il file finisce nel nodo giusto dell'albero.

Output: `handleSave` apre il dialog se i tag non bastano; un bottone editor apre il dialog on-demand; entrambi scrivono i tag nel contenuto e salvano. Riuso totale di `FeaturePlacementDialog` + `setFeatureTags`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Dal piano 01 (già su main):
- `web-ui/src/lib/feature-tags.ts`: `getFeatureTags(content) → { app, flow }`, `setFeatureTags(content, app, flow) → string`.
- `web-ui/src/components/FeaturePlacementDialog.tsx`: props `{ open, title, fileBaseName, suggestedApp?, suggestedFlow?, detectedPages?, existingApps, existingFlows, existingPaths, confirmLabel?, onCancel, onConfirm(app, flow, targetRel) }` — nessun fetch interno.

Editor (web-ui/src/app/editor/_content.tsx):
- `handleSave` (~riga 346): deriva slug dal titolo, app/flow dai primi due @tag, filePath = `{app}/{flow}/{slug}.feature` o flat. POST /api/features con content. Aggiorna il tab (dirty:false, filePath).
- `addTab`/tab bar "+" creano un tab vuoto.
- L'editor NON carica la lista feature: per il dialog serve fetch `GET /api/features` (FeatureSummary[] con app/flow/file).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save tag-driven con dialog se ambiguo + azione placement</name>
  <files>web-ui/src/app/editor/_content.tsx</files>
  <read_first>
    - web-ui/src/app/editor/_content.tsx (handleSave, addTab, header buttons, render)
    - web-ui/src/components/FeaturePlacementDialog.tsx (props)
    - web-ui/src/lib/feature-tags.ts (get/setFeatureTags)
    - web-ui/src/lib/types.ts (FeatureSummary)
  </read_first>
  <action>
1. **Lista feature per il dialog**: aggiungi uno stato e un fetch `GET /api/features` (al mount, o lazy alla prima apertura del dialog) per ricavare `existingApps`, `existingFlows` (unici, non vuoti) ed `existingPaths` (`f.file`).

2. **Stato dialog placement**: `const [placementOpen, setPlacementOpen] = useState(false)` + un riferimento all'azione pendente (es. `'save'` | `'set'`).

3. **handleSave tag-driven**: con `getFeatureTags(content)`:
   - Se `app && flow` → comportamento attuale (salva diretto a `{app}/{flow}/{slug}.feature`). NIENTE modale (Ctrl+S resta veloce).
   - Se manca app o flow → apri `FeaturePlacementDialog` (suggestedApp/Flow da getFeatureTags o primo page marker; fileBaseName = `${slug}.feature`; title "Salva feature", confirmLabel "Salva"). `onConfirm(app, flow)` → `const updated = setFeatureTags(content, app, flow); setContent(updated);` poi esegui il POST /api/features verso `{app}/{flow}/{slug}.feature` con `updated`, aggiorna il tab (dirty:false, filePath), chiudi il dialog. (Estrai la POST in una funzione condivisa per non duplicare.)

4. **Azione esplicita "Imposta cartella/tag"**: aggiungi un bottone nell'header dell'editor (vicino a Save) o nella toolbar, che apre `FeaturePlacementDialog` (suggested dai tag correnti) e su `onConfirm` fa `setContent(setFeatureTags(content, app, flow))` senza salvare (l'utente poi salva). Utile subito dopo il "+".

5. Renderizza un solo `<FeaturePlacementDialog .../>` controllato da `placementOpen`, con `onConfirm` che esegue l'azione pendente (save vs set).
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "_content" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FeaturePlacementDialog" web-ui/src/app/editor/_content.tsx` mostra import + render
    - `grep -n "setFeatureTags" web-ui/src/app/editor/_content.tsx` mostra la scrittura dei tag nel contenuto al confirm
    - handleSave apre il dialog SOLO quando app/flow non sono entrambi presenti; altrimenti salva diretto
    - L'editor fetcha `/api/features` per popolare existingApps/Flows/paths
    - Esiste un bottone esplicito che apre il dialog di placement on-demand
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Save tag-driven (dialog se ambiguo, veloce se i tag bastano) + azione esplicita di placement; i tag finiscono nel contenuto.</done>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- Save con tag completi → diretto; con tag mancanti → dialog → tag scritti + salvataggio nel nodo giusto
- Bottone placement applica i tag al contenuto corrente
</verification>

<success_criteria>
- Definizione tag al Save (se ambiguo) e on-demand, con i tag scritti nel contenuto (fonte di verità)
- Nessun modale forzato quando i tag sono già completi
- Riuso di FeaturePlacementDialog + setFeatureTags
</success_criteria>

<output>
After completion, create `.planning/phases/999.17-tag-driven-placement-movimentazione-feature/999.17-02-SUMMARY.md`
</output>
