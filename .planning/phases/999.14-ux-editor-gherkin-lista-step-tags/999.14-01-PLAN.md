---
phase: 999.14-ux-editor-gherkin-lista-step-tags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/src/lib/page-markers.ts
  - web-ui/src/app/api/tags/route.ts
  - web-ui/src/app/tags/page.tsx
  - web-ui/src/lib/i18n.ts
autonomous: true
requirements: [TAGS-03]
must_haves:
  truths:
    - "Ogni card pagina in /tags ha una sezione espandibile che elenca gli step associati a quella pagina (non solo il count)"
    - "Gli step elencati sono le espressioni Gherkin reali sotto quel marker # #PAGINA, deduplicate, aggregate su tutti i file"
    - "L'estensione di extractPageMarkers è backward-compatible: i consumatori esistenti (FeatureImportDialog, conteggio /api/tags) continuano a funzionare"
  artifacts:
    - path: "web-ui/src/lib/page-markers.ts"
      provides: "extractPageMarkers ritorna anche steps[] per pagina (campo aggiuntivo, backward-compatible)"
      contains: "steps"
    - path: "web-ui/src/app/api/tags/route.ts"
      provides: "TagAggregate include steps[] deduplicati per pagina"
      contains: "steps"
    - path: "web-ui/src/app/tags/page.tsx"
      provides: "Sezione step espandibile per ogni pagina"
      contains: "steps"
  key_links:
    - from: "web-ui/src/app/tags/page.tsx"
      to: "/api/tags"
      via: "fetch — consuma il nuovo campo steps"
      pattern: "steps"
---

<objective>
Arricchire la pagina `/tags`: oltre al count step per pagina, mostrare l'**elenco espandibile degli step** di ciascuna pagina (`# #PAGINA`), riusando l'estrazione esistente.

Purpose: il count da solo dice "quanti", ma il QA vuole vedere *quali* step sono associati a una schermata. Espandere la card pagina per elencare gli step rende `/tags` una mappa navigabile della copertura per schermata.

Output: `extractPageMarkers` ritorna anche `steps[]` per pagina (campo aggiuntivo, retro-compatibile); `/api/tags` aggrega gli step deduplicati; la pagina `/tags` mostra una sezione step espandibile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Stato attuale (web-ui/src/lib/page-markers.ts):
```typescript
export interface PageMarkerCount { page: string; display: string; stepCount: number; }
export function extractPageMarkers(content: string): PageMarkerCount[];
// STEP_LINE_RE = /^\s+(given|when|then|and|but)\s+(.+)/i  — il gruppo (2) è il testo dello step.
// Consumatori esistenti: web-ui/src/app/api/tags/route.ts, web-ui/src/components/FeatureImportDialog.tsx
```

Stato attuale (web-ui/src/app/api/tags/route.ts):
```typescript
interface TagAggregate { page: string; display: string; stepCount: number; files: string[]; }
// aggrega su walkFeatures(FEATURES_DIR); somma stepCount, dedup files.
```

Stato attuale (web-ui/src/app/tags/page.tsx): card per pagina con display + badge count + lista file cliccabili (openInEditor via gsd-editor-incoming).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extractPageMarkers ritorna steps[] + API aggrega gli step</name>
  <files>web-ui/src/lib/page-markers.ts, web-ui/src/app/api/tags/route.ts</files>
  <read_first>
    - web-ui/src/lib/page-markers.ts (logica attuale, STEP_LINE_RE cattura il testo step)
    - web-ui/src/app/api/tags/route.ts (aggregazione)
    - web-ui/src/components/FeatureImportDialog.tsx (consumatore — verifica che non si rompa)
  </read_first>
  <action>
**page-markers.ts**: aggiungi `steps: string[]` a `PageMarkerCount`. Nel loop, quando matcha `STEP_LINE_RE`, oltre a incrementare `stepCount` accumula il testo step (`sm[2].trim()`) in un array per pagina. Ritorna `steps` deduplicati a livello di singolo contenuto (un `Set` per pagina). NON cambiare la firma né i campi esistenti (`page`, `display`, `stepCount` restano) — solo aggiunta.

**api/tags/route.ts**: aggiungi `steps: string[]` a `TagAggregate`. Nell'aggregazione su tutti i file, unisci gli step di ogni pagina in un `Set` (dedup cross-file) e materializzalo in array ordinato. Mantieni `stepCount` come somma (conteggio occorrenze, può restare > steps.length se uno step si ripete tra file — va bene; documenta che `steps` è l'insieme unico).
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "page-markers|api/tags" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "steps" web-ui/src/lib/page-markers.ts` mostra il nuovo campo + accumulo
    - `PageMarkerCount` mantiene `page`, `display`, `stepCount` invariati (solo aggiunta di `steps`)
    - `api/tags` ritorna `pages[].steps` deduplicati cross-file
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori (incluso FeatureImportDialog)
  </acceptance_criteria>
  <done>extractPageMarkers e /api/tags espongono la lista step per pagina, retro-compatibili.</done>
</task>

<task type="auto">
  <name>Task 2: Sezione step espandibile in /tags</name>
  <files>web-ui/src/app/tags/page.tsx, web-ui/src/lib/i18n.ts</files>
  <read_first>
    - web-ui/src/app/tags/page.tsx (struttura card, stile)
    - web-ui/src/lib/i18n.ts (sezione tags)
  </read_first>
  <action>
**tags/page.tsx**: aggiungi `steps: string[]` all'interface `TagAggregate` locale. In ogni card pagina aggiungi un toggle "Mostra step ▼ / Nascondi step ▲" (stato locale per-pagina, es. `Set<string>` di pagine espanse) che, quando aperto, elenca `steps` in una lista compatta monospace (stile coerente con StepBrowser: testo piccolo, hover). Default: collassato. Se `steps` è vuoto, non mostrare il toggle.

**i18n.ts**: aggiungi in `tags` (en/it) le chiavi `showSteps` / `hideSteps` (en: "Show steps" / "Hide steps"; it: "Mostra step" / "Nascondi step") e `stepsLabel` (en: "Steps" / it: "Step").
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "tags/page|i18n" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "steps" web-ui/src/app/tags/page.tsx` mostra il consumo + render lista
    - Esiste un toggle per espandere/collassare gli step per pagina (stato per-pagina)
    - i18n: chiavi showSteps/hideSteps/stepsLabel in en e it
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Ogni pagina in /tags può espandere la lista degli step associati.</done>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- `/api/tags` ritorna `pages[].steps`
- La card pagina espande/collassa la lista step
</verification>

<success_criteria>
- /tags mostra, per pagina, la lista espandibile degli step (oltre al count)
- Nessuna regressione su FeatureImportDialog / count esistente
</success_criteria>

<output>
After completion, create `.planning/phases/999.14-ux-editor-gherkin-lista-step-tags/999.14-01-SUMMARY.md`
</output>
