---
phase: 999.14-ux-editor-gherkin-lista-step-tags
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - web-ui/src/app/editor/_content.tsx
  - web-ui/src/components/StepBrowser.tsx
autonomous: false
requirements: [EDITOR-UX-01, EDITOR-UX-02]
must_haves:
  truths:
    - "Nell'editor, la colonna destra (StepBrowser) NON scorre via insieme al contenuto dell'editor: resta visibile mentre si scrolla l'area editor (scroll indipendente / sticky)"
    - "La sezione 'Preview .feature' nella colonna destra dell'editor è rimossa"
    - "Il pannello Steps mostra di default ricerca + filtri sempre accessibili, con la lista risultati in stato compatto/riducibile (non un lungo elenco che occupa tutta la colonna di default)"
    - "La sidebar sinistra (FileSidebar) resta fissa come ora (nessuna regressione)"
  artifacts:
    - path: "web-ui/src/app/editor/_content.tsx"
      provides: "Layout a colonne con scroll indipendente; rimozione del box Preview .feature"
      contains: "StepBrowser"
    - path: "web-ui/src/components/StepBrowser.tsx"
      provides: "Lista risultati compatta/riducibile con ricerca sempre visibile"
      contains: "useState"
  key_links: []
---

<objective>
Migliorare la UX del Gherkin Editor: (1) colonne laterali **fisse** (la colonna destra Steps non scrolla via con l'editor), (2) pannello Steps **riducibile** con la ricerca come stato di default, (3) **rimozione** del box "Preview .feature" (inutile).

Purpose: con feature lunghe, oggi l'intera area a due colonne sta dentro un unico contenitore `overflow-y-auto`, quindi scrollando l'editor la colonna Steps sparisce. Il QA vuole gli strumenti (Steps) sempre a portata. Inoltre il pannello Steps di default mostra ~100 risultati che occupano tutta la colonna, e il box Preview duplica il contenuto già visibile nell'editor.

Output: layout a colonne con scroll indipendente (editor scrolla, colonna destra sticky con scroll proprio), StepBrowser compatto di default, box Preview rimosso.

NOTA: l'ultima task è un checkpoint `human-verify` (verifica visiva del layout) → il piano è `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Struttura render attuale (web-ui/src/app/editor/_content.tsx ~riga 502-687):
```
<div className="flex h-[calc(100vh-3.5rem)] overflow-hidden">
  {showSidebar && <div className="w-52 shrink-0 overflow-hidden"><FileSidebar/></div>}   ← già fissa
  <div className="flex-1 min-w-0 flex flex-col overflow-hidden">
    <div className="shrink-0 ...">Header + tab bar</div>                                  ← già fisso
    <div className="flex-1 overflow-y-auto px-4 ...">                                       ← QUI sta il problema
      <div className="flex flex-col lg:flex-row gap-4">
        <div className="... lg:w-2/3">Toolbar + GherkinEditor + unknown panel + ImportDropzone</div>
        <div className="flex flex-col gap-4 lg:w-1/3">
          <StepBrowser onInsert={handleInsert} />
          <div>...Preview .feature box (t.editor.preview / previewPlaceholder)...</div>    ← DA RIMUOVERE (~riga 673-682)
        </div>
      </div>
    </div>
  </div>
</div>
```

StepBrowser (web-ui/src/components/StepBrowser.tsx): ha già stato `open` (collapse globale), ricerca `query`, filtri area/keyword, lista `<ul>` con `maxHeight:'480px'` overflow-y-auto. Il badge header mostra `filtered.length/steps.length`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Colonne a scroll indipendente + rimozione Preview</name>
  <files>web-ui/src/app/editor/_content.tsx</files>
  <read_first>
    - web-ui/src/app/editor/_content.tsx (render ~riga 502-687: contenitore scroll, two-column, box Preview)
  </read_first>
  <action>
Ristruttura il contenitore "Scrollable content" (`<div className="flex-1 overflow-y-auto ...">`) così che le due colonne scrollino in modo **indipendente** e la colonna destra (Steps) resti visibile:

- Cambia il contenitore esterno da `flex-1 overflow-y-auto ...` a `flex-1 overflow-hidden` e rendi il layout a colonne un flex-row che riempie l'altezza: `flex flex-col lg:flex-row gap-4 min-h-0 h-full`.
- **Colonna sinistra (editor, lg:w-2/3)**: dalle sue il proprio scroll → `overflow-y-auto min-h-0` (toolbar + editor + unknown panel + ImportDropzone scrollano qui). Mantieni il padding orizzontale dove serve.
- **Colonna destra (lg:w-1/3)**: `overflow-y-auto min-h-0` con `lg:sticky lg:top-0` (oppure semplicemente altezza piena con scroll proprio), così resta in vista mentre l'editor scrolla. Su mobile (`flex-col`) il comportamento può restare impilato.
- **Rimuovi** il box "Preview .feature" (l'intero `<div className="rounded-md border ...">` con `t.editor.preview` e `{content || t.editor.previewPlaceholder}`, ~riga 673-682). Lascia solo `<StepBrowser onInsert={handleInsert} />` nella colonna destra.
- NON toccare la sidebar sinistra (FileSidebar), l'header, la tab bar.
- Mantieni il responsive: su schermi piccoli (`flex-col`) le colonne possono impilarsi e scrollare insieme; lo scroll indipendente/sticky vale da `lg:` in su.

Lascia inalterate le stringhe i18n `t.editor.preview`/`previewPlaceholder` in i18n.ts (possono restare inutilizzate; non è necessario rimuoverle, ma se preferisci puoi rimuovere SOLO se non sono usate altrove — verifica con grep prima).
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "_content" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - Il box Preview è rimosso: `grep -n "previewPlaceholder" web-ui/src/app/editor/_content.tsx` non trova più il render (la chiave può restare in i18n)
    - La colonna destra ha scroll/sticky proprio: `grep -niE "sticky|overflow-y-auto" web-ui/src/app/editor/_content.tsx` mostra il nuovo scroll sulle colonne
    - FileSidebar, header e tab bar invariati
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Le colonne scrollano indipendentemente, la colonna Steps resta visibile, il box Preview è rimosso.</done>
</task>

<task type="auto">
  <name>Task 2: StepBrowser — lista risultati compatta di default</name>
  <files>web-ui/src/components/StepBrowser.tsx</files>
  <read_first>
    - web-ui/src/components/StepBrowser.tsx (stato open/query/filtered, lista maxHeight 480px)
  </read_first>
  <action>
Rendi la lista risultati **compatta/riducibile** mantenendo ricerca e filtri sempre visibili quando il pannello è aperto:

- Aggiungi uno stato `expandedList` (default `false`). Quando `false`, la lista risultati è in modalità compatta: riduci il `maxHeight` (es. ~180px invece di 480px) OPPURE mostra solo i primi N risultati (es. 8) con un pulsante "Mostra tutti ({filtered.length}) ▼"; quando `true`, mostra l'altezza/lista piena con "Riduci ▲".
- Ricerca e filtri (area/keyword pills + Input) restano SEMPRE visibili sopra la lista quando `open` è true (già così) — non nasconderli in modalità compatta.
- Se l'utente digita una query, puoi opzionalmente espandere automaticamente (comodo) — a discrezione, ma non obbligatorio.
- Mantieni il collapse globale `open` esistente (header click) e la navigazione tastiera.
- Scegli UNA delle due strategie (maxHeight ridotto vs primi-N + "mostra tutti") — quella più pulita con il design attuale. Documenta la scelta nel SUMMARY.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "StepBrowser" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - StepBrowser ha un nuovo stato per la modalità compatta/espansa della lista (`grep -niE "expanded|compact|showAll" web-ui/src/components/StepBrowser.tsx`)
    - Ricerca e filtri restano visibili in modalità compatta
    - Esiste un controllo per espandere/ridurre la lista
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Il pannello Steps è compatto di default con ricerca sempre disponibile ed espansione su richiesta.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifica visiva layout editor</name>
  <files>(verifica manuale — nessun file modificato)</files>
  <action>Avviare la web-ui e verificare il nuovo layout editor. Pausa per conferma utente.</action>
  <verify>Verifica manuale dell'utente</verify>
  <done>L'utente conferma il nuovo layout: colonne fisse, Steps compatto, niente Preview</done>
  <what-built>
    Editor UX: colonne a scroll indipendente (Steps sempre visibile), pannello Steps compatto di default con ricerca sempre disponibile, box Preview .feature rimosso. Più la lista step espandibile in /tags (Plan 01).
  </what-built>
  <how-to-verify>
1. `cd web-ui && npm run dev` → http://localhost:3000/editor
2. Carica un feature lungo (es. `src/features/brochure-clinic/login/login.feature`) e **scrolla l'editor**: la colonna destra (Steps) deve restare visibile (non sparire).
3. Il pannello Steps è **compatto** di default (ricerca + filtri visibili, lista ridotta) e si può espandere.
4. Il box "Preview .feature" **non c'è più**.
5. La sidebar sinistra (Explorer) funziona come prima (toggle ☰, lista file).
6. `/tags`: ogni pagina può **espandere la lista degli step** (Plan 01).
7. Console browser: nessun errore React/hydration.
  </how-to-verify>
  <resume-signal>Scrivi "approvato" o descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- Colonna Steps visibile durante lo scroll dell'editor
- Box Preview rimosso
- StepBrowser compatto di default
- Checkpoint umano (Task 3) approvato
</verification>

<success_criteria>
- Colonne editor a scroll indipendente, Steps sempre visibile
- Pannello Steps compatto di default con ricerca
- Box Preview .feature rimosso
- Verifica visiva approvata dall'utente
</success_criteria>

<output>
After completion, create `.planning/phases/999.14-ux-editor-gherkin-lista-step-tags/999.14-02-SUMMARY.md`
</output>
