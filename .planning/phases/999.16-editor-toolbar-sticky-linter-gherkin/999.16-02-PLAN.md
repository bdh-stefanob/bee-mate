---
phase: 999.16-editor-toolbar-sticky-linter-gherkin
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - web-ui/package.json
  - web-ui/src/lib/gherkin-cm.ts
  - web-ui/src/components/GherkinEditor.tsx
autonomous: false
requirements: [LINT-01]
must_haves:
  truths:
    - "L'editor segnala attivamente i veri errori di compilazione del feature usando il parser ufficiale @cucumber/gherkin (es. struttura rotta, Examples/tabelle malformate, keyword/blocchi non validi)"
    - "Gli errori del parser sono mostrati come diagnostiche CodeMirror posizionate sulla riga/colonna corretta, con messaggio dell'errore del parser"
    - "Un gutter (lintGutter) mostra le icone di errore/warning accanto alle righe interessate"
    - "Le regole manuali esistenti (step fuori scenario, scenario senza step, manca Feature, keyword non capitalizzata) restano come complemento"
    - "Se @cucumber/gherkin non si bundla nel client o lancia in modo inatteso, il linter ricade sulle regole manuali senza rompere l'editor (try/catch)"
  artifacts:
    - path: "web-ui/src/lib/gherkin-cm.ts"
      provides: "gherkinLinter potenziato col parser @cucumber/gherkin + regole manuali"
      contains: "@cucumber/gherkin"
    - path: "web-ui/src/components/GherkinEditor.tsx"
      provides: "lintGutter aggiunto alle estensioni dell'editor"
      contains: "lintGutter"
  key_links:
    - from: "web-ui/src/lib/gherkin-cm.ts"
      to: "@cucumber/gherkin"
      via: "import Parser/AstBuilder/GherkinClassicTokenMatcher e parse del documento nel linter"
      pattern: "@cucumber/gherkin"
---

<objective>
Potenziare il linter live dell'editor Gherkin: oggi `gherkinLinter` ([web-ui/src/lib/gherkin-cm.ts]) usa regole regex fatte a mano e NON mostra il gutter. Integrare il **parser ufficiale `@cucumber/gherkin`** (lo stesso di Cucumber) per segnalare **veri errori di compilazione**, aggiungere il **`lintGutter()`** per le icone d'errore, mantenendo le regole manuali come complemento.

Purpose: il QA vuole sapere subito se un feature "compila" (è valido per Cucumber) senza lanciare `npm test`. Il parser ufficiale intercetta errori che le regex non vedono (Examples/tabelle malformate, blocchi in posizione errata, sintassi rotta).

Output: dipendenza `@cucumber/gherkin` in web-ui, `gherkinLinter` che parsa col Parser sincrono e mappa gli errori a diagnostiche CodeMirror (con fallback alle regole manuali), `lintGutter` nelle estensioni dell'editor.

NOTA: l'ultima task è un checkpoint `human-verify` (verifica visiva: toolbar sticky + linter) → il piano è `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
web-ui/src/lib/gherkin-cm.ts:
- `export const gherkinLinter = linter((view) => { ...regole manuali... return diagnostics })` (~riga 130). `import { linter, Diagnostic } from '@codemirror/lint'` già presente.
- Per mappare riga/colonna del parser a offset CodeMirror: `view.state.doc.line(lineNumber).from + (column - 1)` (line/column del parser sono 1-based).

web-ui/src/components/GherkinEditor.tsx:
- `import { gherkinLanguage, gherkinTheme, gherkinLinter } from '@/lib/gherkin-cm'` (riga 19); estensioni a ~riga 276 includono già `gherkinLinter` ma NON `lintGutter`.
- `lintGutter` si importa da `@codemirror/lint` (già in deps): `import { lintGutter } from '@codemirror/lint'`.

API sincrona browser-safe di @cucumber/gherkin (NON usare le helper a stream node):
```typescript
import { Parser, AstBuilder, GherkinClassicTokenMatcher } from '@cucumber/gherkin';
import { IdGenerator } from '@cucumber/messages';
const parser = new Parser(new AstBuilder(IdGenerator.uuid()), new GherkinClassicTokenMatcher());
try {
  parser.parse(content);
} catch (err) {
  // CompositeParserException: err.errors[] con { message, location: { line, column } }
  // (oppure un singolo ParserException con .location). Normalizzare a un array.
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungi @cucumber/gherkin e integra il parser nel linter</name>
  <files>web-ui/package.json, web-ui/src/lib/gherkin-cm.ts</files>
  <read_first>
    - web-ui/src/lib/gherkin-cm.ts (gherkinLinter attuale, regole manuali)
    - web-ui/package.json (deps; verifica versione di @cucumber/messages eventualmente già presente)
  </read_first>
  <action>
1. **Dipendenza**: aggiungi `@cucumber/gherkin` (e `@cucumber/messages` se non già presente come dep) a `web-ui` con `cd web-ui && npm install @cucumber/gherkin @cucumber/messages`. Usa versioni compatibili (le ultime stabili). Committa il package.json + lockfile.

2. **Integra il parser** in `gherkinLinter` (gherkin-cm.ts): all'inizio del callback del linter, prova a parsare `view.state.doc.toString()` col Parser sincrono (vedi snippet in interfaces). Avvolgi TUTTO in `try/catch`:
   - Se il parse lancia una `CompositeParserException` (o `ParserException`), estrai `err.errors` (o `[err]`) e per ciascun errore con `location { line, column }` crea una `Diagnostic` `severity: 'error'`, `from = doc.line(line).from + max(0, column-1)`, `to = doc.line(line).to`, `message = err.message` (ripulito). Gestisci location mancante/righe fuori range con clamp.
   - Se l'import/parse fallisce per ragioni NON di sintassi (es. modulo non disponibile nel bundle), NON propagare: cattura e prosegui solo con le regole manuali (il linter non deve mai rompere l'editor).
   - **Mantieni le regole manuali esistenti** (step fuori scenario, scenario senza step, manca Feature, keyword non capitalizzata): aggiungi i diagnostics del parser a quelli manuali. Evita duplicati ovvi (se il parser ha già emesso un errore sulla stessa riga, le regole manuali possono restare — accettabile).

3. Tipizzazione: importa i tipi in modo sicuro; se `@cucumber/gherkin` non espone tipi per le eccezioni, usa un narrowing difensivo (`err as { errors?: Array<{ message: string; location?: { line: number; column?: number } }> }`).
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "gherkin-cm" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/package.json` include `@cucumber/gherkin`
    - `gherkin-cm.ts` importa e usa `Parser`/`AstBuilder`/`GherkinClassicTokenMatcher` dentro `gherkinLinter`, con try/catch e fallback alle regole manuali
    - Gli errori del parser diventano `Diagnostic` con `from/to` derivati da `doc.line(...).from`
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Il linter usa il parser ufficiale Gherkin per i veri errori di compilazione, con fallback alle regole manuali.</done>
</task>

<task type="auto">
  <name>Task 2: lintGutter + verifica bundling client</name>
  <files>web-ui/src/components/GherkinEditor.tsx</files>
  <read_first>
    - web-ui/src/components/GherkinEditor.tsx (import da @codemirror/lint, lista extensions ~riga 276)
  </read_first>
  <action>
1. In `GherkinEditor.tsx`, importa `lintGutter` da `@codemirror/lint` e aggiungilo alle `extensions` (vicino a `gherkinLinter`), così le righe con errori mostrano un'icona nel gutter.
2. **Verifica bundling**: esegui `cd web-ui && npm run build` per assicurarti che `@cucumber/gherkin` si bundli nel client senza errori (nessun import node-only che rompe il build). Se il build fallisce per dipendenze node del parser, isola l'uso del parser in modo che il fallback alle regole manuali resti attivo e documenta il limite nel SUMMARY (in tal caso il linter resta sulle regole manuali + gutter).
  </action>
  <verify>
    <automated>cd web-ui && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lintGutter" web-ui/src/components/GherkinEditor.tsx` mostra import + uso in extensions
    - `cd web-ui && npm run build` completa con successo (o, se il parser non si bundla, il build passa col fallback e il limite è documentato nel SUMMARY)
  </acceptance_criteria>
  <done>Il gutter mostra le icone d'errore; il build client passa col parser (o col fallback documentato).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifica visiva toolbar sticky + linter</name>
  <files>(verifica manuale — nessun file modificato)</files>
  <action>Avviare la web-ui e verificare toolbar sticky e linter. Pausa per conferma utente.</action>
  <verify>Verifica manuale dell'utente</verify>
  <done>L'utente conferma toolbar sticky e linter funzionanti</done>
  <what-built>
    Editor: (01) toolbar sticky in cima durante lo scroll; (02) linter live col parser @cucumber/gherkin che segnala veri errori di compilazione + gutter con icone.
  </what-built>
  <how-to-verify>
1. `cd web-ui && npm run dev` → http://localhost:3000/editor
2. **Toolbar sticky (01):** carica un feature lungo e scrolla → la barra Structure/Steps resta in alto.
3. **Linter (02):** introduci un errore Gherkin (es. cancella i `:` da `Feature`, rompi una tabella `Examples`, metti uno step fuori da uno Scenario) → compare una sottolineatura d'errore + un'icona nel gutter sulla riga, con messaggio al hover. Correggendo, l'errore sparisce.
4. Console browser: nessun errore React/runtime.
  </how-to-verify>
  <resume-signal>Scrivi "approvato" o descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- `cd web-ui && npm run build` passa (parser bundlato o fallback documentato)
- Errori Gherkin segnalati attivamente con gutter
- Checkpoint umano (Task 3) approvato
</verification>

<success_criteria>
- Linter live con parser ufficiale Gherkin per veri errori di compilazione + gutter visibile
- Regole manuali mantenute come complemento, con fallback robusto
- Toolbar sticky verificata
- Verifica visiva approvata dall'utente
</success_criteria>

<output>
After completion, create `.planning/phases/999.16-editor-toolbar-sticky-linter-gherkin/999.16-02-SUMMARY.md`
</output>
