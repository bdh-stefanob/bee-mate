---
phase: 999.13-feature-file-import-nell-editor-sezione-tags-hashtag
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - web-ui/src/lib/page-markers.ts
  - web-ui/src/app/api/tags/route.ts
  - web-ui/src/app/tags/page.tsx
  - web-ui/src/app/layout.tsx
  - web-ui/src/lib/i18n.ts
autonomous: false
requirements: [TAGS-01, TAGS-02]
must_haves:
  truths:
    - "Esiste una pagina /tags raggiungibile dalla nav che elenca tutti i marker # #PAGINA trovati nei .feature di src/features/"
    - "Per ogni pagina è mostrato il numero di step ad essa associati (count aggregato su tutti i file)"
    - "Da ogni voce della pagina /tags un click porta all'editor sul file che contiene quella pagina"
    - "L'aggregazione delle pagine usa la STESSA logica di derivazione di unknownSteps (PAGE_MARKER_RE + normalizzazione minuscolo-trattini)"
  artifacts:
    - path: "web-ui/src/lib/page-markers.ts"
      provides: "Funzione condivisa che estrae { page, stepCount } da un contenuto .feature (logica PAGE_MARKER_RE transitiva)"
      contains: "export function extractPageMarkers"
    - path: "web-ui/src/app/api/tags/route.ts"
      provides: "GET /api/tags → aggregato pagine con count step e file associati"
      exports: ["GET"]
    - path: "web-ui/src/app/tags/page.tsx"
      provides: "Pagina /tags con elenco pagine, count step, link all'editor"
      min_lines: 30
    - path: "web-ui/src/app/layout.tsx"
      provides: "Link Tags nella nav"
      contains: "/tags"
  key_links:
    - from: "web-ui/src/app/tags/page.tsx"
      to: "/api/tags"
      via: "fetch in useEffect"
      pattern: "fetch\\(.*api/tags"
    - from: "web-ui/src/app/api/tags/route.ts"
      to: "web-ui/src/lib/page-markers.ts"
      via: "import extractPageMarkers + listFeatures/walkFeatures"
      pattern: "extractPageMarkers"
    - from: "web-ui/src/app/tags/page.tsx"
      to: "/editor"
      via: "click su voce → localStorage gsd-editor-incoming + router.push('/editor') (stesso pattern di features/page.tsx)"
      pattern: "gsd-editor-incoming"
---

<objective>
Creare una sezione **Tags/Hashtag** (`/tags`) che aggrega tutti i marker `# #PAGINA` presenti nei file `.feature` di `src/features/`, mostrando per ogni pagina il numero di step associati e un link diretto all'editor sul file che contiene quella pagina.

Purpose: i marker di pagina (`#LOGIN`, `#MFA`, `#BROCHURE HOMEPAGE`, …) sono la mappa implicita delle schermate testate. Una vista aggregata aiuta il QA a capire la copertura per schermata e a saltare rapidamente al file giusto. Completa il filtro `#PageName` (backlog 999.10) e l'estrazione page già usata in editor/proposte.

Output: lib condivisa `extractPageMarkers` (riusa la logica di `unknownSteps`), API `GET /api/tags`, pagina `/tags`, link in nav, traduzioni i18n. Questo piano chiude l'intera fase 999.13, quindi include un **checkpoint di verifica visiva** dell'intero workflow (upload + proposta + tags).

NOTA: l'ultima task è un checkpoint `human-verify` → il piano è `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

<interfaces>
<!-- Contratti esistenti da riusare. -->

Logica di derivazione pagina da replicare (da _content.tsx unknownSteps, ~riga 205):
```typescript
// All-caps comment = page marker: "# #LOGIN", "#WEIGHT LOSS", "# MEDICINE PREFER"
const PAGE_MARKER_RE = /^\s*#\s*#?([A-Z][A-Z0-9 _-]{1,})\s*$/;
const STEP_LINE_RE   = /^\s+(given|when|then|and|but)\s+(.+)/i;
// Il marker è TRANSITIVO: resta la pagina attiva per tutti gli step successivi finché
// non compare il marker successivo. currentPage = match[1].trim().toLowerCase().replace(/\s+/g, '-');
// Nei file reali (es. login.feature) il marker è RIPETUTO prima di ogni step (#LOGIN su righe separate).
```

Da web-ui/src/lib/features.ts:
```typescript
export function walkFeatures(dir: string, base?: string): string[]; // relpath POSIX dei .feature
export function listFeatures(featuresDir?: string): FeatureSummary[];
```

Da web-ui/src/lib/repo.ts:
```typescript
export const FEATURES_DIR: string; // src/features/ (root .feature)
```

Pattern "apri nell'editor" (da web-ui/src/app/features/page.tsx, openInEditor):
```typescript
async function openInEditor(file: string) {
  const res = await fetch(`/api/download?file=${encodeURIComponent(file)}`);
  if (!res.ok) return;
  const content = await res.text();
  localStorage.setItem('gsd-editor-incoming', JSON.stringify({ content, filePath: file }));
  router.push('/editor');
}
```

Nav (da web-ui/src/app/layout.tsx, ~riga 32-50): catena di `<Link href="/editor">`, `<Link href="/features">`, poi `<NavSettingsLink />`. Le label usano stringhe inline ("Catalog", "Editor", "Features").

i18n (da web-ui/src/lib/i18n.ts): oggetto `translations.en` / `translations.it`, con sezioni `nav`, `features`, ecc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lib condivisa extractPageMarkers + API GET /api/tags</name>
  <files>web-ui/src/lib/page-markers.ts, web-ui/src/app/api/tags/route.ts</files>
  <read_first>
    - web-ui/src/app/editor/_content.tsx (unknownSteps useMemo ~riga 205: PAGE_MARKER_RE, STEP_LINE_RE, transitività)
    - web-ui/src/lib/features.ts (walkFeatures, listFeatures)
    - web-ui/src/lib/repo.ts (FEATURES_DIR)
    - web-ui/src/app/api/features/route.ts (pattern handler GET con try/catch + NextResponse)
  </read_first>
  <action>
**File 1 — web-ui/src/lib/page-markers.ts** (nuovo):

Crea una funzione pura riusabile che estrae i page marker e conta gli step per pagina da un singolo contenuto `.feature`. Replica ESATTAMENTE la logica di `unknownSteps`:

```typescript
const PAGE_MARKER_RE = /^\s*#\s*#?([A-Z][A-Z0-9 _-]{1,})\s*$/;
const STEP_LINE_RE   = /^\s+(given|when|then|and|but)\s+(.+)/i;

export interface PageMarkerCount {
  page: string;       // slug normalizzato: toLowerCase().replace(/\s+/g, '-')  (es. "login", "brochure-homepage")
  display: string;    // etichetta leggibile dal marker originale trimmato (es. "LOGIN", "BROCHURE HOMEPAGE")
  stepCount: number;  // numero di step (righe Given/When/Then/And/But) sotto questa pagina
}

/** Estrae i marker pagina da UN contenuto .feature. Il marker è transitivo:
 *  conta tutti gli step successivi finché non cambia marker. Aggrega ripetizioni
 *  dello stesso marker (i file reali ripetono #LOGIN prima di ogni step). */
export function extractPageMarkers(content: string): PageMarkerCount[] {
  const counts = new Map<string, { display: string; stepCount: number }>();
  let currentPage = '';
  let currentDisplay = '';
  for (const line of content.split('\n')) {
    const pm = line.match(PAGE_MARKER_RE);
    if (pm) {
      currentDisplay = pm[1].trim();
      currentPage = currentDisplay.toLowerCase().replace(/\s+/g, '-');
      if (!counts.has(currentPage)) counts.set(currentPage, { display: currentDisplay, stepCount: 0 });
      continue;
    }
    const sm = line.match(STEP_LINE_RE);
    if (sm && currentPage) {
      const entry = counts.get(currentPage)!;
      entry.stepCount += 1;
    }
  }
  return [...counts.entries()].map(([page, v]) => ({ page, display: v.display, stepCount: v.stepCount }));
}
```

**File 2 — web-ui/src/app/api/tags/route.ts** (nuovo):

`GET /api/tags` → scansiona tutti i .feature, aggrega per pagina su TUTTI i file. Usa `walkFeatures(FEATURES_DIR)` + `fs.readFileSync` + `extractPageMarkers`. Struttura risposta:

```typescript
// Tipo risposta
interface TagAggregate {
  page: string;        // slug
  display: string;     // label
  stepCount: number;   // somma su tutti i file
  files: string[];     // relpath POSIX dei .feature che contengono questa pagina
}
// GET → NextResponse.json({ pages: TagAggregate[] }) ordinato per stepCount desc, poi page asc.
```

Implementazione: itera i relpath di `walkFeatures(FEATURES_DIR)`; per ciascuno leggi il contenuto con `fs.readFileSync(path.join(FEATURES_DIR, rel), 'utf-8')` dentro try/catch (skip su errore lettura); chiama `extractPageMarkers(content)`; accumula in una `Map<page, TagAggregate>` sommando `stepCount` e aggiungendo il relpath a `files` (dedup). Gestisci errori globali con try/catch → `NextResponse.json({ error }, { status: 500 })` come in `/api/features`. Segui lo stesso stile import (`import * as fs from 'fs'`, `import * as path from 'path'`, `import { NextResponse } from 'next/server'`).
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "page-markers|api/tags" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/src/lib/page-markers.ts` esiste e `grep -n "export function extractPageMarkers" web-ui/src/lib/page-markers.ts` trova la funzione
    - `page-markers.ts` usa lo STESSO `PAGE_MARKER_RE` di _content.tsx e normalizza con `.toLowerCase().replace(/\s+/g, '-')`
    - `web-ui/src/app/api/tags/route.ts` esiste, esporta `GET`, importa `extractPageMarkers` e `walkFeatures`/`FEATURES_DIR`
    - La risposta è `{ pages: [...] }` con campi `page`, `display`, `stepCount`, `files`
    - `cd web-ui && npx tsc --noEmit` non riporta errori nuovi su questi file
  </acceptance_criteria>
  <done>extractPageMarkers conta gli step per pagina con la logica di unknownSteps; GET /api/tags aggrega le pagine su tutti i .feature con count e file associati.</done>
</task>

<task type="auto">
  <name>Task 2: Pagina /tags + link in nav + traduzioni</name>
  <files>web-ui/src/app/tags/page.tsx, web-ui/src/app/layout.tsx, web-ui/src/lib/i18n.ts</files>
  <read_first>
    - web-ui/src/app/features/page.tsx (struttura 'use client', useLanguage, fetch, openInEditor, stile bottoni/card teal)
    - web-ui/src/app/layout.tsx (nav Links)
    - web-ui/src/lib/i18n.ts (struttura translations.en/it, sezione nav)
    - web-ui/src/app/api/tags/route.ts (forma della risposta, dal Task 1)
  </read_first>
  <action>
**File 1 — web-ui/src/app/tags/page.tsx** (nuovo, `'use client'`):

Pagina che fetcha `GET /api/tags` e mostra le pagine. Per ogni pagina: `display` (label leggibile), badge `stepCount` step, e l'elenco dei file associati. Click su un file → apre nell'editor con lo STESSO pattern di `features/page.tsx`:

```typescript
const router = useRouter();
async function openInEditor(file: string) {
  const res = await fetch(`/api/download?file=${encodeURIComponent(file)}`);
  if (!res.ok) return;
  const content = await res.text();
  localStorage.setItem('gsd-editor-incoming', JSON.stringify({ content, filePath: file }));
  router.push('/editor');
}
```

Usa `useLanguage()` per il titolo (`t.tags.title`). Stato: loading (skeleton come in features), error, lista. Stile coerente con features/page.tsx (Tailwind, accenti teal, card/righe con hover). Layout: titolo + lista di card pagina; ogni card mostra `display`, count step, e i file cliccabili. Tipizza la risposta con un'interface locale corrispondente a `{ pages: { page; display; stepCount; files }[] }`.

Caso vuoto: se `pages.length === 0`, mostra `t.tags.empty`.

**File 2 — web-ui/src/app/layout.tsx**:

Aggiungi un `<Link href="/tags" className="text-sm text-teal-100 hover:text-white transition-colors">Tags</Link>` nella nav, subito DOPO il Link Features (riga ~41-43) e PRIMA di `<NavSettingsLink />`. Mantieni lo stesso className degli altri link.

**File 3 — web-ui/src/lib/i18n.ts**:

Aggiungi in ENTRAMBI `translations.en` e `translations.it`:
- in `nav`: campo `tags` (`en: 'Tags'`, `it: 'Tag'`)
- nuova sezione `tags`:
  - en: `{ title: 'Pages / Tags', stepCount: 'steps', empty: 'No page markers found in feature files', files: 'Files' }`
  - it: `{ title: 'Pagine / Tag', stepCount: 'step', empty: 'Nessun marker di pagina trovato nei feature file', files: 'File' }`

(Il link in nav può usare la stringa inline "Tags" per coerenza minima con gli altri link inline, oppure `t.nav.tags` se preferisci — la pagina /tags DEVE usare `t.tags.*`.)
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "tags/page|layout|i18n" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `web-ui/src/app/tags/page.tsx` esiste, è `'use client'`, fetcha `/api/tags` (`grep -n "fetch('/api/tags')" web-ui/src/app/tags/page.tsx`)
    - La pagina usa il pattern `gsd-editor-incoming` + `router.push('/editor')` per aprire i file (`grep -n "gsd-editor-incoming" web-ui/src/app/tags/page.tsx`)
    - `grep -n 'href="/tags"' web-ui/src/app/layout.tsx` trova il link in nav
    - `grep -n "tags:" web-ui/src/lib/i18n.ts` mostra la sezione tags in en E it (almeno 2 occorrenze: nav.tags + sezione tags, per lingua)
    - `cd web-ui && npx tsc --noEmit` non riporta errori nuovi
  </acceptance_criteria>
  <done>/tags elenca le pagine aggregate con count step e link all'editor; il link Tags è in nav; le traduzioni en/it esistono.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifica visiva end-to-end fase 999.13</name>
  <files>(verifica manuale — nessun file modificato)</files>
  <action>Avviare la web-ui e seguire gli step di verifica sotto (how-to-verify), confermando upload→nuovo tab, dialog proposta, pagina /tags. Pausa per conferma utente.</action>
  <verify>Verifica manuale dell'utente secondo how-to-verify (build automatica già coperta dalle task 1-2 e dal piano)</verify>
  <done>L'utente conferma che i tre flussi funzionano e non ci sono errori in console</done>
  <what-built>
    L'intera fase 999.13: (Plan 01) upload .feature → nuovo tab con commenti preservati; (Plan 02) dialog proposta step con page marker dopo l'upload; (Plan 03) pagina /tags con aggregazione page marker + link all'editor.
  </what-built>
  <how-to-verify>
1. Avvia la web-ui: `cd web-ui && npm run dev` → apri http://localhost:3000
2. **Upload + nuovo tab (Plan 01):** vai su /editor. Scrivi qualcosa in un tab. Trascina (o seleziona col dropzone) il file `src/features/brochure-clinic/login/login.feature`. Verifica: si apre un NUOVO tab (il tab precedente è intatto), la label è "Brochure Clinic Login", e i commenti `#LOGIN`, `#MFA`, `#BROCHURE HOMEPAGE` sono presenti identici nel contenuto.
3. **Dialog proposta (Plan 02):** se il file contiene step fuori catalogo, il pannello "step non nel catalogo" si apre automaticamente con gli step pre-selezionati e i badge pagina. Verifica che puoi anche premere "Ignora" senza errori. (Se tutti gli step fossero a catalogo, nessun pannello — comportamento corretto.)
4. **Pagina /tags (Plan 03):** clicca "Tags" in nav (→ /tags). Verifica: compaiono pagine come LOGIN, MFA, BROCHURE HOMEPAGE, LOCKED ACCOUNT, ORDER con un count step plausibile. Clicca un file sotto una pagina → vieni portato su /editor con quel file caricato.
5. Controlla la console del browser: nessun errore React/hydration.
  </how-to-verify>
  <resume-signal>Scrivi "approvato" o descrivi i problemi riscontrati</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/tags | query GET senza input utente nel filesystem path (solo lettura di src/features/) |
| browser → /api/download | già esistente: file param validato da safeFeaturePath |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.13-01 | Information Disclosure | GET /api/tags | mitigate | Legge SOLO sotto FEATURES_DIR via walkFeatures; nessun parametro path dal client. Non esporre path assoluti: i `files` sono relpath POSIX relativi a FEATURES_DIR. |
| T-999.13-02 | Tampering | apri-nell'editor da /tags | accept | Riusa /api/download esistente, già protetto da safeFeaturePath (path traversal + .feature). Nessuna nuova superficie. |
| T-999.13-03 | Denial of Service | extractPageMarkers su file grandi | accept | Lettura locale di file QA piccoli; nessun input esterno non fidato. Rischio basso in contesto desktop/locale. |
</threat_model>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- `cd web-ui && npm run build` compila
- `GET /api/tags` ritorna `{ pages: [...] }` con count > 0 per i file esistenti
- Checkpoint umano (Task 3) approvato
</verification>

<success_criteria>
- /tags esiste, raggiungibile da nav, elenca i page marker aggregati con count step
- Click su un file in /tags apre l'editor su quel file
- extractPageMarkers condivisa, allineata alla logica di unknownSteps
- Verifica visiva end-to-end dell'intera fase approvata dall'utente
</success_criteria>

<output>
After completion, create `.planning/phases/999.13-feature-file-import-nell-editor-sezione-tags-hashtag/999.13-03-SUMMARY.md`
</output>
