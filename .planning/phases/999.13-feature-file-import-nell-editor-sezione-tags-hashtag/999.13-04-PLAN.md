---
phase: 999.13-feature-file-import-nell-editor-sezione-tags-hashtag
plan: 04
type: execute
wave: 4
depends_on: []
gap_closure: true
files_modified:
  - scripts/import-scenarios.ts
autonomous: true
requirements: [IMPORT-FIX-01]
must_haves:
  truths:
    - "buildFeatureContent NON sostituisce più il tag originale (@app @flow) con @import<timestamp>: se la feature importata ha un proprio tag line, viene preservato verbatim"
    - "buildFeatureContent NON scarta più le righe # #PAGINA (page marker) presenti negli scenari: vengono riemesse nel .feature generato"
    - "Il titolo Feature: <nome> riflette il nome reale della feature importata (non 'Imported' quando un Feature: è presente)"
    - "area NON ripiega più su slugify(inputBasename) quando questo è un nome temporaneo tipo import-<timestamp>: la directory di destinazione non contiene più import-<timestamp>"
  artifacts:
    - path: "scripts/import-scenarios.ts"
      provides: "buildFeatureContent preserva tag originali + page marker; derivazione area robusta (no tmp basename)"
      contains: "rawHeaderLines"
  key_links: []
---

<objective>
Correggere il bug per cui l'import server (`POST /api/import` → `scripts/import-scenarios.ts`) **distrugge** il contenuto della feature: il tag originale `@app @flow` viene sostituito da `@import<timestamp>` e tutte le righe `# #PAGINA` (page marker) vengono scartate.

Purpose: l'obiettivo dell'intera fase 999.13 è *preservare i commenti `# #PAGINA`*. Il path di import `.txt` server-side li distrugge in `buildFeatureContent`, che ricostruisce la feature da zero tenendo solo `Given/When/Then/And/But`. Inoltre, quando il nome Feature non si risolve, `area` ripiega sul basename del file temporaneo (`import-<timestamp>`), producendo tag e directory tipo `@import1750857600000`. I dati originali ci sono già nel parser (`rawHeaderLines` contiene il tag e il `Feature:`; `scenario.rawLines` contiene le righe `#...`): il fix è interamente in `buildFeatureContent` + la derivazione di `area`.

Output: `buildFeatureContent` preserva tag originali, titolo e page marker; `area` non usa mai il basename del tmp file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Strutture già esistenti in scripts/import-scenarios.ts (NON modificare il parser, i dati ci sono già):

```typescript
interface ParsedScenario { type: 'Scenario' | 'Scenario Outline'; name: string; steps: ...[]; rawLines: string[]; }
interface ParsedFeature  { name: string; rawHeaderLines: string[]; scenarios: ParsedScenario[]; }
// parseScenarios(content): rawHeaderLines = righe PRIMA del primo Scenario (include il tag "@app @flow" e "Feature: ..."),
//                          scenario.rawLines = TUTTE le righe grezze dello scenario (incluse le righe "# #PAGINA").
function slugify(s: string): string;  // [^a-z0-9]+ → '-'
```

Funzione da correggere (scripts/import-scenarios.ts ~riga 364):
```typescript
function buildFeatureContent(feature: ParsedFeature, area: string): string {
  const lines: string[] = [];
  lines.push(`@${area}`);                  // ← BUG: sovrascrive il tag originale
  lines.push(`Feature: ${feature.name}`);
  lines.push('');
  for (const scenario of feature.scenarios) {
    lines.push(`  ${scenario.type}: ${scenario.name}`);
    let inExamples = false;
    for (const rawLine of scenario.rawLines) {
      const trimmed = rawLine.trim();
      if (trimmed.match(/^(Scenario Outline|Scenario):/)) continue;
      if (trimmed.match(/^Examples:/i)) { inExamples = true; lines.push(`    Examples:`); continue; }
      if (inExamples && trimmed.startsWith('|')) { lines.push(`      ${trimmed}`); continue; }
      const stepMatch = trimmed.match(/^(Given|When|Then|And|But|\*)\s+(.*)/);
      if (stepMatch) { lines.push(`    ${stepMatch[1]} ${stepMatch[2]}`); continue; }
      // ← BUG: righe "# #PAGINA" cadono qui e vengono scartate
      if (trimmed === '') { lines.push(''); }
    }
    lines.push('');
  }
  return lines.join('\n');
}
```

Derivazione area (scripts/import-scenarios.ts ~riga 255):
```typescript
const area = areaArg ? slugify(areaArg) : slugify(feature.name) || slugify(inputBasename);
// ← BUG: con feature.name vuoto e nessun --area, area = slugify("import-1750857600000")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildFeatureContent preserva tag originale + page marker</name>
  <files>scripts/import-scenarios.ts</files>
  <read_first>
    - scripts/import-scenarios.ts (parseScenarios ~riga 90-171, buildFeatureContent ~riga 364, derivazione area ~riga 254-259)
    - src/features/brochure-clinic/login/login.feature (formato reale: "@brochure-clinic @login" + "Feature: ..." + "#PAGINA" prima di ogni step)
  </read_first>
  <action>
Modifica **solo** `buildFeatureContent` e la derivazione di `area` in `scripts/import-scenarios.ts`.

**A) Preserva il tag originale** invece di forzare `@${area}`:
- Cerca in `feature.rawHeaderLines` la prima riga che (trimmata) inizia con `@` (tag line Gherkin).
- Se trovata → emetti quella riga verbatim (trimmata) come prima riga.
- Se NON trovata → fallback all'attuale `@${area}` (così l'import di file senza tag continua a funzionare).

**B) Preserva le righe `# #PAGINA` (e ogni commento) dentro gli scenari**:
- Nel loop su `scenario.rawLines`, PRIMA del ramo finale, aggiungi: se `trimmed.startsWith('#')` → `lines.push('    ' + trimmed)` e `continue`. Così i page marker (`#LOGIN`, `#BROCHURE HOMEPAGE`, …) vengono riemessi con indentazione coerente con gli step (4 spazi), come fa il parser client `parseExtendedFormat`.

**C) Titolo**: lascia `Feature: ${feature.name}`; `feature.name` è già il nome reale quando un `Feature:` è presente nel sorgente. (Nessuna modifica necessaria oltre a non romperlo.)

**D) area robusta** (derivazione ~riga 255): evita il fallback al basename del tmp file. Nuova logica:
```typescript
// Deriva app/area dal tag originale se presente (es. "@brochure-clinic @login" → app=brochure-clinic, area=login)
const headerTagLine = feature.rawHeaderLines.find(l => l.trim().startsWith('@'));
const headerTags = headerTagLine ? (headerTagLine.trim().match(/@(\S+)/g) ?? []).map(s => slugify(s.slice(1))) : [];
const area = areaArg
  ? slugify(areaArg)
  : (slugify(feature.name) || headerTags[1] || headerTags[0] || 'imported');
```
Punti chiave: l'ordine di fallback NON include più `slugify(inputBasename)` (che produceva `import-<timestamp>`); usa il nome feature, poi i tag, poi la costante `'imported'`. Se `appArg` è ancora il default e ci sono header tag, è accettabile usare `headerTags[0]` per `appArg` SOLO se questo non rompe la firma esistente — se complica, lascia `appArg` invariato e limita la modifica ad `area` (l'obiettivo critico è eliminare `import-<timestamp>`).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "import-scenarios" || echo "OK: nessun errore tsc su import-scenarios"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rawHeaderLines.find" scripts/import-scenarios.ts` trova la ricerca del tag header in buildFeatureContent
    - `buildFeatureContent` emette la riga `#...` (page marker): `grep -n "startsWith('#')" scripts/import-scenarios.ts` la trova nel loop scenari
    - La derivazione di `area` NON contiene più `slugify(inputBasename)` come fallback: `grep -n "slugify(inputBasename)" scripts/import-scenarios.ts` non compare nel ramo di `area` (può restare altrove se usato per altro)
    - Type-check del progetto scaffold passa (`npx tsc --noEmit`)
    - Test di non-regressione manuale (documentato in SUMMARY): un contenuto con `@brochure-clinic @login` + `Feature: X` + righe `#LOGIN` produce, via buildFeatureContent, un output che CONTIENE `@brochure-clinic @login`, `Feature: X` e almeno una riga `#LOGIN`
  </acceptance_criteria>
  <done>buildFeatureContent preserva tag originale, titolo e page marker; area non usa più il basename del file temporaneo.</done>
</task>

<task type="auto">
  <name>Task 2: Verifica con il file d'esempio reale (round-trip)</name>
  <files>scripts/import-scenarios.ts</files>
  <read_first>
    - src/features/brochure-clinic/login/login.feature
  </read_first>
  <action>
Aggiungi un controllo di non-regressione **leggero e auto-contenuto** (NON un nuovo framework di test): se nel repo esiste già una suite per gli script (`scripts/__tests__` o simili) aggiungi lì un caso; altrimenti documenta nel SUMMARY un round-trip manuale eseguito con un piccolo snippet `npx ts-node -e "..."` che importa `buildFeatureContent`/`parseScenarios` su un contenuto di esempio (tag + Feature + due step con `#LOGIN`/`#MFA`) e verifica che l'output contenga tag originale e page marker.

Non introdurre dipendenze nuove. Se ts-node inline non è praticabile in questo ambiente, esegui invece il dry-run del progetto (`npm run test:dry`) per assicurarti che gli step esistenti continuino a risolvere, e annota l'esito nel SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && (npm run test:dry 2>&1 | tail -5 || echo "test:dry non disponibile — annota nel SUMMARY")</automated>
  </verify>
  <acceptance_criteria>
    - Il SUMMARY documenta un round-trip (inline ts-node o test) che dimostra preservazione di tag + `#PAGINA`, OPPURE l'esito di `npm run test:dry`
    - Nessuna nuova dipendenza aggiunta a package.json
  </acceptance_criteria>
  <done>Verifica di non-regressione documentata: l'import server preserva tag e page marker sul contenuto d'esempio.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passa sullo scaffold
- buildFeatureContent su contenuto con tag + `#PAGINA` preserva entrambi
- Nessun `import-<timestamp>` né come tag né come directory di destinazione
</verification>

<success_criteria>
- L'import `.txt` server-side non sostituisce più il tag originale con `@import<timestamp>`
- Le righe `# #PAGINA` sopravvivono all'import server
- area di destinazione non deriva più dal basename del file temporaneo
</success_criteria>

<output>
After completion, create `.planning/phases/999.13-feature-file-import-nell-editor-sezione-tags-hashtag/999.13-04-SUMMARY.md`
</output>
