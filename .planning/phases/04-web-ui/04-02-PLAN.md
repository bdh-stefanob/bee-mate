---
phase: 04-web-ui
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - web-ui/components/StepCatalog.tsx
  - web-ui/lib/catalog.ts
  - web-ui/app/page.tsx
  - web-ui/__tests__/lib/catalog.test.ts
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "La home (/) mostra una tabella con tutti gli step del catalog (colonne expression, area, status, app)"
    - "L'utente può filtrare gli step per testo libero (match su expression), per area e per status"
    - "I badge status sono colorati: wanted arancio #FF6B2C, implemented verde #2ECC71, deprecated grigio #9CA3AF"
    - "Doppio click su una riga step apre l'editor con quello step pre-inserito"
  artifacts:
    - path: "web-ui/components/StepCatalog.tsx"
      provides: "tabella step con filtri e badge (Client Component)"
      min_lines: 60
    - path: "web-ui/lib/catalog.ts"
      provides: "filterSteps(steps, {query, area, status}) pura e testabile"
      contains: "export function filterSteps"
  key_links:
    - from: "web-ui/components/StepCatalog.tsx"
      to: "/api/catalog"
      via: "fetch in useEffect"
      pattern: "fetch\\(['\"]/api/catalog"
    - from: "web-ui/components/StepCatalog.tsx"
      to: "/editor?step=..."
      via: "router.push onDoubleClick"
      pattern: "onDoubleClick"
---

<objective>
Pagina Catalog completa: `StepCatalog.tsx` (Client Component) carica gli step da `/api/catalog`, li mostra in tabella shadcn con filtri (testo libero su expression, dropdown area, dropdown status), badge status colorati Boots-friendly, e doppio click su riga → naviga all'editor con lo step pre-inserito (`/editor?step=<encoded>`). La logica di filtro vive in `lib/catalog.ts` (funzione pura, testata).

Purpose: è una delle due pagine "wow" della demo. Deve essere immediata, cercabile, leggibile su 1080p.
Output: home funzionante con catalog ricercabile e link all'editor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui/04-CONTEXT.md
@.planning/phases/04-web-ui/04-RESEARCH.md
@web-ui/lib/types.ts
@web-ui/lib/repo.ts
@web-ui/app/api/catalog/route.ts

<interfaces>
<!-- Contratti dal plan 01 (già in codebase). Usare direttamente, niente esplorazione. -->

web-ui/lib/types.ts:
```ts
export interface CatalogStep {
  expression: string; parameters: string[]; app: string; area: string;
  domain: string; status: 'implemented' | 'wanted' | 'deprecated';
  page?: string; requester?: string; sourceRef: string; documented: boolean;
}
export interface Catalog { totalSteps: number; steps: CatalogStep[]; }
```

GET /api/catalog → ritorna l'intero step-catalog.json: `{ totalSteps, steps: CatalogStep[] }`.

Stili badge (da CONTEXT.md "Badge colori Boots-friendly" + RESEARCH "Code Examples"):
```ts
const STATUS_STYLES = {
  wanted:      'bg-[#FF6B2C] text-white',
  implemented: 'bg-[#2ECC71] text-white',
  deprecated:  'bg-[#9CA3AF] text-white',
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/catalog.ts — filterSteps pura + estrazione facets</name>
  <read_first>
    - web-ui/lib/types.ts (CatalogStep)
    - step-catalog.json (valori reali di area e status per i test)
  </read_first>
  <files>web-ui/lib/catalog.ts, web-ui/__tests__/lib/catalog.test.ts</files>
  <behavior>
    - Test 1: filterSteps(steps, {query:'basket'}) ritorna solo step la cui expression contiene 'basket' (case-insensitive)
    - Test 2: filterSteps(steps, {area:'auth'}) ritorna solo step con area==='auth'
    - Test 3: filterSteps(steps, {status:'wanted'}) ritorna solo step con status==='wanted'
    - Test 4: filterSteps(steps, {}) ritorna tutti gli step
    - Test 5: filtri combinati (query+area+status) applicano AND
    - Test 6: uniqueAreas(steps) ritorna le aree distinte ordinate; uniqueStatuses(steps) ritorna gli status distinti
  </behavior>
  <action>
    Creare web-ui/lib/catalog.ts:
    ```ts
    import type { CatalogStep } from './types';
    export interface CatalogFilter { query?: string; area?: string; status?: string; }
    export function filterSteps(steps: CatalogStep[], f: CatalogFilter): CatalogStep[] { /* AND su query (includes lowercase su expression), area (eq), status (eq); campi vuoti/undefined = no filtro */ }
    export function uniqueAreas(steps: CatalogStep[]): string[] { /* distinct sorted */ }
    export function uniqueStatuses(steps: CatalogStep[]): string[] { /* distinct sorted */ }
    ```
    Trattare query/area/status vuoti ('' o undefined) come "nessun filtro".
  </action>
  <acceptance_criteria>
    - `grep 'export function filterSteps' web-ui/lib/catalog.ts` ha match
    - `grep 'export function uniqueAreas' web-ui/lib/catalog.ts` ha match
    - `cd web-ui && npm test -- catalog` passa tutti i test del behavior (6)
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npm test -- catalog</automated>
  </verify>
  <done>filterSteps + facets puri e testati.</done>
</task>

<task type="auto">
  <name>Task 2: StepCatalog.tsx — tabella, filtri, badge, doppio click→editor; montato in page.tsx</name>
  <read_first>
    - web-ui/lib/catalog.ts (Task 1)
    - web-ui/lib/types.ts
    - .planning/phases/04-web-ui/04-CONTEXT.md (sezione "Pagina Catalog")
    - .planning/phases/04-web-ui/04-RESEARCH.md ("Badge status Boots-friendly")
  </read_first>
  <files>web-ui/components/StepCatalog.tsx, web-ui/app/page.tsx</files>
  <action>
    Creare web-ui/components/StepCatalog.tsx come Client Component (`'use client'`):
    - State: `steps: CatalogStep[]`, `query`, `area`, `status`, `loading`, `error`.
    - useEffect on mount: `fetch('/api/catalog')` → setSteps(json.steps). Gestire errore con messaggio inline.
    - Filtri UI: Input shadcn (search su expression), Select shadcn per area (opzioni da uniqueAreas + "Tutte"), Select shadcn per status (uniqueStatuses + "Tutti").
    - Tabella shadcn (Table/TableHeader/TableBody/TableRow/TableCell): colonne Expression, Area, Status, App. Righe = filterSteps(steps, {query, area, status}).
    - Badge status con STATUS_STYLES (arancio/verde/grigio). Usare il componente Badge shadcn con className override.
    - Doppio click su TableRow: `const router = useRouter(); router.push('/editor?step=' + encodeURIComponent(<keyword di default 'Given'> + ' ' + step.expression))`. Nota: l'editor (plan 03) leggerà `searchParams.get('step')`. Per coerenza inserire l'expression con prefisso keyword `Given ` di default (l'utente potrà cambiarla nell'editor).
    - Mostrare conteggio risultati (es. "N step").
    - Layout usabile su 1080p: container con padding, tabella full-width, niente paginazione (max ~50 step).
    Montare `<StepCatalog />` in web-ui/app/page.tsx (sostituendo il placeholder), con un titolo "Step Catalog".
  </action>
  <acceptance_criteria>
    - `grep "'use client'" web-ui/components/StepCatalog.tsx` ha match
    - `grep "fetch('/api/catalog')\|fetch(\"/api/catalog\")" web-ui/components/StepCatalog.tsx` ha match
    - `grep '#FF6B2C' web-ui/components/StepCatalog.tsx` e `grep '#2ECC71' web-ui/components/StepCatalog.tsx` e `grep '#9CA3AF' web-ui/components/StepCatalog.tsx` hanno match
    - `grep 'filterSteps\|uniqueAreas\|uniqueStatuses' web-ui/components/StepCatalog.tsx` ha match
    - `grep 'onDoubleClick' web-ui/components/StepCatalog.tsx` ha match e usa `router.push` verso `/editor?step=`
    - `grep 'StepCatalog' web-ui/app/page.tsx` ha match (componente montato)
    - `cd web-ui && npx tsc --noEmit` esce 0
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>Home mostra tabella step filtrabile, badge colorati, doppio click naviga all'editor con lo step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pagina Catalog completa su http://localhost:3000 : tabella step da step-catalog.json, ricerca testuale, filtri area/status, badge colorati, doppio click → editor.</what-built>
  <how-to-verify>
    1. In `web-ui/` esegui `npm run dev`.
    2. Apri http://localhost:3000 — verifica che la tabella mostri ~18 step con colonne Expression/Area/Status/App.
    3. Scrivi "basket" nella ricerca → la lista si filtra.
    4. Seleziona area "auth" e status "implemented" → la lista si restringe coerentemente.
    5. Verifica i badge: wanted = arancio, implemented = verde, deprecated = grigio.
    6. Doppio click su una riga → vieni portato su /editor con lo step nell'URL (?step=...).
  </how-to-verify>
  <resume-signal>Scrivi "approvato" oppure descrivi i problemi visti.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/catalog | sola lettura di step-catalog.json, nessun input utente verso fs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | StepCatalog → /api/catalog | accept | Espone solo il catalog del repo (nessun dato sensibile), tool localhost-only. |
| T-04-05 | Tampering | router.push('/editor?step=' + encodeURIComponent(...)) | mitigate | encodeURIComponent sul valore step prima di metterlo in URL; l'editor tratterà il valore come testo plain, non come HTML/eval. |
</threat_model>

<verification>
- `cd web-ui && npm test -- catalog` → verde
- `cd web-ui && npx tsc --noEmit` → 0
- Smoke visivo (checkpoint): tabella, filtri, badge, doppio click→editor.
</verification>

<success_criteria>
- UI-01 soddisfatto: catalog ricercabile e filtrabile con badge status corretti e link all'editor funzionante.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui/04-02-SUMMARY.md`
</output>
