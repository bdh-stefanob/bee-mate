---
phase: 04-web-ui
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-03]
files_modified:
  - web-ui/lib/features.ts
  - web-ui/app/api/features/route.ts
  - web-ui/app/api/download/route.ts
  - web-ui/components/FeaturePreview.tsx
  - web-ui/app/features/page.tsx
  - web-ui/__tests__/lib/features.test.ts
  - web-ui/__tests__/api/download.test.ts
autonomous: true
requirements: [UI-02, UI-04, UI-06]
must_haves:
  truths:
    - "La pagina /features elenca tutti i src/features/**/*.feature del repo (nome file, area, n scenari)"
    - "Click su un feature → mostra il contenuto formattato in FeaturePreview"
    - "GET /api/download?file=<rel> serve il .feature; risponde 403 su path traversal e su estensione non .feature"
    - "walkFeatures ritorna solo file con estensione .feature"
  artifacts:
    - path: "web-ui/lib/features.ts"
      provides: "walkFeatures + parseFeatureSummary puri e testati"
      contains: "export function walkFeatures"
    - path: "web-ui/app/api/download/route.ts"
      provides: "GET con guard path-traversal (safeFeaturePath)"
      contains: "safeFeaturePath"
    - path: "web-ui/components/FeaturePreview.tsx"
      provides: "anteprima .feature formattata"
      min_lines: 20
  key_links:
    - from: "web-ui/app/api/download/route.ts"
      to: "lib/repo.safeFeaturePath"
      via: "guard prima di leggere il file"
      pattern: "safeFeaturePath"
    - from: "web-ui/components/FeaturePreview.tsx"
      to: "/api/download?file="
      via: "fetch del contenuto + link download"
      pattern: "fetch\\(.*/api/download"
---

<objective>
Pagina catalogo feature files: `/api/features` elenca i `.feature` del repo (riusando `walkFeatures` + parsing in `lib/features.ts`), la pagina `/features` mostra la lista (nome, area, n scenari) e al click apre `FeaturePreview` con il contenuto. `/api/download?file=<rel>` serve il file con guard path-traversal (`safeFeaturePath` dal plan 01) e Content-Disposition. Questa pagina è volutamente minimale (priorità demo: catalog + editor).

Purpose: chiudere il loop — i .feature generati dall'import (plan 03) sono visibili e scaricabili. Completa UI-02/UI-04/UI-06.
Output: pagina features funzionante + download sicuro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui/04-CONTEXT.md
@.planning/phases/04-web-ui/04-RESEARCH.md
@web-ui/lib/types.ts
@web-ui/lib/repo.ts
@web-ui/app/api/features/route.ts
@web-ui/app/api/download/route.ts
@src/features/auth/login.feature

<interfaces>
<!-- Contratti dai plan 01. Usare direttamente. -->

web-ui/lib/repo.ts:
```ts
export const REPO_ROOT: string;
export const FEATURES_DIR: string;                 // = <repo>/src/features
export function safeFeaturePath(rel: string): string | null;  // guard path-traversal + .feature
```
web-ui/lib/types.ts → FeatureSummary { file, name, area, scenarioCount }

Formato .feature (verificato — src/features/auth/login.feature):
```
@auth
Feature: Authentication
  Scenario: ...
  Scenario Outline: ...
```
Nota: il plan 01 ha già creato /api/features funzionante e /api/download come stub 501.
Questo plan SPOSTA la logica di walk/parse in lib/features.ts (testabile) e IMPLEMENTA download.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/features.ts — walkFeatures + parseFeatureSummary; refactor /api/features per usarli</name>
  <read_first>
    - src/features/auth/login.feature (formato: tag @area, Feature:, Scenario/Scenario Outline)
    - web-ui/lib/repo.ts (FEATURES_DIR)
    - .planning/phases/04-web-ui/04-RESEARCH.md ("Glob .feature files ricorsivo")
    - web-ui/app/api/features/route.ts (logica attuale dal plan 01, da estrarre)
  </read_first>
  <files>web-ui/lib/features.ts, web-ui/app/api/features/route.ts, web-ui/__tests__/lib/features.test.ts</files>
  <behavior>
    - Test 1: walkFeatures(FEATURES_DIR) ritorna SOLO path che terminano in '.feature' (nessun altro file)
    - Test 2: walkFeatures include i file nelle sottocartelle (auth/login.feature, orders/..., checkout/...)
    - Test 3: i path ritornati sono relativi a FEATURES_DIR e usano '/' (non '\') anche su Windows
    - Test 4: parseFeatureSummary(content, file) estrae name da 'Feature:', area dal tag '@' (o dalla cartella se assente), scenarioCount = numero di righe 'Scenario:'/'Scenario Outline:'
  </behavior>
  <action>
    Creare web-ui/lib/features.ts:
    ```ts
    import * as fs from 'fs'; import * as path from 'path';
    import type { FeatureSummary } from './types';
    export function walkFeatures(dir: string, base = dir): string[] { /* ricorsivo, solo .feature, relativi a base, replace \\ -> / */ }
    export function parseFeatureSummary(content: string, relFile: string): FeatureSummary {
      // name: match /^Feature:\s*(.*)/m
      // area: match /^@(\S+)/m sulla prima riga tag, fallback = prima cartella di relFile
      // scenarioCount: count di /^\s*(Scenario|Scenario Outline):/gm
    }
    export function listFeatures(featuresDir: string): FeatureSummary[] { /* walkFeatures + readFileSync + parseFeatureSummary per ciascuno */ }
    ```
    Refactor web-ui/app/api/features/route.ts: il GET ora chiama `listFeatures(FEATURES_DIR)` e ritorna NextResponse.json(result). Stessa shape FeatureSummary[] del plan 01 — nessuna regressione UI.
  </action>
  <acceptance_criteria>
    - `grep 'export function walkFeatures' web-ui/lib/features.ts` ha match
    - `grep 'export function parseFeatureSummary' web-ui/lib/features.ts` ha match
    - `grep 'listFeatures\|walkFeatures' web-ui/app/api/features/route.ts` ha match (route usa il lib)
    - `cd web-ui && npm test -- features` passa tutti i 4 test del behavior
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npm test -- features</automated>
  </verify>
  <done>walkFeatures/parseFeatureSummary puri e testati; /api/features li usa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/download — serve .feature con guard path-traversal</name>
  <read_first>
    - web-ui/lib/repo.ts (safeFeaturePath — guard creato nel plan 01)
    - .planning/phases/04-web-ui/04-RESEARCH.md (Pattern 2 download + Security Domain path traversal)
    - web-ui/app/api/download/route.ts (stub 501 dal plan 01, da sostituire)
  </read_first>
  <files>web-ui/app/api/download/route.ts, web-ui/__tests__/api/download.test.ts</files>
  <behavior>
    - Test 1: GET con ?file=auth/login.feature → status 200, body contiene 'Feature:', header Content-Disposition con filename login.feature
    - Test 2: GET con ?file=../../package.json → status 403 (path traversal bloccato)
    - Test 3: GET con ?file=auth/login.txt → status 403 (estensione non .feature)
    - Test 4: GET con ?file=auth/nonexistent.feature → status 404
  </behavior>
  <action>
    Sostituire lo stub. Implementare GET(request):
    1. `const file = new URL(request.url).searchParams.get('file') ?? '';`
    2. `const resolved = safeFeaturePath(file);` — se null → `new Response('Forbidden', { status: 403 })` (copre traversal + estensione).
    3. Se `!fs.existsSync(resolved)` → 404.
    4. Leggere il contenuto, ritornare:
       ```ts
       return new Response(content, { headers: {
         'Content-Type': 'text/plain; charset=utf-8',
         'Content-Disposition': `attachment; filename="${path.basename(resolved)}"`,
       }});
       ```
    Riusare il guard centralizzato safeFeaturePath (NON reimplementare la validazione — DRY con plan 01).
  </action>
  <acceptance_criteria>
    - `grep 'safeFeaturePath' web-ui/app/api/download/route.ts` ha match (guard riusato)
    - `grep '403' web-ui/app/api/download/route.ts` e `grep '404' web-ui/app/api/download/route.ts` hanno match
    - `grep 'Content-Disposition' web-ui/app/api/download/route.ts` ha match
    - `cd web-ui && npm test -- download` passa tutti i 4 test del behavior (incluso 403 su traversal — UI-04)
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npm test -- download</automated>
  </verify>
  <done>Download sicuro: 200 sui .feature validi, 403 su traversal/estensione, 404 su inesistente.</done>
</task>

<task type="auto">
  <name>Task 3: FeaturePreview.tsx + features/page.tsx</name>
  <read_first>
    - web-ui/lib/types.ts (FeatureSummary)
    - .planning/phases/04-web-ui/04-CONTEXT.md (sezione "Catalogo Feature files")
    - web-ui/app/api/features/route.ts e web-ui/app/api/download/route.ts (Task 1/2)
  </read_first>
  <files>web-ui/components/FeaturePreview.tsx, web-ui/app/features/page.tsx</files>
  <action>
    FeaturePreview.tsx (`'use client'`):
    - Prop `file: string | null` (path relativo). Se file !== null: `fetch('/api/download?file=' + encodeURIComponent(file))` → testo → mostra in `<pre className="font-mono ...">` dentro una Card shadcn.
    - Link/pulsante "Download" che punta a `/api/download?file=<encoded>` (download diretto del file del repo).
    features/page.tsx — Client Component (`'use client'`):
    - useEffect: fetch('/api/features') → setFeatures(FeatureSummary[]).
    - Render lista (Card o Table): per ogni feature mostra name, area (badge), scenarioCount, file.
    - State `selected: string | null`. Click su un item → setSelected(item.file).
    - Render `<FeaturePreview file={selected} />` in un pannello laterale.
    - Layout minimale ma pulito (questa è la pagina meno prioritaria per la demo).
    Titolo "Feature Catalog".
  </action>
  <acceptance_criteria>
    - `grep "'use client'" web-ui/components/FeaturePreview.tsx` ha match
    - `grep '/api/download' web-ui/components/FeaturePreview.tsx` ha match
    - `grep "fetch('/api/features')\|fetch(\"/api/features\")" web-ui/app/features/page.tsx` ha match
    - `grep 'FeaturePreview' web-ui/app/features/page.tsx` ha match (montato)
    - `grep 'scenarioCount\|area' web-ui/app/features/page.tsx` ha match (mostra metadati)
    - `cd web-ui && npx tsc --noEmit` esce 0
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>Pagina /features elenca i .feature con metadati, click apre preview, download funziona.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/download?file= | parametro file controllato dall'utente usato per leggere dal filesystem |
| browser → /api/features | sola lettura ricorsiva di src/features/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | api/download ?file= | mitigate | safeFeaturePath (plan 01): path.resolve + prefix check su FEATURES_DIR + suffisso .feature → 403 su `../` e su estensioni diverse. Test automatici 403/404 obbligatori. |
| T-04-11 | Information Disclosure | api/features walk | accept | Elenca solo src/features/**/*.feature del repo (contenuti già pubblici nel repo), tool localhost-only. |
| T-04-12 | Tampering | FeaturePreview file param | mitigate | encodeURIComponent sul file prima di costruire la query; la validazione reale avviene server-side in safeFeaturePath. |
</threat_model>

<verification>
- `cd web-ui && npm test -- features` e `npm test -- download` → verdi (inclusi i test 403/404)
- `cd web-ui && npx tsc --noEmit` → 0
- `cd web-ui && npm test` full suite → verde
- Smoke: /features elenca i .feature, click apre preview, download scarica il file.
</verification>

<success_criteria>
- UI-02, UI-04, UI-06 soddisfatti: lista feature + preview + download sicuro (path traversal bloccato e testato).
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui/04-04-SUMMARY.md`
</output>
