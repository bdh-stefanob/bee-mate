---
phase: 04-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/package.json
  - web-ui/next.config.ts
  - web-ui/tsconfig.json
  - web-ui/components.json
  - web-ui/vitest.config.ts
  - web-ui/app/layout.tsx
  - web-ui/app/page.tsx
  - web-ui/app/editor/page.tsx
  - web-ui/app/features/page.tsx
  - web-ui/app/globals.css
  - web-ui/lib/repo.ts
  - web-ui/lib/types.ts
  - web-ui/app/api/catalog/route.ts
  - web-ui/app/api/features/route.ts
  - web-ui/app/api/import/route.ts
  - web-ui/app/api/download/route.ts
  - web-ui/README.md
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06]
must_haves:
  truths:
    - "npm run dev in web-ui/ avvia il server su localhost:3000 senza errori"
    - "Le tre pagine (/, /editor, /features) caricano con la nav comune"
    - "Le 4 API route esistono e rispondono (anche se stub per import/download)"
    - "REPO_ROOT risolve alla root del repo (un livello sopra web-ui/)"
    - "npm test in web-ui/ esegue Vitest (anche con 0 test passanti inizialmente)"
  artifacts:
    - path: "web-ui/lib/repo.ts"
      provides: "REPO_ROOT condiviso + helper path-safe"
      contains: "REPO_ROOT"
    - path: "web-ui/lib/types.ts"
      provides: "CatalogStep, Catalog, FeatureSummary contracts"
      contains: "interface CatalogStep"
    - path: "web-ui/app/layout.tsx"
      provides: "root layout con nav Catalog/Editor/Features"
      min_lines: 20
    - path: "web-ui/app/api/catalog/route.ts"
      provides: "GET handler che legge step-catalog.json"
      exports: ["GET"]
    - path: "web-ui/vitest.config.ts"
      provides: "config Vitest environment node"
      contains: "environment"
  key_links:
    - from: "web-ui/app/api/catalog/route.ts"
      to: "step-catalog.json (repo root)"
      via: "fs.readFileSync con REPO_ROOT"
      pattern: "REPO_ROOT"
    - from: "web-ui/lib/repo.ts"
      to: "process.cwd() + '..'"
      via: "path.resolve"
      pattern: "process\\.cwd\\(\\)"
---

<objective>
Scaffolding completo della web-ui: progetto Next.js 15 App Router + TypeScript + Shadcn/ui + Tailwind v4 in `web-ui/`, con le tre pagine vuote ma navigabili, i contract types condivisi, l'helper `REPO_ROOT`, le 4 API route (catalog e features funzionanti, import e download come stub tipizzati) e Vitest configurato.

Purpose: dare a tutte le wave successive una base che compila, naviga e ha i contratti (types + REPO_ROOT) già pronti — niente scavenger hunt. Le wave 2/3/4 implementano contro questi contratti.
Output: web-ui/ avviabile con `npm run dev`, 3 pagine, 4 route, types condivisi, test runner pronto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-web-ui/04-CONTEXT.md
@.planning/phases/04-web-ui/04-RESEARCH.md
@CLAUDE.md
@step-catalog.json
@scripts/import-scenarios.ts

<interfaces>
<!-- Contratti che questo plan CREA e che le wave 2/3/4 consumeranno. -->
<!-- Struttura reale di step-catalog.json (verificata): chiave top-level `steps[]`. -->

step-catalog.json (root del repo):
```json
{
  "generatedAt": "ISO",
  "totalSteps": 18,
  "documentedSteps": 17,
  "undocumentedSteps": 1,
  "steps": [
    {
      "expression": "I add {string} to the basket",
      "parameters": ["{string}"],
      "app": "app-a",
      "area": "imported",
      "domain": "app-a/imported",
      "status": "wanted",          // "implemented" | "wanted" | "deprecated"
      "page": "CartPage",          // opzionale
      "requester": "TBD",          // opzionale
      "sourceRef": "src/steps/...",
      "doc": { "intent": "...", "params": {} },
      "documented": true
    }
  ]
}
```

Feature file (src/features/<area>/<slug>.feature) — formato verificato:
```
@auth
Feature: Authentication

  Scenario: User logs in with valid credentials
    Given ...
  Scenario Outline: ...
    Examples:
      | role |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inizializza progetto Next.js 15 + shadcn + Vitest in web-ui/</name>
  <read_first>
    - .planning/phases/04-web-ui/04-RESEARCH.md (sezioni "Standard Stack", "Installation", "Wave 0 Gaps")
    - package.json (root — Node v24, conferma assenza workspace monorepo)
  </read_first>
  <files>web-ui/package.json, web-ui/next.config.ts, web-ui/tsconfig.json, web-ui/components.json, web-ui/vitest.config.ts, web-ui/app/globals.css</files>
  <action>
    Dalla repo root eseguire (shell:true su Windows):
    1. `npx --yes create-next-app@15 web-ui --ts --app --tailwind --eslint --src-dir false --import-alias "@/*" --use-npm --yes`
       (Se i flag interattivi bloccano, usare `npx create-next-app@15 web-ui --yes` e accettare i default; l'obiettivo è App Router + TS + Tailwind v4.)
    2. In web-ui/: `npx --yes shadcn@latest init --yes --base-color neutral` (genera components.json + globals.css con @theme).
    3. In web-ui/: `npx --yes shadcn@latest add button badge input select table card separator sonner --yes`
       (sonner = toast moderno raccomandato da shadcn; se `toast` deprecato usare sonner.)
       In caso di conflitti peer React 19, riprovare il comando con `--legacy-peer-deps`.
    4. Pin di next a 15.x: in web-ui/package.json verificare che "next" sia "15.x"; se create-next-app ha installato 16.x, eseguire `npm install next@15 --legacy-peer-deps` in web-ui/.
    5. Aggiungere Vitest: in web-ui/ `npm install -D vitest --legacy-peer-deps`. Creare web-ui/vitest.config.ts:
       ```ts
       import { defineConfig } from 'vitest/config';
       export default defineConfig({ test: { environment: 'node', include: ['__tests__/**/*.test.ts'] } });
       ```
       Aggiungere a web-ui/package.json scripts: "test": "vitest run", "test:watch": "vitest".
    NON aggiungere altre dipendenze. NON importare nulla da ../src.
  </action>
  <acceptance_criteria>
    - File `web-ui/package.json` esiste e `grep '"next"' web-ui/package.json` mostra una versione che inizia con `15.`
    - `grep '"test"' web-ui/package.json` contiene `vitest`
    - `grep '@theme\|tailwindcss' web-ui/app/globals.css` ha almeno un match (Tailwind v4 CSS-first, niente tailwind.config.js)
    - File `web-ui/components.json` esiste (shadcn inizializzato)
    - Directory `web-ui/components/ui/` contiene button.tsx, badge.tsx, input.tsx, select.tsx, table.tsx, card.tsx
    - `cd web-ui && npx tsc --noEmit` esce con codice 0
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>web-ui/ è un progetto Next.js 15 App Router con shadcn e Vitest installati, tsc pulito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract types + helper REPO_ROOT path-safe</name>
  <read_first>
    - step-catalog.json (struttura steps[])
    - scripts/import-scenarios.ts (funzione slugify, righe 57-62; pattern path.resolve validazione righe 177-186)
    - .planning/phases/04-web-ui/04-RESEARCH.md (Pattern 1 e Pattern 2)
  </read_first>
  <files>web-ui/lib/types.ts, web-ui/lib/repo.ts, web-ui/__tests__/lib/repo.test.ts</files>
  <behavior>
    - Test 1: `safeFeaturePath('auth/login.feature')` ritorna un path che inizia con la features dir e termina in `.feature`
    - Test 2: `safeFeaturePath('../../package.json')` lancia o ritorna null (path traversal bloccato)
    - Test 3: `safeFeaturePath('auth/login.txt')` lancia o ritorna null (estensione non .feature)
    - Test 4: `slugify('User Login Flow!')` === 'user-login-flow'
  </behavior>
  <action>
    Creare web-ui/lib/types.ts con i contratti consumati dalle wave successive:
    ```ts
    export interface CatalogStep {
      expression: string;
      parameters: string[];
      app: string;
      area: string;
      domain: string;
      status: 'implemented' | 'wanted' | 'deprecated';
      page?: string;
      requester?: string;
      sourceRef: string;
      documented: boolean;
    }
    export interface Catalog { totalSteps: number; steps: CatalogStep[]; }
    export interface FeatureSummary { file: string; name: string; area: string; scenarioCount: number; }
    ```
    Creare web-ui/lib/repo.ts:
    - `export const REPO_ROOT = path.resolve(process.cwd(), '..');`
    - `export const FEATURES_DIR = path.resolve(REPO_ROOT, 'src', 'features');`
    - `export function slugify(s: string): string` — copia ESATTA della funzione in import-scenarios.ts (lowercase, replace [^a-z0-9]+ con '-', trim dei '-').
    - `export function safeFeaturePath(rel: string): string | null` — risolve `path.resolve(FEATURES_DIR, rel)`; ritorna null se non inizia con `FEATURES_DIR + path.sep` (e non è FEATURES_DIR) oppure se non termina in `.feature`. Questo è il guard path-traversal richiesto da UI-04.
    Mai usare `import` diretto del JSON (dati stale in dev) — solo fs nelle route.
  </action>
  <acceptance_criteria>
    - `grep 'interface CatalogStep' web-ui/lib/types.ts` ha match
    - `grep "status: 'implemented' | 'wanted' | 'deprecated'" web-ui/lib/types.ts` ha match
    - `grep 'REPO_ROOT' web-ui/lib/repo.ts` e `grep 'FEATURES_DIR' web-ui/lib/repo.ts` hanno match
    - `grep 'safeFeaturePath' web-ui/lib/repo.ts` ha match e la funzione controlla `.feature` e il prefisso FEATURES_DIR
    - `cd web-ui && npm test -- repo` passa tutti i 4 test del behavior
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npm test -- repo</automated>
  </verify>
  <done>Types condivisi e helper path-safe esistono, testati. slugify identica a import-scenarios.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Layout con nav, 3 pagine placeholder, 4 API route (catalog/features reali, import/download stub)</name>
  <read_first>
    - web-ui/lib/repo.ts e web-ui/lib/types.ts (creati nel Task 2)
    - .planning/phases/04-web-ui/04-RESEARCH.md (Pattern 1 lettura catalog, sezione "Code Examples")
  </read_first>
  <files>web-ui/app/layout.tsx, web-ui/app/page.tsx, web-ui/app/editor/page.tsx, web-ui/app/features/page.tsx, web-ui/app/api/catalog/route.ts, web-ui/app/api/features/route.ts, web-ui/app/api/import/route.ts, web-ui/app/api/download/route.ts, web-ui/README.md, web-ui/__tests__/api/catalog.test.ts</files>
  <action>
    1. web-ui/app/layout.tsx: root layout con una nav top semplice (Link Next): "Catalog" (/), "Editor" (/editor), "Features" (/features). Mantieni i metadata e il body di create-next-app.
    2. web-ui/app/page.tsx: placeholder `<h1>Step Catalog</h1>` + commento `{/* StepCatalog component — wave 2 */}` (il componente vero arriva nel plan 02).
    3. web-ui/app/editor/page.tsx: placeholder `<h1>Gherkin Editor</h1>` (plan 03).
    4. web-ui/app/features/page.tsx: placeholder `<h1>Feature Catalog</h1>` (plan 04).
    5. web-ui/app/api/catalog/route.ts (REALE, UI-01):
       ```ts
       import { NextResponse } from 'next/server';
       import * as fs from 'fs'; import * as path from 'path';
       import { REPO_ROOT } from '@/lib/repo';
       export async function GET() {
         const p = path.join(REPO_ROOT, 'step-catalog.json');
         const data = JSON.parse(fs.readFileSync(p, 'utf-8'));
         return NextResponse.json(data);
       }
       ```
    6. web-ui/app/api/features/route.ts (REALE, UI-02/UI-06): GET che fa walk ricorsivo di FEATURES_DIR (fs.readdirSync withFileTypes), per ogni .feature legge il contenuto e produce FeatureSummary { file (path relativo a src/features con '/'), name (da `Feature:`), area (da tag `@` prima riga o cartella), scenarioCount (conteggio righe match `/^\s*(Scenario|Scenario Outline):/`) }. Ritorna NextResponse.json(summaries).
    7. web-ui/app/api/import/route.ts: STUB tipizzato — `export async function POST() { return NextResponse.json({ ok: false, error: 'not implemented (plan 03)' }, { status: 501 }); }`
    8. web-ui/app/api/download/route.ts: STUB tipizzato — `export async function GET() { return new Response('not implemented (plan 04)', { status: 501 }); }`
    9. web-ui/README.md: documenta che `npm run dev` va lanciato DA web-ui/ (Pitfall 2 della research: process.cwd() deve essere web-ui/).
    10. web-ui/__tests__/api/catalog.test.ts: importa il GET handler di catalog, lo invoca, e asserisce che la response JSON ha `steps` come array con length > 0 (UI-01).
  </action>
  <acceptance_criteria>
    - `grep 'Editor\|Features\|Catalog' web-ui/app/layout.tsx` mostra i 3 link di nav
    - I file web-ui/app/page.tsx, web-ui/app/editor/page.tsx, web-ui/app/features/page.tsx esistono ed esportano un default component
    - `grep 'export async function GET' web-ui/app/api/catalog/route.ts` e `web-ui/app/api/features/route.ts` hanno match
    - `grep 'REPO_ROOT\|FEATURES_DIR' web-ui/app/api/features/route.ts` ha match
    - `grep '501' web-ui/app/api/import/route.ts` e `web-ui/app/api/download/route.ts` hanno match (stub espliciti)
    - `grep 'web-ui' web-ui/README.md` documenta il cwd
    - `cd web-ui && npm test -- catalog` passa (response ha steps[] non vuoto)
    - `cd web-ui && npx tsc --noEmit` esce 0
  </acceptance_criteria>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit && npm test -- catalog</automated>
  </verify>
  <done>3 pagine navigabili, /api/catalog e /api/features funzionanti, import/download stub 501, README documenta il cwd, test catalog verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API route | input utente (query/body) entra nel processo Node con accesso fs |
| API route → filesystem repo | letture/scritture su file fuori da web-ui/ (REPO_ROOT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | lib/repo.ts safeFeaturePath | mitigate | Guard centralizzato: path.resolve + prefix check su FEATURES_DIR + suffisso .feature. Tutte le route che servono file lo usano (download in plan 04). |
| T-04-02 | Information Disclosure | api/catalog, api/features | accept | Servono solo file del repo (catalog + .feature), nessun dato sensibile; tool localhost-only per la demo. |
| T-04-03 | Tampering | api/import (stub) | mitigate | Stub 501 in questo plan; injection mitigata nel plan 03 (file scritto in tmpdir con nome generato, nessun input utente nel comando shell). |
</threat_model>

<verification>
- `cd web-ui && npx tsc --noEmit` → 0
- `cd web-ui && npm test` → suite verde (repo + catalog)
- `cd web-ui && npm run dev` poi GET http://localhost:3000/api/catalog → JSON con steps[]; le 3 pagine caricano con nav.
</verification>

<success_criteria>
- web-ui/ avviabile, 3 pagine navigabili, /api/catalog e /api/features reali, import/download stub tipizzati, contratti types + REPO_ROOT pronti per le wave successive, Vitest verde.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-ui/04-01-SUMMARY.md`
</output>
