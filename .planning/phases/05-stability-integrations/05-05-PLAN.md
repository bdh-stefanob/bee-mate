---
phase: 05-stability-integrations
plan: "05"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-04"
files_modified:
  - web-ui/src/app/editor/page.tsx
  - web-ui/src/app/page.tsx
  - web-ui/src/app/features/page.tsx
  - web-ui/src/app/settings/page.tsx
  - web-ui/src/components/StepBrowser.tsx
  - web-ui/src/app/layout.tsx
autonomous: true
requirements:
  - RESP-01

must_haves:
  truths:
    - "A 1280px di larghezza, nessuna pagina ha scrollbar orizzontale"
    - "Il layout editor 2/3+1/3 funziona a 1280px senza overflow — la colonna destra non cade sotto la sinistra"
    - "StepBrowser non trabocca dal pannello laterale — la lista ha max-height con overflow-y-auto"
    - "GherkinEditor (CodeMirror) ha height:auto con min-height:400px, non 100vh"
    - "Il nav header rimane orizzontale a 1280px — non collassa"
    - "Il testo nei punti critici usa break-words o truncate appropriati"
  artifacts:
    - path: "web-ui/src/app/layout.tsx"
      provides: "nav header orizzontale fisso, no hamburger"
      contains: "max-w-screen-xl"
    - path: "web-ui/src/app/editor/page.tsx"
      provides: "layout 2 colonne stabile a 1280px+"
      contains: "lg:w-2/3"
  key_links:
    - from: "web-ui/src/app/layout.tsx"
      to: "tutte le pagine"
      via: "max-w-screen-xl mx-auto container"
      pattern: "max-w-screen-xl"
    - from: "web-ui/src/components/StepBrowser.tsx"
      to: "colonna destra editor"
      via: "max-height overflow-y-auto"
      pattern: "overflow-y-auto"
---

<objective>
Audit e fix del layout desktop (1280px–1920px): rimuovere overflow orizzontale, stabilizzare il layout 2 colonne dell'editor, correggere GherkinEditor height, assicurare nav orizzontale fisso.

Purpose: La web-ui deve funzionare da desktop Windows laptop 1080p–1440p senza scrollbar orizzontali né colonne che crollano — prerequisito per la demo manager.
Output: Tutte le pagine corrette per 1280px+, nessun overflow, GherkinEditor altezza corretta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stability-integrations/05-CONTEXT.md
@web-ui/src/app/layout.tsx
@web-ui/src/app/editor/page.tsx
@web-ui/src/components/StepBrowser.tsx

<interfaces>
<!-- Fix specifici dal CONTEXT.md (LOCKED) -->

```
GherkinEditor: CodeMirror deve avere height:auto con min-height:400px (non 100vh)
Editor layout: 2/3 + 1/3 funziona da 1280px senza overflow — entrambe le colonne con min-w-0
Nav header: rimane orizzontale sempre — nessun hamburger
Max-width container: max-w-screen-xl (1280px) applicato ovunque
StepBrowser: lista step con maxHeight controllata, overflow-y-auto
Testo: truncate vs break-words nei punti critici
```

Da web-ui/src/app/editor/page.tsx (attuale):
```tsx
// Layout 2 colonne esistente:
<div className="flex flex-col lg:flex-row gap-4">
  <div className="flex flex-col gap-3 lg:w-2/3 min-w-0">   // LEFT: già ha min-w-0
    ...
  </div>
  <div className="flex flex-col gap-4 lg:w-1/3 min-w-0">   // RIGHT: già ha min-w-0
    ...
  </div>
</div>
```

Da web-ui/src/components/StepBrowser.tsx (attuale):
```tsx
// Lista step:
<ul ref={listRef} role="listbox" aria-label="Step expressions"
    className="overflow-y-auto" style={{ maxHeight: '320px' }}>
// GIA' ha maxHeight 320px — verificare se causa overflow nel contesto della colonna
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit e fix layout — editor, nav, container</name>
  <read_first>
    - web-ui/src/app/layout.tsx (struttura nav, container max-width)
    - web-ui/src/app/editor/page.tsx (layout 2 colonne, GherkinEditor props)
    - web-ui/src/components/GherkinEditor.tsx (CodeMirror altezza — cercare style height o minHeight)
  </read_first>
  <files>
    web-ui/src/app/layout.tsx,
    web-ui/src/app/editor/page.tsx,
    web-ui/src/components/GherkinEditor.tsx
  </files>
  <action>
**Prima di modificare qualsiasi file, leggere i file e fare l'audit.**

**Checklist audit (eseguire per ogni punto):**

1. **Nav header (layout.tsx)**
   - Verificare che il nav non abbia classi `md:flex hidden` o simili che collassano su mobile
   - Se il nav ha un hamburger o `flex-col` su mobile: sostituire con `flex-row flex-wrap gap-2` sempre visibile
   - Verificare che il container abbia `max-w-screen-xl mx-auto px-4` (o equivalente)
   - Se mancante: aggiungere `max-w-screen-xl mx-auto` al wrapper principale del nav

2. **Editor page (editor/page.tsx)**
   - Verificare che il container esterno abbia `max-w-screen-xl mx-auto` (già presente: `max-w-screen-xl mx-auto`)
   - Verificare che il layout flex `flex-col lg:flex-row` sia corretto
   - Verificare `overflow-hidden` o `min-w-0` su entrambe le colonne (già presenti)
   - La `<pre>` del preview ha `whitespace-pre-wrap break-words overflow-auto max-h-96` — OK se già presente
   - Se mancante `overflow-hidden` sul contenitore delle colonne: aggiungere

3. **GherkinEditor.tsx (CodeMirror altezza)**
   - Cercare dove è impostata l'altezza di CodeMirror (nello style del div contenitore o nelle extensions)
   - Se c'è `height: '100vh'` o `minHeight: '100vh'`: sostituire con `minHeight: '400px'`
   - Se c'è una classe `h-screen` o `h-full`: valutare se causare overflow e sostituire con `min-h-[400px]`
   - Il div contenitore di CodeMirror deve avere: `className="w-full min-h-[400px] rounded-md border border-border overflow-hidden"`
   - Nelle EditorView extensions, se c'è `EditorView.theme({ '&': { height: '100vh' } })`: cambiare a `height: 'auto'` + `minHeight: '400px'`

**Fix da applicare dopo l'audit:**

In GherkinEditor.tsx, trovare la configurazione altezza CodeMirror. La struttura tipica è:
```typescript
// Nel JSX:
<div ref={editorRef} className="..." style={{ minHeight: '400px' }} />

// Oppure nelle extensions:
EditorView.theme({
  '&': { minHeight: '400px', height: 'auto' },
  '.cm-content': { minHeight: '400px' },
  '.cm-scroller': { overflow: 'auto' },
})
```

Assicurarsi che:
- Il tema CodeMirror abbia `height: 'auto'` e `minHeight: '400px'` (non `100vh`)
- `.cm-scroller` abbia `overflow: 'auto'` (non `overflow: 'hidden'`)
- Il div wrapper abbia `min-h-[400px]` in Tailwind

In layout.tsx, verificare il wrapper del nav e aggiungere `max-w-screen-xl mx-auto` se mancante.
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>
    - GherkinEditor ha height:auto + minHeight:400px (verificato in .cm-content o style)
    - Nav layout ha max-w-screen-xl e rimane orizzontale
    - Editor page container ha max-w-screen-xl mx-auto
    - Entrambe le colonne hanno min-w-0 (già presenti)
    - tsc --noEmit → exit 0
  </done>
  <acceptance_criteria>
    - `grep -n "minHeight.*400\|min-h-\[400px\]" web-ui/src/components/GherkinEditor.tsx` → almeno 1 occorrenza
    - `grep -rn "100vh" web-ui/src/components/GherkinEditor.tsx` → 0 occorrenze (rimosso)
    - `grep -n "max-w-screen-xl" web-ui/src/app/editor/page.tsx` → almeno 1 occorrenza
    - `grep -n "max-w-screen-xl" web-ui/src/app/layout.tsx` → almeno 1 occorrenza
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Audit e fix pagine Catalog, Features, Settings + StepBrowser</name>
  <read_first>
    - web-ui/src/app/page.tsx (pagina Catalog — verificare container, tabella overflow)
    - web-ui/src/app/features/page.tsx (pagina Features — verificare container, tabella overflow)
    - web-ui/src/app/settings/page.tsx (pagina Settings — verificare max-w-2xl)
    - web-ui/src/components/StepBrowser.tsx (lista step — verificare maxHeight nel contesto)
    - web-ui/src/components/StepCatalog.tsx (tabella catalog — overflow-x-auto su container)
  </read_first>
  <files>
    web-ui/src/app/page.tsx,
    web-ui/src/app/features/page.tsx,
    web-ui/src/app/settings/page.tsx,
    web-ui/src/components/StepBrowser.tsx,
    web-ui/src/components/StepCatalog.tsx
  </files>
  <action>
**Prima di modificare qualsiasi file, leggere tutti i file elencati.**

**Checklist audit per ogni pagina:**

**1. web-ui/src/app/page.tsx (Catalog)**
- Verificare che il container esterno abbia `max-w-screen-xl mx-auto p-4` o equivalente
- Se mancante: aggiungere wrapper `<div className="p-4 lg:p-6 max-w-screen-xl mx-auto">`

**2. web-ui/src/components/StepCatalog.tsx (tabella)**
- La tabella deve essere wrappata in `<div className="overflow-x-auto">` per gestire il caso in cui le colonne eccedano su finestre strette
- Se mancante: aggiungere il wrapper overflow-x-auto
- Verificare le classi `truncate` sulle celle: la colonna Expression deve avere `max-w-xs truncate` per evitare che testi lunghi rompano il layout

**3. web-ui/src/app/features/page.tsx (Features)**
- Stesso pattern: container max-w-screen-xl, tabella con overflow-x-auto
- Verificare che il componente FeaturePreview (se presente) non abbia altezza fissa che causa overflow

**4. web-ui/src/app/settings/page.tsx (Settings)**
- Container è `max-w-2xl mx-auto` (già definito in 05-02 con `max-w-2xl`) — corretto, non cambiare
- Verificare che gli Input non eccedano la colonna

**5. web-ui/src/components/StepBrowser.tsx**
- La lista step ha già `style={{ maxHeight: '320px' }}` — verificare se sia sufficiente nel contesto della colonna destra dell'editor
- La colonna destra è `lg:w-1/3` — il browser potrebbe collassare su finestre piccole
- Assicurarsi che il contenitore del StepBrowser abbia `overflow-hidden` per non uscire dal pannello:
  ```tsx
  <div className="rounded-md border border-border bg-background overflow-hidden">
  ```
- Verificare che la lista abbia anche `w-full` esplicito: `className="overflow-y-auto w-full"`

**Fix specifici da applicare:**

In StepCatalog.tsx, cercare l'elemento `<table>` o il suo contenitore:
```tsx
// PRIMA (probabile):
<div className="...">
  <table className="...">

// DOPO:
<div className="overflow-x-auto">
  <table className="w-full min-w-[600px] ...">
```

In ogni pagina che manca `max-w-screen-xl mx-auto`:
```tsx
// Aggiungere al div più esterno del return:
<div className="p-4 lg:p-6 max-w-screen-xl mx-auto">
```

In StepBrowser, aggiungere `overflow-hidden` al wrapper radice se mancante.

**Testo truncate:**
- Verificare che le espressioni step lunghe in StepBrowser abbiano già `truncate` (presente: `className="flex-1 font-mono truncate"`)
- Verificare che i nomi feature in FeaturePreview abbiano `truncate` se su una riga
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - Tutte le pagine (catalog, features, settings) hanno container max-w-screen-xl o max-w-2xl
    - StepCatalog ha overflow-x-auto sul wrapper della tabella
    - StepBrowser ha overflow-hidden sul wrapper radice
    - tsc --noEmit → exit 0, vitest run → tutti verdi (nessuna regressione)
  </done>
  <acceptance_criteria>
    - `grep -rn "overflow-x-auto" web-ui/src/components/StepCatalog.tsx` → almeno 1 occorrenza
    - `grep -rn "max-w-screen-xl\|max-w-2xl" web-ui/src/app/page.tsx web-ui/src/app/features/page.tsx` → almeno 1 occorrenza per file
    - `grep -n "overflow-hidden" web-ui/src/components/StepBrowser.tsx` → almeno 1 occorrenza
    - `grep -n "100vh" web-ui/src/` ricorsivo → 0 occorrenze in componenti UI (solo eventualmente in test o config)
    - `cd web-ui && npx tsc --noEmit` → exit 0
    - `cd web-ui && npx vitest run` → exit 0, nessuna regressione
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: verifica visiva layout desktop</name>
  <what-built>
    Audit e fix responsiveness desktop: GherkinEditor altezza corretta, nav fisso, container max-w, tabelle con overflow-x-auto, StepBrowser senza traboccamento.
  </what-built>
  <how-to-verify>
    1. Avviare la web-ui: `cd web-ui && npm run dev`
    2. Aprire Chrome/Edge a http://localhost:3000 con finestra a 1280px di larghezza (Developer Tools → Responsive → 1280x768)
    3. Verificare OGNI pagina:
       - http://localhost:3000 (Catalog): nessuna scrollbar orizzontale, tabella scorre dentro il suo container
       - http://localhost:3000/editor (Editor): le due colonne (2/3+1/3) sono affiancate; l'editor CodeMirror occupa almeno 400px di altezza ma non esplode; StepBrowser non trabocca a destra
       - http://localhost:3000/features (Features): stessa verifica catalog
       - http://localhost:3000/settings (Settings): form centrato, nessun campo eccede la colonna
    4. Allargare la finestra a 1920px: le pagine devono rimanere centrate con max-w contenuto
    5. Verificare che il nav header abbia sempre Catalog, Editor, Features, Settings affiancati (mai collassati)
  </how-to-verify>
  <resume-signal>
    Digita "approved" se tutto funziona, oppure descrivi i problemi riscontrati (es. "colonna destra cade sotto a 1280px", "editor esce dal viewport") e il task viene fixato.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS/layout | Solo modifiche presentazionali — nessuna nuova superficie di sicurezza |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | overflow testo | accept | I testi troncati con `truncate` mostrano solo il visibile — nessun dato sensibile nel DOM nascosto |
</threat_model>

<verification>
Verifica complessiva:

```bash
cd web-ui && npx tsc --noEmit
cd web-ui && npx vitest run
grep -rn "100vh" web-ui/src/components/GherkinEditor.tsx
grep -rn "overflow-x-auto" web-ui/src/components/StepCatalog.tsx
grep -rn "max-w-screen-xl" web-ui/src/app/layout.tsx
```

Atteso: tsc exit 0, vitest tutti verdi, nessun 100vh nell'editor, overflow-x-auto in StepCatalog, max-w nel layout.
</verification>

<success_criteria>
- GherkinEditor: height:auto + minHeight:400px (no 100vh)
- Nav header: max-w-screen-xl, sempre orizzontale
- Tutte le pagine: container max-w-screen-xl o max-w-2xl
- StepCatalog tabella: overflow-x-auto
- StepBrowser: overflow-hidden, lista con maxHeight 320px
- Nessuna scrollbar orizzontale a 1280px (verificato da checkpoint umano)
- tsc --noEmit e vitest run passano
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/05-stability-integrations/05-05-SUMMARY.md`
</output>
