---
phase: 05-stability-integrations
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - web-ui/src/app/api/jira/sync/route.ts
  - web-ui/src/app/settings/page.tsx
  - web-ui/src/lib/i18n.ts
autonomous: true
requirements:
  - JIRA-01
  - JIRA-02

must_haves:
  truths:
    - "POST /api/jira/sync legge tutti i .feature in src/features/**/*.feature, estrae gli scenari con @ticket:[A-Z]+-\\d+ e posta un commento ADF su ogni issue Jira"
    - "La route ritorna { synced: N, skipped: M, errors: [] } con conteggio preciso"
    - "Il bottone 'Sincronizza con Jira' è visibile nella pagina Settings solo se jiraBaseUrl e jiraToken sono configurati"
    - "Il token Jira non appare mai nel body client→server (solo nell'header x-jira-token)"
    - "Il commento Jira usa il formato ADF minimo con codeBlock language='gherkin'"
  artifacts:
    - path: "web-ui/src/app/api/jira/sync/route.ts"
      provides: "POST /api/jira/sync — legge .feature, estrae @ticket, posta commenti Jira ADF"
      exports: ["POST"]
      contains: "rest/api/3/issue"
    - path: "web-ui/src/app/settings/page.tsx"
      provides: "sezione Jira Sync con bottone + feedback contatore"
      contains: "jiraSync"
  key_links:
    - from: "web-ui/src/app/settings/page.tsx"
      to: "web-ui/src/app/api/jira/sync/route.ts"
      via: "fetch('/api/jira/sync', { headers: { 'x-jira-token': ..., 'x-jira-url': ... } })"
      pattern: "x-jira-token"
    - from: "web-ui/src/app/api/jira/sync/route.ts"
      to: "{jiraBaseUrl}/rest/api/3/issue/{key}/comment"
      via: "fetch POST con Authorization: Bearer"
      pattern: "rest/api/3/issue"
---

<objective>
Implementare l'integrazione Jira: API route POST /api/jira/sync che legge i .feature con @ticket:BOOT-XXX e posta lo scenario come commento ADF su ogni issue Jira, più UI di trigger nella pagina Settings.

Purpose: Permettere la sincronizzazione degli scenari BDD con le Jira issue corrispondenti — mostrare alla manager l'integrazione BDD↔Jira durante la demo M1.
Output: api/jira/sync/route.ts, settings/page.tsx aggiornato con sezione Jira Sync, i18n aggiornato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stability-integrations/05-CONTEXT.md
@web-ui/src/app/api/catalog/route.ts
@web-ui/src/lib/repo.ts
@web-ui/src/lib/i18n.ts

<interfaces>
<!-- Contratti LOCKED dal CONTEXT.md -->

```typescript
// POST /api/jira/sync
// Headers: x-jira-url (base URL Jira), x-jira-token (Bearer token)
// Body: {} (vuoto)
// Response: { synced: number; skipped: number; errors: string[] }

// Pattern @ticket nel .feature:
// @ticket:BOOT-123   oppure @ticket:XXXX-NNN
// regex: /@ticket:([A-Z]+-\d+)/g

// Jira Cloud REST API v3 (ADF format):
// POST {jiraBaseUrl}/rest/api/3/issue/{key}/comment
// Headers: Authorization: Bearer {jiraToken}
//          Content-Type: application/json
//          Accept: application/json
// Body (ADF minimo):
// {
//   "body": {
//     "type": "doc",
//     "version": 1,
//     "content": [{
//       "type": "codeBlock",
//       "attrs": { "language": "gherkin" },
//       "content": [{ "type": "text", "text": "<scenario gherkin>" }]
//     }]
//   }
// }
```

Da ROADMAP.md sezione 5.8:
- Leggere `.feature` in `src/features/` (path: REPO_ROOT + '/src/features')
- Estrarre solo scenari con `@ticket:[A-Z]+-\d+`
- `POST /rest/api/3/issue/{key}/comment` — solo commenti, non modificare la issue principale
- `fetch` nativo Node 18+ — nessuna libreria extra

Da web-ui/src/lib/repo.ts (plan 04-01):
```typescript
export const REPO_ROOT: string;  // path.resolve(process.cwd(), '..')
export const FEATURES_DIR: string;  // path.join(REPO_ROOT, 'src', 'features')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API route POST /api/jira/sync</name>
  <read_first>
    - web-ui/src/lib/repo.ts (REPO_ROOT, FEATURES_DIR per leggere i .feature)
    - web-ui/src/app/api/catalog/route.ts (pattern NextResponse, error handling)
    - .planning/phases/05-stability-integrations/05-CONTEXT.md (spec ADF format, sezione Jira Sync)
    - ROADMAP.md sezione 5.8 (tag convention @ticket, direzione push)
  </read_first>
  <files>
    web-ui/src/app/api/jira/sync/route.ts
  </files>
  <action>
Creare `web-ui/src/app/api/jira/sync/route.ts` (creare la directory `api/jira/sync/`):

```typescript
import { NextResponse } from 'next/server';
import * as fs from 'fs';
import * as path from 'path';
import { FEATURES_DIR } from '@/lib/repo';

// Estrae il testo di ogni Scenario (dal tag @ticket fino alla riga vuota successiva
// o al prossimo Scenario/Feature)
function extractScenariosWithTicket(
  featureContent: string
): Array<{ ticketKey: string; scenarioText: string }> {
  const results: Array<{ ticketKey: string; scenarioText: string }> = [];
  const lines = featureContent.split('\n');
  let i = 0;

  while (i < lines.length) {
    const ticketMatch = lines[i].match(/@ticket:([A-Z]+-\d+)/);
    if (ticketMatch) {
      const ticketKey = ticketMatch[1];
      // Raccogliere le righe dello scenario fino al prossimo @ticket o Feature o fine file
      const scenarioLines: string[] = [lines[i]];
      i++;
      while (i < lines.length) {
        const line = lines[i];
        // Stop al prossimo tag @ticket o alla keyword Feature: al livello radice
        if (/@ticket:/.test(line) || /^Feature:/.test(line.trim())) break;
        scenarioLines.push(line);
        i++;
      }
      results.push({
        ticketKey,
        scenarioText: scenarioLines.join('\n').trim(),
      });
    } else {
      i++;
    }
  }
  return results;
}

// Raccogliere tutti i .feature in modo ricorsivo
function walkFeatureFiles(dir: string): string[] {
  const results: string[] = [];
  if (!fs.existsSync(dir)) return results;
  const entries = fs.readdirSync(dir, { withFileTypes: true });
  for (const entry of entries) {
    const fullPath = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      results.push(...walkFeatureFiles(fullPath));
    } else if (entry.isFile() && entry.name.endsWith('.feature')) {
      results.push(fullPath);
    }
  }
  return results;
}

function buildAdfComment(scenarioText: string): object {
  return {
    body: {
      type: 'doc',
      version: 1,
      content: [
        {
          type: 'codeBlock',
          attrs: { language: 'gherkin' },
          content: [{ type: 'text', text: scenarioText }],
        },
      ],
    },
  };
}

export async function POST(request: Request) {
  // 1. Leggere token dagli header
  const jiraUrl   = request.headers.get('x-jira-url');
  const jiraToken = request.headers.get('x-jira-token');

  if (!jiraUrl || !jiraToken) {
    return NextResponse.json(
      { ok: false, error: 'Missing headers: x-jira-url, x-jira-token' },
      { status: 400 }
    );
  }

  // Normalizzare l'URL base (rimuovere trailing slash)
  const baseUrl = jiraUrl.replace(/\/$/, '');

  // 2. Raccogliere tutti i .feature
  const featureFiles = walkFeatureFiles(FEATURES_DIR);

  let synced = 0;
  let skipped = 0;
  const errors: string[] = [];

  // 3. Per ogni file, estrarre gli scenari con @ticket
  for (const filePath of featureFiles) {
    let content: string;
    try {
      content = fs.readFileSync(filePath, 'utf-8');
    } catch {
      errors.push(`Cannot read ${path.basename(filePath)}`);
      continue;
    }

    const scenarios = extractScenariosWithTicket(content);
    if (scenarios.length === 0) {
      skipped++;
      continue;
    }

    // 4. Per ogni scenario, postare il commento su Jira
    for (const { ticketKey, scenarioText } of scenarios) {
      const commentUrl = `${baseUrl}/rest/api/3/issue/${ticketKey}/comment`;
      try {
        const res = await fetch(commentUrl, {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${jiraToken}`,
            'Content-Type': 'application/json',
            'Accept': 'application/json',
          },
          body: JSON.stringify(buildAdfComment(scenarioText)),
        });

        if (!res.ok) {
          const errData = await res.json().catch(() => ({})) as { message?: string; errorMessages?: string[] };
          const msg = errData.message ?? errData.errorMessages?.[0] ?? `HTTP ${res.status}`;
          errors.push(`${ticketKey}: ${msg}`);
        } else {
          synced++;
        }
      } catch (err: unknown) {
        const msg = err instanceof Error ? err.message : 'Network error';
        errors.push(`${ticketKey}: ${msg}`);
      }
    }
  }

  return NextResponse.json({ synced, skipped, errors });
}
```

Note:
- `FEATURES_DIR` da `@/lib/repo` (stabilito in plan 04-01)
- Token letto dall'header `x-jira-token`, mai dal body
- `baseUrl.replace(/\/$/, '')` normalizza l'URL Jira dell'utente
- `buildAdfComment` usa il formato ADF minimo specificato in CONTEXT.md
- Errori per singola issue non bloccano le altre (loop continua)
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>
    - web-ui/src/app/api/jira/sync/route.ts esiste ed esporta `POST`
    - Usa FEATURES_DIR da @/lib/repo
    - Legge x-jira-token dall'header
    - Formato ADF codeBlock con language='gherkin'
    - Ritorna { synced, skipped, errors }
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </done>
  <acceptance_criteria>
    - `grep -n "x-jira-token" web-ui/src/app/api/jira/sync/route.ts` → header get, non body
    - `grep -n "FEATURES_DIR" web-ui/src/app/api/jira/sync/route.ts` → 1 occorrenza (import + uso)
    - `grep -n "rest/api/3/issue" web-ui/src/app/api/jira/sync/route.ts` → 1 occorrenza (URL commento)
    - `grep -n "codeBlock" web-ui/src/app/api/jira/sync/route.ts` → 1 occorrenza (ADF format)
    - `grep -n "language.*gherkin" web-ui/src/app/api/jira/sync/route.ts` → 1 occorrenza
    - `grep -n "synced" web-ui/src/app/api/jira/sync/route.ts` → almeno 3 occorrenze (init + increment + return)
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sezione Jira Sync nella pagina Settings</name>
  <read_first>
    - web-ui/src/app/settings/page.tsx (appena creato in 05-02 — aggiungere sezione)
    - web-ui/src/hooks/useSettings.ts (useSettings con jiraBaseUrl, jiraToken)
    - web-ui/src/lib/i18n.ts (aggiungere chiavi jiraSync.*)
  </read_first>
  <files>
    web-ui/src/app/settings/page.tsx,
    web-ui/src/lib/i18n.ts
  </files>
  <action>
**1. Aggiungere chiavi i18n in web-ui/src/lib/i18n.ts**

Aggiungere in `en.settings` e `it.settings`:
```typescript
// en.settings:
jiraSyncTitle: 'Jira Sync',
jiraSyncButton: 'Sync scenarios to Jira',
jiraSyncLoading: 'Syncing…',
jiraSyncResult: '{synced} scenarios synced, {errors} errors',
jiraSyncNotConfigured: 'Configure Jira URL and token above to enable sync',

// it.settings:
jiraSyncTitle: 'Sincronizzazione Jira',
jiraSyncButton: 'Sincronizza scenari su Jira',
jiraSyncLoading: 'Sincronizzazione in corso…',
jiraSyncResult: '{synced} scenari sincronizzati, {errors} errori',
jiraSyncNotConfigured: 'Configura URL Jira e token sopra per abilitare la sync',
```

IMPORTANTE: usare SOLO la forma a stringa parametrica sopra (compatibile con `as const` in i18n.ts).
NON usare funzioni lambda come valori i18n — TypeScript con `as const` le rifiuta.
Nel componente, sostituire i segnaposto manualmente:
```typescript
const resultText = t.settings.jiraSyncResult
  .replace('{synced}', String(data.synced))
  .replace('{errors}', String(data.errors.length));
```

**2. Aggiornare web-ui/src/app/settings/page.tsx**

Aggiungere sotto la sezione Jira esistente una nuova sezione "Jira Sync".
Il bottone è visibile SOLO se `settings.jiraBaseUrl && settings.jiraToken` non sono vuoti.

```typescript
// Aggiungere stato per la sync
const [isSyncing, setIsSyncing] = useState(false);
const [syncResult, setSyncResult] = useState<{ synced: number; errors: string[] } | null>(null);

const handleJiraSync = useCallback(async () => {
  if (!settings.jiraBaseUrl || !settings.jiraToken) return;
  setIsSyncing(true);
  setSyncResult(null);
  try {
    const res = await fetch('/api/jira/sync', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-jira-url': settings.jiraBaseUrl,
        'x-jira-token': settings.jiraToken,
      },
      body: JSON.stringify({}),
    });
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const data = await res.json() as { synced: number; skipped: number; errors: string[] };
    setSyncResult({ synced: data.synced, errors: data.errors });
    if (data.errors.length === 0) {
      toast.success(`${data.synced} scenari sincronizzati su Jira`);
    } else {
      toast.error(`Sync completata con ${data.errors.length} errori`);
    }
  } catch (err: unknown) {
    const msg = err instanceof Error ? err.message : 'Unknown error';
    toast.error(`Jira sync fallita: ${msg}`);
  } finally {
    setIsSyncing(false);
  }
}, [settings.jiraBaseUrl, settings.jiraToken]);
```

Aggiungere nel JSX, dopo la sezione Jira credentials, una nuova sezione:
```tsx
{/* Jira Sync Section */}
<section className="space-y-4">
  <h2 className="text-base font-semibold text-foreground border-b border-border pb-2">
    {t.settings.jiraSyncTitle}
  </h2>
  {settings.jiraBaseUrl && settings.jiraToken ? (
    <div className="space-y-3">
      <button
        onClick={handleJiraSync}
        disabled={isSyncing}
        className="px-4 py-2 rounded-md bg-blue-600 text-white text-sm hover:bg-blue-700 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
      >
        {isSyncing ? t.settings.jiraSyncLoading : t.settings.jiraSyncButton}
      </button>
      {syncResult && (
        <div className="text-sm text-muted-foreground">
          <p>{syncResult.synced} scenari sincronizzati</p>
          {syncResult.errors.length > 0 && (
            <ul className="mt-1 space-y-0.5">
              {syncResult.errors.map((e, i) => (
                <li key={i} className="text-destructive text-xs font-mono">{e}</li>
              ))}
            </ul>
          )}
        </div>
      )}
    </div>
  ) : (
    <p className="text-sm text-muted-foreground">{t.settings.jiraSyncNotConfigured}</p>
  )}
</section>
```

Aggiungere `import { toast } from 'sonner';` se non già presente (aggiunto in 05-01 se disponibile, altrimenti aggiungerlo ora).
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - settings/page.tsx ha sezione "Jira Sync" con bottone condizionale
    - Il bottone è visibile solo se jiraBaseUrl e jiraToken sono configurati
    - Il token è inviato nell'header x-jira-token, non nel body
    - Toast successo/errore dopo la sync
    - i18n aggiornato con chiavi jiraSync* in EN e IT
    - tsc --noEmit → exit 0, vitest run → tutti verdi
  </done>
  <acceptance_criteria>
    - `grep -n "x-jira-token" web-ui/src/app/settings/page.tsx` → 1 occorrenza nell'header fetch
    - `grep -n "jiraToken" web-ui/src/app/settings/page.tsx` → almeno 2 occorrenze (condizione + header)
    - `grep -n "handleJiraSync" web-ui/src/app/settings/page.tsx` → almeno 2 occorrenze (definizione + bottone)
    - `grep -n "jiraSyncTitle" web-ui/src/lib/i18n.ts` → 2 occorrenze (en e it)
    - Il body della fetch contiene solo `{}`: `grep -n "body.*JSON.stringify" web-ui/src/app/settings/page.tsx` → `JSON.stringify({})` o `JSON.stringify({})` senza token
    - `cd web-ui && npx tsc --noEmit` → exit 0
    - `cd web-ui && npx vitest run` → exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/jira/sync | Header x-jira-token, body {} |
| /api/jira/sync → {jiraBaseUrl}/rest/api/3 | Server-to-server con Bearer token |
| filesystem → route | Lettura .feature da FEATURES_DIR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | jiraToken negli header | mitigate | Token letto da `request.headers.get('x-jira-token')` — non esposto in log, non nel body |
| T-05-04-02 | Tampering | jiraBaseUrl come URL server | mitigate | La route non valida il dominio Jira (uso localhost accettabile); per produzione aggiungere allowlist domini |
| T-05-04-03 | Information Disclosure | errori Jira API → client | mitigate | Gli errori mostrano solo il ticketKey e il messaggio Jira — non il token |
| T-05-04-04 | Elevation of Privilege | commenti Jira | accept | La route posta solo commenti (non modifica la issue principale) — scope limitato per design |
| T-05-04-05 | Information Disclosure | FEATURES_DIR traversal | accept | `walkFeatureFiles` legge solo file con estensione `.feature` da FEATURES_DIR — path fisso, non da input utente |
</threat_model>

<verification>
Verifica complessiva:

```bash
cd web-ui && npx tsc --noEmit
cd web-ui && npx vitest run
grep -n "x-jira-token" web-ui/src/app/api/jira/sync/route.ts
grep -n "FEATURES_DIR" web-ui/src/app/api/jira/sync/route.ts
grep -n "x-jira-token" web-ui/src/app/settings/page.tsx
grep -n "codeBlock" web-ui/src/app/api/jira/sync/route.ts
```

Atteso: tsc exit 0, vitest tutti verdi, token da header nella route, FEATURES_DIR usato, formato ADF codeBlock presente.
</verification>

<success_criteria>
- api/jira/sync/route.ts legge i .feature da FEATURES_DIR, estrae @ticket, posta ADF su Jira
- Token letto da header x-jira-token (mai dal body)
- Formato ADF: codeBlock con language='gherkin'
- Risposta { synced, skipped, errors }
- settings/page.tsx ha sezione Jira Sync con bottone condizionale + feedback risultato
- i18n aggiornato EN + IT
- tsc --noEmit e vitest run passano
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/05-stability-integrations/05-04-SUMMARY.md`
</output>
