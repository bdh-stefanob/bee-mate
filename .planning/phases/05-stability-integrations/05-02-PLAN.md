---
phase: 05-stability-integrations
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/src/hooks/useSettings.ts
  - web-ui/src/app/settings/page.tsx
  - web-ui/src/app/layout.tsx
  - web-ui/src/lib/i18n.ts
autonomous: true
requirements:
  - SET-01

must_haves:
  truths:
    - "La pagina /settings esiste ed è raggiungibile dal nav header"
    - "I campi GitHub (token, owner, repo, branch) e Jira (baseUrl, token) sono salvati in localStorage alla chiave 'bdd-scaffold-settings'"
    - "Al blur di un campo, il valore viene salvato automaticamente senza premere submit; compare un badge 'Salvato' inline"
    - "Il hook useSettings() restituisce { settings: AppSettings, update: (patch) => void } con default stringhe vuote"
    - "I token non compaiono mai in console.log né vengono inviati al server in alcuna richiesta di questa pagina"
  artifacts:
    - path: "web-ui/src/hooks/useSettings.ts"
      provides: "hook useSettings con AppSettings interface e STORAGE_KEY"
      exports: ["useSettings", "AppSettings"]
      contains: "bdd-scaffold-settings"
    - path: "web-ui/src/app/settings/page.tsx"
      provides: "pagina /settings con 6 campi input + autosave on blur"
      contains: "useSettings"
  key_links:
    - from: "web-ui/src/app/settings/page.tsx"
      to: "web-ui/src/hooks/useSettings.ts"
      via: "import useSettings"
      pattern: "useSettings"
    - from: "web-ui/src/app/layout.tsx"
      to: "web-ui/src/app/settings/page.tsx"
      via: "nav link href='/settings'"
      pattern: "/settings"
---

<objective>
Creare la pagina Settings (/settings) con il hook useSettings per persistenza dei token GitHub e Jira in localStorage, e aggiungere il link al nav header.

Purpose: Fornire storage sicuro dei token per le integrazioni GitHub e Jira (Wave 2). I token non entrano mai nel codice sorgente né nei body delle richieste HTTP.
Output: useSettings.ts hook, settings/page.tsx, nav header aggiornato, i18n aggiornato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stability-integrations/05-CONTEXT.md
@web-ui/src/app/layout.tsx
@web-ui/src/lib/i18n.ts

<interfaces>
<!-- Contratto LOCKED dal CONTEXT.md — implementare ESATTAMENTE come specificato -->

```typescript
// web-ui/src/hooks/useSettings.ts
export interface AppSettings {
  githubToken: string;
  githubOwner: string;
  githubRepo: string;
  githubBranch: string;   // default: 'main'
  jiraBaseUrl: string;
  jiraToken: string;
}

const STORAGE_KEY = 'bdd-scaffold-settings';

export function useSettings(): {
  settings: AppSettings;
  update: (patch: Partial<AppSettings>) => void;
}
// Legge da localStorage al mount. Default: tutte le stringhe vuote
// (githubBranch default 'main'). Scrive in localStorage a ogni update().
// SSR-safe: lettura localStorage solo nel browser (typeof window !== 'undefined').
```

<!-- Layout esistente da web-ui/src/app/layout.tsx — aggiungere link Settings accanto al language toggle -->
<!-- Verificare la struttura del nav prima di modificare -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook useSettings con localStorage</name>
  <read_first>
    - web-ui/src/lib/i18n.ts (aggiungere chiavi settings.*)
    - web-ui/src/app/layout.tsx (struttura header/nav esistente per aggiungere link Settings)
  </read_first>
  <files>
    web-ui/src/hooks/useSettings.ts,
    web-ui/src/lib/i18n.ts
  </files>
  <action>
**1. Creare web-ui/src/hooks/useSettings.ts**

Implementare ESATTAMENTE l'interfaccia definita in CONTEXT.md (per D-SET-01):

```typescript
'use client';
import { useState, useEffect, useCallback } from 'react';

export interface AppSettings {
  githubToken: string;
  githubOwner: string;
  githubRepo: string;
  githubBranch: string;
  jiraBaseUrl: string;
  jiraToken: string;
}

const STORAGE_KEY = 'bdd-scaffold-settings';

const DEFAULTS: AppSettings = {
  githubToken: '',
  githubOwner: '',
  githubRepo: '',
  githubBranch: 'main',
  jiraBaseUrl: '',
  jiraToken: '',
};

function readStorage(): AppSettings {
  if (typeof window === 'undefined') return { ...DEFAULTS };
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return { ...DEFAULTS };
    return { ...DEFAULTS, ...JSON.parse(raw) } as AppSettings;
  } catch {
    return { ...DEFAULTS };
  }
}

export function useSettings(): {
  settings: AppSettings;
  update: (patch: Partial<AppSettings>) => void;
} {
  const [settings, setSettings] = useState<AppSettings>(DEFAULTS);

  useEffect(() => {
    setSettings(readStorage());
  }, []);

  const update = useCallback((patch: Partial<AppSettings>) => {
    setSettings(prev => {
      const next = { ...prev, ...patch };
      try {
        if (typeof window !== 'undefined') {
          localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
        }
      } catch { /* localStorage non disponibile — silenzioso */ }
      return next;
    });
  }, []);

  return { settings, update };
}
```

Note di sicurezza:
- I token NON sono mai loggati (nessun console.log dei valori)
- Lettura localStorage con guard SSR-safe (`typeof window !== 'undefined'`)
- JSON.parse in try/catch per gestire dati corrotti

**2. Aggiungere chiavi i18n in web-ui/src/lib/i18n.ts**

Aggiungere oggetto `settings` in ENTRAMBI en e it:

```typescript
// in translations.en:
settings: {
  title: 'Settings',
  nav: 'Settings',
  github: 'GitHub Integration',
  githubToken: 'Personal Access Token (PAT)',
  githubTokenHint: 'Token with repo write scope. Never committed.',
  githubOwner: 'Repository Owner',
  githubRepo: 'Repository Name',
  githubBranch: 'Default Branch',
  jira: 'Jira Integration',
  jiraBaseUrl: 'Jira Base URL',
  jiraBaseUrlHint: 'e.g. https://yourcompany.atlassian.net',
  jiraToken: 'Jira API Token',
  jiraTokenHint: 'API token from Atlassian account settings. Never committed.',
  saved: 'Saved',
},

// in translations.it:
settings: {
  title: 'Impostazioni',
  nav: 'Impostazioni',
  github: 'Integrazione GitHub',
  githubToken: 'Personal Access Token (PAT)',
  githubTokenHint: 'Token con scope repo write. Mai committato.',
  githubOwner: 'Owner del Repository',
  githubRepo: 'Nome del Repository',
  githubBranch: 'Branch di Default',
  jira: 'Integrazione Jira',
  jiraBaseUrl: 'URL base Jira',
  jiraBaseUrlHint: 'es. https://tuaazienda.atlassian.net',
  jiraToken: 'API Token Jira',
  jiraTokenHint: 'Token API dalle impostazioni account Atlassian. Mai committato.',
  saved: 'Salvato',
},
```
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>
    - web-ui/src/hooks/useSettings.ts esiste ed esporta `useSettings` e `AppSettings`
    - STORAGE_KEY è 'bdd-scaffold-settings'
    - Default githubBranch è 'main', tutti gli altri default sono stringa vuota
    - Nessun console.log dei valori dei token nel codice
    - i18n.ts ha oggetto `settings` in en e it con tutte le chiavi
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </done>
  <acceptance_criteria>
    - `grep -n "STORAGE_KEY" web-ui/src/hooks/useSettings.ts` → valore `'bdd-scaffold-settings'`
    - `grep -n "githubBranch: 'main'" web-ui/src/hooks/useSettings.ts` → 1 occorrenza nei DEFAULTS
    - `grep -n "typeof window" web-ui/src/hooks/useSettings.ts` → almeno 1 occorrenza (guard SSR)
    - `grep -n "console.log" web-ui/src/hooks/useSettings.ts` → 0 occorrenze
    - `grep -n "settings:" web-ui/src/lib/i18n.ts` → 2 occorrenze (en e it)
    - `grep -n "'bdd-scaffold-settings'" web-ui/src/hooks/useSettings.ts` → 1 occorrenza
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pagina Settings + voce nav</name>
  <read_first>
    - web-ui/src/app/layout.tsx (struttura nav header per aggiungere link Settings con icona)
    - web-ui/src/hooks/useSettings.ts (appena creato — usare l'interfaccia)
    - web-ui/src/lib/i18n.ts (appena aggiornato — usare t.settings.*)
  </read_first>
  <files>
    web-ui/src/app/settings/page.tsx,
    web-ui/src/app/layout.tsx
  </files>
  <action>
**1. Creare web-ui/src/app/settings/page.tsx**

Client Component con form di 6 campi. Autosave on blur. Badge "Salvato" inline per 2 secondi dopo ogni save:

```typescript
'use client';
import { useState, useCallback } from 'react';
import { Settings as SettingsIcon } from 'lucide-react';
import { Input } from '@/components/ui/input';
import { useSettings } from '@/hooks/useSettings';
import { useLanguage } from '@/providers/Providers';

export default function SettingsPage() {
  const { t } = useLanguage();
  const { settings, update } = useSettings();
  const [savedKey, setSavedKey] = useState<string | null>(null);

  const handleBlur = useCallback((key: keyof typeof settings, value: string) => {
    update({ [key]: value });
    setSavedKey(key);
    setTimeout(() => setSavedKey(k => k === key ? null : k), 2000);
  }, [settings, update]);

  const field = (
    key: keyof typeof settings,
    label: string,
    hint: string,
    type: 'text' | 'password' = 'text'
  ) => (
    <div className="space-y-1.5">
      <label className="text-sm font-medium text-foreground flex items-center gap-2">
        {label}
        {savedKey === key && (
          <span className="text-xs text-emerald-600 dark:text-emerald-400 font-normal">
            {t.settings.saved}
          </span>
        )}
      </label>
      {hint && <p className="text-xs text-muted-foreground">{hint}</p>}
      <Input
        type={type}
        defaultValue={settings[key]}
        onBlur={e => handleBlur(key, e.target.value)}
        autoComplete="off"
        className="font-mono text-sm"
      />
    </div>
  );

  return (
    <div className="p-4 lg:p-6 max-w-2xl mx-auto space-y-8">
      <div className="flex items-center gap-3">
        <SettingsIcon className="w-6 h-6 text-muted-foreground" />
        <h1 className="text-2xl font-bold text-foreground">{t.settings.title}</h1>
      </div>

      {/* GitHub Section */}
      <section className="space-y-4">
        <h2 className="text-base font-semibold text-foreground border-b border-border pb-2">
          {t.settings.github}
        </h2>
        {field('githubToken', t.settings.githubToken, t.settings.githubTokenHint, 'password')}
        {field('githubOwner', t.settings.githubOwner, '')}
        {field('githubRepo', t.settings.githubRepo, '')}
        {field('githubBranch', t.settings.githubBranch, '')}
      </section>

      {/* Jira Section */}
      <section className="space-y-4">
        <h2 className="text-base font-semibold text-foreground border-b border-border pb-2">
          {t.settings.jira}
        </h2>
        {field('jiraBaseUrl', t.settings.jiraBaseUrl, t.settings.jiraBaseUrlHint)}
        {field('jiraToken', t.settings.jiraToken, t.settings.jiraTokenHint, 'password')}
      </section>
    </div>
  );
}
```

**2. Aggiornare web-ui/src/app/layout.tsx**

Leggere la struttura nav esistente. Aggiungere link Settings accanto al language toggle.
Il link deve usare l'icona `Settings` da lucide-react e il testo `t.settings.nav` (ma il layout.tsx è Server Component — verificare se usa useLanguage o ha i18n hardcoded).

layout.tsx è un Server Component (RSC) in Next.js 15 App Router — NON supporta `useContext` né `useLanguage`.
NON tentare di usare `useLanguage` in layout.tsx: causerebbe un build error.

Aggiungere un link statico con testo hardcoded "Settings":
```tsx
import Link from 'next/link';
import { Settings as SettingsIcon } from 'lucide-react';
// nel nav, accanto agli altri link:
<Link href="/settings" className="flex items-center gap-1 text-sm text-foreground hover:text-primary transition-colors">
  <SettingsIcon className="w-4 h-4" />
  Settings
</Link>
```

Se la i18n del testo "Settings" è necessaria, estrarre un piccolo Client Component:
```tsx
// web-ui/src/components/NavSettingsLink.tsx
'use client';
import Link from 'next/link';
import { Settings as SettingsIcon } from 'lucide-react';
import { useLanguage } from '@/providers/Providers';
export function NavSettingsLink() {
  const { t } = useLanguage();
  return (
    <Link href="/settings" className="flex items-center gap-1 text-sm text-foreground hover:text-primary transition-colors">
      <SettingsIcon className="w-4 h-4" />
      {t.settings.nav}
    </Link>
  );
}
```
In questo caso importare `NavSettingsLink` in layout.tsx (un RSC può importare Client Components senza problemi).

Il link deve essere VISIVAMENTE accanto agli altri link di navigazione, mai collassato su mobile.
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - web-ui/src/app/settings/page.tsx esiste come Client Component
    - La pagina ha sezioni GitHub (4 campi) e Jira (2 campi)
    - I campi password hanno `type="password"` (githubToken, jiraToken)
    - Autosave on blur via handleBlur → update()
    - Badge "Salvato" visibile per 2 secondi dopo ogni blur
    - layout.tsx ha link `/settings` nel nav
    - tsc --noEmit → exit 0, vitest run → tutti verdi
  </done>
  <acceptance_criteria>
    - `grep -n "type.*password" web-ui/src/app/settings/page.tsx` → almeno 2 occorrenze (githubToken, jiraToken)
    - `grep -n "onBlur" web-ui/src/app/settings/page.tsx` → almeno 1 occorrenza
    - `grep -n "useSettings" web-ui/src/app/settings/page.tsx` → 1 occorrenza (import + uso)
    - `grep -n "/settings" web-ui/src/app/layout.tsx` → almeno 1 occorrenza (link nav)
    - `grep -n "console.log" web-ui/src/app/settings/page.tsx` → 0 occorrenze
    - `cd web-ui && npx tsc --noEmit` → exit 0
    - `cd web-ui && npx vitest run` → exit 0, nessuna regressione
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → hook useSettings | Dati letti dal browser storage — potenzialmente manipolati dall'utente o da XSS |
| Settings page → API routes (Wave 2) | Token da settings usati come header x-github-token / x-jira-token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | token in localStorage | accept | localStorage non è crittografato; per uso localhost accettabile. Nota documentata. I token non finiscono mai nel codice sorgente né nei commit. |
| T-05-02-02 | Tampering | JSON.parse localStorage | mitigate | try/catch in readStorage() con fallback a DEFAULTS — dati corrotti non causano crash |
| T-05-02-03 | Information Disclosure | token fields nel DOM | mitigate | `type="password"` sui campi token nasconde il valore nel DOM inspector di default |
| T-05-02-04 | Information Disclosure | console.log token | mitigate | Nessun console.log dei valori settings — verificato in acceptance_criteria |
</threat_model>

<verification>
Verifica complessiva:

```bash
cd web-ui && npx tsc --noEmit
cd web-ui && npx vitest run
grep -n "STORAGE_KEY" web-ui/src/hooks/useSettings.ts
grep -n "/settings" web-ui/src/app/layout.tsx
grep -rn "console.log" web-ui/src/hooks/useSettings.ts web-ui/src/app/settings/page.tsx
```

Atteso: tsc exit 0, vitest tutti verdi, STORAGE_KEY presente, link settings nel nav, zero console.log.
</verification>

<success_criteria>
- useSettings.ts creato con interfaccia AppSettings esatta (6 campi, STORAGE_KEY='bdd-scaffold-settings')
- settings/page.tsx con autosave on blur, badge "Salvato", campi password per i token
- Link Settings nel nav header
- i18n.ts aggiornato con chiavi settings.* in EN e IT
- Nessun token loggato o esposto
- tsc --noEmit e vitest run passano
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/05-stability-integrations/05-02-SUMMARY.md`
</output>
