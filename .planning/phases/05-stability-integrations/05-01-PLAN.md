---
phase: 05-stability-integrations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/src/components/ErrorBoundary.tsx
  - web-ui/src/providers/Providers.tsx
  - web-ui/src/app/page.tsx
  - web-ui/src/app/editor/page.tsx
  - web-ui/src/app/features/page.tsx
  - web-ui/src/components/StepBrowser.tsx
  - web-ui/src/components/ImportDropzone.tsx
  - web-ui/src/lib/i18n.ts
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-03

must_haves:
  truths:
    - "Se un componente React crasha, l'utente vede 'Qualcosa è andato storto' + bottone 'Ricarica pagina' invece di una pagina bianca"
    - "Gli errori fetch (catalog, import, download) mostrano un toast rosso con il messaggio dell'errore invece di scomparire silenziosamente"
    - "Le pagine Catalog e Features mostrano skeleton loader mentre attendono la risposta API"
    - "StepBrowser mostra skeleton 3 righe al posto del testo 'Caricamento…' durante il fetch"
  artifacts:
    - path: "web-ui/src/components/ErrorBoundary.tsx"
      provides: "React class component ErrorBoundary + ErrorFallback"
      exports: ["ErrorBoundary", "ErrorFallback"]
    - path: "web-ui/src/providers/Providers.tsx"
      provides: "Providers wrapper con ErrorBoundary e Toaster montati"
      contains: "ErrorBoundary"
  key_links:
    - from: "web-ui/src/providers/Providers.tsx"
      to: "web-ui/src/components/ErrorBoundary.tsx"
      via: "import + wrapping"
      pattern: "ErrorBoundary"
    - from: "web-ui/src/providers/Providers.tsx"
      to: "sonner Toaster"
      via: "import { Toaster } from 'sonner'"
      pattern: "Toaster"
---

<objective>
Aggiungere lo stability layer alla web-ui: ErrorBoundary globale, Toaster sonner per notifiche errori/successo, e skeleton loader coerenti su tutte le pagine.

Purpose: Prevenire crash silenziosi e fornire feedback visivo durante le operazioni asincrone — base per le integrazioni GitHub e Jira in Wave 2.
Output: ErrorBoundary.tsx, Providers.tsx aggiornato, skeleton su catalog/features/StepBrowser, toast al posto di .catch silenziosi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stability-integrations/05-CONTEXT.md
@web-ui/src/providers/Providers.tsx
@web-ui/src/lib/i18n.ts
@web-ui/package.json

<interfaces>
<!-- Contratti esistenti da rispettare -->

Da web-ui/src/providers/Providers.tsx:
```typescript
// Struttura attuale:
export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider attribute="class" defaultTheme="system" enableSystem>
      <LanguageContext.Provider value={...}>
        <TooltipProvider delay={400}>
          {children}
        </TooltipProvider>
      </LanguageContext.Provider>
    </NextThemesProvider>
  );
}
// ErrorBoundary va aggiunto come wrapper esterno; Toaster dentro l'ultimo provider
```

Da web-ui/package.json (già installato):
- sonner@2.0.7 — import: `import { Toaster, toast } from 'sonner'`
- shadcn base-nova — componenti in web-ui/src/components/ui/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorBoundary + Toaster in Providers</name>
  <read_first>
    - web-ui/src/providers/Providers.tsx (file corrente da modificare)
    - web-ui/src/lib/i18n.ts (per testo "Qualcosa è andato storto" / "Something went wrong")
    - web-ui/src/app/editor/page.tsx (esempi di .catch silenziosi da correggere)
    - web-ui/src/components/StepBrowser.tsx (altro .catch silenzioso da correggere)
  </read_first>
  <files>
    web-ui/src/components/ErrorBoundary.tsx,
    web-ui/src/providers/Providers.tsx,
    web-ui/src/lib/i18n.ts,
    web-ui/src/app/editor/page.tsx,
    web-ui/src/components/StepBrowser.tsx,
    web-ui/src/components/ImportDropzone.tsx
  </files>
  <action>
**1. Creare web-ui/src/components/ErrorBoundary.tsx**

React class component (obbligatorio per Error Boundaries — non funziona come function component):

```typescript
'use client';
import React from 'react';

interface Props {
  fallback: React.ReactNode;
  children: React.ReactNode;
}
interface State { hasError: boolean; error: Error | null }

export class ErrorBoundary extends React.Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.state = { hasError: false, error: null };
  }
  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error };
  }
  render() {
    if (this.state.hasError) return this.props.fallback;
    return this.props.children;
  }
}

export function ErrorFallback() {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen gap-4 p-8">
      <p className="text-lg font-semibold text-destructive">Qualcosa è andato storto</p>
      <p className="text-sm text-muted-foreground">Something went wrong</p>
      <button
        onClick={() => window.location.reload()}
        className="px-4 py-2 rounded-md bg-primary text-primary-foreground text-sm hover:opacity-90 transition-opacity"
      >
        Ricarica pagina
      </button>
    </div>
  );
}
```

**2. Aggiornare web-ui/src/providers/Providers.tsx**

Aggiungere import ErrorBoundary, ErrorFallback e Toaster. Avvolgere tutto con ErrorBoundary. Aggiungere `<Toaster richColors closeButton />` prima della chiusura dell'ultimo provider:

```typescript
import { ErrorBoundary, ErrorFallback } from '@/components/ErrorBoundary';
import { Toaster } from 'sonner';

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ErrorBoundary fallback={<ErrorFallback />}>
      <NextThemesProvider attribute="class" defaultTheme="system" enableSystem>
        <LanguageContext.Provider value={{ lang, setLang, t: translations[lang] }}>
          <TooltipProvider delay={400}>
            {children}
            <Toaster richColors closeButton position="bottom-right" />
          </TooltipProvider>
        </LanguageContext.Provider>
      </NextThemesProvider>
    </ErrorBoundary>
  );
}
```

**3. Sostituire .catch silenziosi con toast.error**

In editor/page.tsx, riga del fetch /api/catalog:
```typescript
// PRIMA:
.catch(() => {/* catalog not available — autocomplete disabled */});
// DOPO:
.catch((err: Error) => { toast.error(`Catalog non disponibile: ${err.message}`); });
```
Aggiungere `import { toast } from 'sonner';` in cima al file.

In StepBrowser.tsx, riga del fetch /api/catalog:
```typescript
// PRIMA:
.catch(() => {});
// DOPO:
.catch((err: Error) => { toast.error(`Step catalog non disponibile: ${err.message}`); });
```
Aggiungere `import { toast } from 'sonner';` in cima al file.

In ImportDropzone.tsx, trovare il catch dell'import e sostituire con toast.error.

**4. Aggiungere chiavi i18n mancanti in web-ui/src/lib/i18n.ts**

Aggiungere nella sezione `en` e `it` sotto `editor`:
```typescript
// in en.editor:
importError: 'Import failed',
// in it.editor:
importError: 'Importazione fallita',
```
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit</automated>
  </verify>
  <done>
    - web-ui/src/components/ErrorBoundary.tsx esiste ed esporta `ErrorBoundary` (class component) e `ErrorFallback`
    - web-ui/src/providers/Providers.tsx contiene `import { ErrorBoundary` e `import { Toaster`
    - La struttura JSX in Providers.tsx ha `ErrorBoundary` come wrapper esterno e `Toaster` dentro
    - editor/page.tsx e StepBrowser.tsx importano `toast` da `'sonner'`
    - Nessun `.catch(() => {})` silenzioso rimane (grep conferma)
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </done>
  <acceptance_criteria>
    - `grep -r "from 'sonner'" web-ui/src/` trova almeno 3 file (Providers.tsx, editor/page.tsx, StepBrowser.tsx)
    - `grep -n "ErrorBoundary" web-ui/src/providers/Providers.tsx` → almeno 2 occorrenze (import + uso JSX)
    - `grep -n "Toaster" web-ui/src/providers/Providers.tsx` → almeno 1 occorrenza
    - `grep -rn "catch(() => {})" web-ui/src/` → 0 risultati (tutti rimossi)
    - `grep -rn "getDerivedStateFromError" web-ui/src/components/ErrorBoundary.tsx` → 1 occorrenza
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Skeleton loaders su Catalog, Features e StepBrowser</name>
  <read_first>
    - web-ui/src/app/page.tsx (pagina Catalog — aggiungere skeleton durante loading)
    - web-ui/src/app/features/page.tsx (pagina Features — stesso pattern)
    - web-ui/src/components/StepBrowser.tsx (già letto in Task 1 — aggiungere skeleton)
    - web-ui/src/components/ui/ (verificare se esiste skeleton.tsx da shadcn)
  </read_first>
  <files>
    web-ui/src/app/page.tsx,
    web-ui/src/app/features/page.tsx,
    web-ui/src/components/StepBrowser.tsx
  </files>
  <action>
**Pattern skeleton da usare** (verificare prima se `web-ui/src/components/ui/skeleton.tsx` esiste; se sì usarlo; se no, usare il fallback animate-pulse):

```typescript
// Se skeleton.tsx esiste da shadcn:
import { Skeleton } from '@/components/ui/skeleton';
// Uso: <Skeleton className="h-4 w-full" />

// Fallback se non esiste:
function SkeletonRow() {
  return <div className="h-4 w-full rounded animate-pulse bg-muted" />;
}
```

**1. web-ui/src/app/page.tsx (Catalog page)**

Trovare il punto dove `loading` è true (il componente StepCatalog.tsx gestisce il proprio stato di loading — verificare come è strutturato). Probabilmente la pagina monta StepCatalog direttamente. Se StepCatalog ha un proprio stato `loading`, wrappare con Suspense non è necessario — invece modificare StepCatalog per mostrare skeleton invece del testo "Loading step catalog…".

Se la pagina ha uno stato di loading diretto, aggiungere:
```tsx
{loading && (
  <div className="space-y-2 p-4">
    {[...Array(5)].map((_, i) => (
      <div key={i} className="flex gap-3">
        <Skeleton className="h-4 w-1/2" />
        <Skeleton className="h-4 w-16" />
        <Skeleton className="h-4 w-20" />
      </div>
    ))}
  </div>
)}
```

**2. web-ui/src/app/features/page.tsx (Features page)**

Stesso approccio: durante `loading === true`, mostrare 4 righe skeleton invece del testo "Caricamento…":
```tsx
{loading && (
  <div className="space-y-3 p-4">
    {[...Array(4)].map((_, i) => (
      <div key={i} className="flex gap-4 items-center">
        <Skeleton className="h-5 w-1/3" />
        <Skeleton className="h-4 w-16" />
        <Skeleton className="h-4 w-8" />
      </div>
    ))}
  </div>
)}
```

**3. web-ui/src/components/StepBrowser.tsx**

Sostituire la condizione `steps.length === 0 ? t.stepBrowser.loading : t.stepBrowser.noResults` con skeleton quando il catalog è in caricamento.

Aggiungere stato `isLoading` separato:
```typescript
const [isLoading, setIsLoading] = useState(true);
// Nel useEffect fetch: finally(() => setIsLoading(false))
```

Quando `isLoading === true`, mostrare 3 righe skeleton nel posto della lista:
```tsx
{isLoading ? (
  <div className="space-y-2 p-3">
    {[...Array(3)].map((_, i) => (
      <div key={i} className="flex gap-2 items-center">
        <Skeleton className="h-4 w-5 shrink-0" />
        <Skeleton className="h-4 flex-1" />
        <Skeleton className="h-4 w-16 shrink-0" />
      </div>
    ))}
  </div>
) : (
  // lista esistente
)}
```

Usare il componente Skeleton da shadcn se disponibile (leggere web-ui/src/components/ui/ prima di implementare). Se non disponibile, usare `<div className="h-4 rounded animate-pulse bg-muted" />`.
</action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>
    - StepBrowser ha stato `isLoading` e mostra 3 righe skeleton durante il fetch
    - Catalog page e Features page mostrano skeleton al posto del testo di loading
    - `cd web-ui && npx tsc --noEmit` → exit 0
    - `cd web-ui && npx vitest run` → tutti i test passano (nessuna regressione)
  </done>
  <acceptance_criteria>
    - `grep -n "isLoading" web-ui/src/components/StepBrowser.tsx` → almeno 2 occorrenze (useState + setter)
    - `grep -n "animate-pulse\|Skeleton" web-ui/src/components/StepBrowser.tsx` → almeno 1 occorrenza
    - `grep -n "animate-pulse\|Skeleton" web-ui/src/app/features/page.tsx` → almeno 1 occorrenza
    - `grep -n "animate-pulse\|Skeleton" web-ui/src/app/page.tsx` → almeno 1 occorrenza (Catalog page)
    - `cd web-ui && npx vitest run` → exit 0, nessun test fallito
    - `cd web-ui && npx tsc --noEmit` → exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React component tree → ErrorBoundary | Eccezioni JavaScript non handled propagano fino al boundary |
| client → /api/* | Richieste fetch con error handling visibile all'utente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | toast.error messages | accept | Messaggi di errore mostrano HTTP status code (già pubblico) ma non stack trace né dettagli server interni |
| T-05-01-02 | Denial of Service | ErrorBoundary | accept | ErrorBoundary locale, non globale per singolo componente — reload necessario ma non espone superficie d'attacco |
</threat_model>

<verification>
Verifica complessiva del piano:

```bash
cd web-ui && npx tsc --noEmit
cd web-ui && npx vitest run
grep -rn "catch(() => {})" web-ui/src/
grep -n "ErrorBoundary" web-ui/src/providers/Providers.tsx
grep -n "Toaster" web-ui/src/providers/Providers.tsx
```

Atteso: tsc exit 0, vitest tutti verdi, nessun .catch silenzioso, ErrorBoundary e Toaster presenti in Providers.
</verification>

<success_criteria>
- ErrorBoundary.tsx creato come class component con ErrorFallback (testo italiano + inglese)
- Providers.tsx wrappa tutto con ErrorBoundary, monta Toaster sonner
- Tutti i .catch silenziosi sostituiti con toast.error
- StepBrowser, Catalog, Features page hanno skeleton loader durante il caricamento
- tsc --noEmit e vitest run passano senza regressioni
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/05-stability-integrations/05-01-SUMMARY.md`
</output>
