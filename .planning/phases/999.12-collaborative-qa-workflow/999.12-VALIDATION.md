---
phase: 999.12
slug: collaborative-qa-workflow
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-24
---

# Phase 999.12 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.8 |
| **Config file** | `web-ui/vitest.config.*` (or inline in package.json) |
| **Quick run command** | `cd web-ui && npx vitest run` |
| **Full suite command** | `cd web-ui && npm test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd web-ui && npx vitest run`
- **After every plan wave:** Run `cd web-ui && npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Threat Ref | Secure Behavior | Test Type | Automated Command | Status |
|---------|------|------|------------|-----------------|-----------|-------------------|--------|
| 999.12-01-T1 | 01 | 1 | T-999.12-01/02/03 | GITHUB_NAME_RE guard; sanitizeError strips token | unit | `cd web-ui && npx vitest run src/lib/github-utils.test.ts` | ⬜ pending |
| 999.12-01-T2 | 01 | 1 | — | N/A — type/UI only | type-check | `cd web-ui && npx tsc --noEmit` | ⬜ pending |
| 999.12-01-T3 | 01 | 1 | T-999.12-01/02/04 | owner/repo/branch validated; FS fallback on failure; token not in response | unit | `cd web-ui && npx vitest run src/app/api/catalog/route.test.ts` | ⬜ pending |
| 999.12-02-T1 | 02 | 2 | T-999.12-05/06/07/09 | branch GITHUB_NAME_RE; commitName <> strip + 100-char cap; duplicate 409; sanitizeError | unit | `cd web-ui && npx vitest run src/app/api/catalog/propose/route.test.ts` | ⬜ pending |
| 999.12-02-T2 | 02 | 2 | T-999.12-06/07 | commitName validated; existing GITHUB_NAME_RE/sanitize preserved | type-check | `cd web-ui && npx tsc --noEmit` | ⬜ pending |
| 999.12-02-T3 | 02 | 2 | T-999.12-08 | Push disabled when identity missing (client guard) | type-check + lint | `cd web-ui && npx tsc --noEmit && npm run lint` | ⬜ pending |
| 999.12-03-T1 | 03 | 3 | T-999.12-10 | try/catch on invalid regex; returns false | unit | `cd web-ui && npx vitest run src/lib/catalog-match.test.ts` | ⬜ pending |
| 999.12-03-T2 | 03 | 3 | T-999.12-12 | tooltip uses textContent (not innerHTML) | type-check + lint | `cd web-ui && npx tsc --noEmit && npm run lint` | ⬜ pending |
| 999.12-03-T3 | 03 | 3 | T-999.12-11 | server-side validation in propose route (Wave 2); modal only collects | type-check + lint | `cd web-ui && npx tsc --noEmit && npm run lint` | ⬜ pending |
| 999.12-03-T4 | 03 | 3 | — | N/A — visual badge only | type-check | `cd web-ui && npx tsc --noEmit` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

These test files must be created as part of their respective tasks (TDD — created in the task itself):

- [ ] `web-ui/src/lib/github-utils.test.ts` — Plan 01 Task 1 (TDD) — covers getFileSHA, putContents, branchEnsure, sanitizeError
- [ ] `web-ui/src/app/api/catalog/route.test.ts` — Plan 01 Task 3 (TDD) — covers proposals merge + 404 fallback + FS fallback
- [ ] `web-ui/src/app/api/catalog/propose/route.test.ts` — Plan 02 Task 1 (TDD) — covers duplicate detection (409), FS fallback, branch ensure
- [ ] `web-ui/src/lib/catalog-match.test.ts` — Plan 03 Task 1 (TDD) — covers exact match, parameter token, case-insensitive, invalid regex

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|----------|------------|-------------------|
| Settings UI shows Commit identity section with PAT link | React component render; no Vitest/jsdom configured for page-level tests | Open `http://localhost:3000/settings`, verify "Commit identity" section is visible with catalogBranch, commitName, commitEmail fields and the token doc link |
| CommitPreviewDialog shows correct identity / disables Push when empty | Component behavior in browser | In editor with GitHub configured, click push button; verify dialog shows name/email/branch; clear commitName in Settings, verify Push is disabled and warning text is shown |
| Orange underline on unknown step in CodeMirror | CodeMirror DOM — not testable in jsdom | In editor, type a step not in the catalog; verify orange underline appears; hover to see tooltip; click to open ProposeStepModal |
| "Proposed" badge visible in StepBrowser/Catalog after proposal | Full round-trip requiring GitHub | With GitHub configured, propose a step; refresh catalog; verify the "Proposed" orange badge appears |
| CommitPreviewDialog fires before EVERY push (feature file and proposal) | UX flow requiring browser | With GitHub configured, push a feature file and separately propose a step; verify dialog appears for both |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING test file references
- [ ] No watch-mode flags in automated commands
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
