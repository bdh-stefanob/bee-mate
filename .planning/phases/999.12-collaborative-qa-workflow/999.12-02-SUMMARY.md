---
phase: "999.12"
plan: "02"
subsystem: web-ui
tags: [github-api, commit-identity, propose-route, dialog, security, vitest, typescript]
dependency_graph:
  requires:
    - lib/github-utils.ts (branchEnsure, getFileContent, putContents, GITHUB_NAME_RE, sanitizeError)
    - AppSettings.catalogBranch / commitName / commitEmail (Wave 1)
    - CatalogStep.status 'proposed' + proposedAt (Wave 1)
  provides:
    - POST /api/catalog/propose (GitHub push of step-proposals.json + FS fallback)
    - POST /api/github/push (extended with author/committer identity)
    - CommitPreviewDialog component
    - Both push paths in _content.tsx gated behind CommitPreviewDialog
  affects:
    - web-ui/src/app/api/catalog/propose/route.ts
    - web-ui/src/app/api/github/push/route.ts
    - web-ui/src/components/CommitPreviewDialog.tsx
    - web-ui/src/app/editor/_content.tsx
tech_stack:
  added: []
  patterns:
    - TDD RED→GREEN for propose route (4 tests)
    - GitHub REST API branchEnsure + fetch-merge-push for step-proposals.json
    - Optimistic concurrency (D-04): fetch SHA, merge in memory, PUT; GitHub rejects stale SHA
    - Token-in-header security (T-05-03-03, T-999.12-07): token never in body or error response
    - CommitPreviewDialog: fixed inset overlay, bg-black/50 backdrop, Escape-to-close (StepDetailModal pattern)
    - Preview state gates both push paths (D-08): feature push and proposal push
key_files:
  created:
    - web-ui/src/components/CommitPreviewDialog.tsx
    - web-ui/__tests__/api/propose.test.ts
  modified:
    - web-ui/src/app/api/catalog/propose/route.ts
    - web-ui/src/app/api/github/push/route.ts
    - web-ui/src/app/editor/_content.tsx
decisions:
  - "Test file placed in __tests__/api/propose.test.ts (not src/app/api/catalog/propose/route.test.ts) to match Vitest config include: __tests__/**/*.test.ts — same deviation as Wave 1"
  - "global.fetch mock uses 'as unknown as typeof fetch' cast — same pattern as __tests__/lib/github-utils.test.ts"
  - "doPropose on 409 uses toast.error(data.error) without throwing, so setPreview(null) in onConfirm still runs cleanly"
metrics:
  duration: "~20 minutes"
  completed_date: "2026-06-24"
  tasks_completed: 3
  files_created: 2
  files_modified: 3
  tests_added: 4
---

# Phase 999.12 Plan 02: Commit Pipeline — Propose Route + Author Identity + CommitPreviewDialog

**One-liner:** POST /api/catalog/propose rewritten to push step-proposals.json to the catalog branch via GitHub API (branchEnsure + duplicate detection + FS fallback); POST /api/github/push extended with author/committer identity; CommitPreviewDialog gates every push with confirm/cancel showing "Committing as: name <email> → branch: X".

---

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite POST /api/catalog/propose (TDD RED→GREEN) | f620dd5 | propose/route.ts, __tests__/api/propose.test.ts |
| 2 | Add commit author/committer to POST /api/github/push | 6def0b7 | push/route.ts |
| 3 | Build CommitPreviewDialog + wire _content.tsx | ef41613 | CommitPreviewDialog.tsx, _content.tsx |

---

## Success Criteria — Verified

- [x] POST /api/catalog/propose pushes step-proposals.json to catalog branch with author/committer, auto-creates branch (branchEnsure), rejects duplicates (409), falls back to FS when unconfigured
- [x] POST /api/github/push sets author/committer from validated x-commit-name/x-commit-email headers
- [x] CommitPreviewDialog renders "Review commit" with identity line in font-mono, teal "Push to catalog" button, ghost "Cancel", Push disabled when commitName/commitEmail missing
- [x] Both push paths in _content.tsx open CommitPreviewDialog first; push only on confirm (D-08)
- [x] Token never leaks (sanitizeError applied in propose route catch)
- [x] 4 new Vitest tests pass (new expression, duplicate 409, branch auto-create, FS fallback)
- [x] tsc --noEmit exits 0
- [x] npm run lint reports no new errors (pre-existing errors in electron/ and FileSidebar.tsx only)

---

## Test Results

```
__tests__/api/propose.test.ts — 4 tests PASS
  ✓ GitHub path (new expression): returns { ok: true, added: 1 }, putContents called, status:'proposed', sourceRef:'Mario Rossi'
  ✓ GitHub path, duplicate: returns 409 with 'Duplicates are not allowed.', no PUT
  ✓ GitHub path, branch auto-create: POST /git/refs called when branch is 404
  ✓ FS fallback: fs.writeFileSync called, status:'wanted', no GitHub fetch
Total new tests: 4 PASS, 0 FAIL
```

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Test file path mismatch with Vitest config**
- **Found during:** Task 1 (same issue as Wave 1)
- **Issue:** Plan specifies `web-ui/src/app/api/catalog/propose/route.test.ts` but Vitest config only includes `__tests__/**/*.test.ts`
- **Fix:** Created canonical test at `web-ui/__tests__/api/propose.test.ts`
- **Files modified:** `web-ui/__tests__/api/propose.test.ts` (created)
- **Commit:** f620dd5

**2. [Rule 1 - Bug] `global.fetch` type cast required**
- **Found during:** Task 1 (tsc --noEmit after GREEN)
- **Issue:** `vi.fn()` mock not assignable to `typeof fetch` without explicit cast
- **Fix:** Added `as unknown as typeof fetch` cast on all four `global.fetch = fetchMock` assignments — matches the pattern already used in `__tests__/lib/github-utils.test.ts`
- **Files modified:** `web-ui/__tests__/api/propose.test.ts`
- **Commit:** 6def0b7 (included as part of Task 2 staged changes)

---

## Known Stubs

None. All implemented functions have real behavior:
- `proposeToFs` reads/writes the actual `step-catalog.json` from REPO_ROOT
- `doCommitGitHub` / `doPropose` send real fetch calls with all identity headers
- `CommitPreviewDialog` renders full UI with real disabled/pushing states

---

## Threat Flags

No new threat surface beyond what is documented in the plan's `<threat_model>`:
- T-999.12-05: catalogBranch validated with GITHUB_NAME_RE before any GitHub URL — mitigated
- T-999.12-06: commitName/commitEmail sanitized (`<>` stripped, length capped) — mitigated
- T-999.12-07: token sanitized in catch block via `sanitizeError(message, token)` — mitigated
- T-999.12-08: spoofing accepted by design (identity self-declared, PAT authenticates)
- T-999.12-09: optimistic concurrency accepted (GitHub rejects stale SHA, user retries)

---

## Self-Check: PASSED

| Item | Status |
|------|--------|
| web-ui/src/components/CommitPreviewDialog.tsx | FOUND |
| web-ui/__tests__/api/propose.test.ts | FOUND |
| web-ui/src/app/api/catalog/propose/route.ts (rewritten) | FOUND |
| web-ui/src/app/api/github/push/route.ts (extended) | FOUND |
| web-ui/src/app/editor/_content.tsx (wired) | FOUND |
| commit f620dd5 (Task 1) | FOUND |
| commit 6def0b7 (Task 2) | FOUND |
| commit ef41613 (Task 3) | FOUND |
