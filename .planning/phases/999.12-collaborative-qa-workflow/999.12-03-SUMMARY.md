---
phase: "999.12"
plan: "03"
subsystem: web-ui
tags: [codemirror, decoration, catalog-match, propose-modal, ui, typescript, vitest]
dependency_graph:
  requires:
    - "999.12-01 (CatalogStep.status 'proposed', GET /api/catalog merge)"
    - "999.12-02 (POST /api/catalog/propose GitHub push, CommitPreviewDialog)"
  provides:
    - lib/catalog-match.ts (shared matchesCatalog — used by _content.tsx and GherkinEditor)
    - GherkinEditor unknownStepPlugin (cm-unknown-step decoration, hover tooltip, click-to-propose)
    - ProposeStepModal (single-step proposal form, area dropdown, inline duplicate error)
    - Proposed badge in StepBrowser, StepCatalog, StepDetailModal
  affects:
    - web-ui/src/lib/catalog-match.ts
    - web-ui/src/components/GherkinEditor.tsx
    - web-ui/src/components/ProposeStepModal.tsx
    - web-ui/src/app/editor/_content.tsx
    - web-ui/src/components/StepBrowser.tsx
    - web-ui/src/components/StepCatalog.tsx
    - web-ui/src/components/StepDetailModal.tsx
tech_stack:
  added: []
  patterns:
    - CodeMirror 6 ViewPlugin + Decoration.mark + RangeSetBuilder (ascending order — Pitfall 1)
    - Ref technique for stale closure avoidance in ViewPlugin (Pitfall 2)
    - hoverTooltip from @codemirror/view for accessible unknown-step tooltip (WCAG 1.4.1)
    - EditorView.domEventHandlers for click-to-propose
    - TDD (RED → GREEN) for catalog-match.ts
    - STATUS_STYLES map pattern (extended with proposed)
key_files:
  created:
    - web-ui/src/lib/catalog-match.ts
    - web-ui/__tests__/lib/catalog-match.test.ts
    - web-ui/src/components/ProposeStepModal.tsx
  modified:
    - web-ui/src/components/GherkinEditor.tsx
    - web-ui/src/app/editor/_content.tsx
    - web-ui/src/components/StepBrowser.tsx
    - web-ui/src/components/StepCatalog.tsx
    - web-ui/src/components/StepDetailModal.tsx
decisions:
  - "catalog-match.test.ts placed in __tests__/lib/ (not src/lib/) to match vitest.config.ts include pattern — same decision as Plan 01"
  - "unknownStepPlugin reads stepExpressionsRef.current inside update() — avoids stale capture (Pitfall 2)"
  - "hoverTooltip re-evaluates the step on each hover (no stored range set) — keeps logic simple and avoids CM6 range-set traversal"
  - "ProposeStepModal uses simple inline conditional (fixed overlay) following StepDetailModal pattern, not @base-ui/react Dialog — consistent with existing modal pattern in the codebase"
  - "doProposeSingle re-fetches /api/catalog after success to refresh stepExpressions and catalogAreas — decoration clears on next CM6 update cycle"
metrics:
  duration: "~35 minutes"
  completed_date: "2026-06-24"
  tasks_completed: 4
  files_created: 3
  files_modified: 5
  tests_added: 8
---

# Phase 999.12 Plan 03: UI Surface — Unknown Steps + Proposal Modal + Proposed Badges

**One-liner:** Shared `matchesCatalog` in `lib/catalog-match.ts`; CodeMirror 6 `unknownStepPlugin` orange-underlines step lines absent from the combined catalog with a hover tooltip and click-to-propose; `ProposeStepModal` pre-fills expression + keyword and lets the colleague pick an area; orange "Proposed" badge added to StepBrowser, StepCatalog, and StepDetailModal STATUS_STYLES.

---

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Extract lib/catalog-match.ts + TDD tests; rewire _content.tsx | e0a9da7 | catalog-match.ts, __tests__/lib/catalog-match.test.ts, _content.tsx |
| 2 | Add unknownStepPlugin to GherkinEditor (decoration + tooltip + click) | a9eb979 | GherkinEditor.tsx |
| 3 | Build ProposeStepModal + wire to editor click handler | 0c43b4c | ProposeStepModal.tsx, _content.tsx |
| 4 | Show Proposed badge in StepBrowser, StepCatalog, StepDetailModal | a8a2edb | StepBrowser.tsx, StepCatalog.tsx, StepDetailModal.tsx |

---

## Success Criteria — Verified

- [x] matchesCatalog lives in lib/catalog-match.ts, imported by _content.tsx (no local duplicate), tested (8 tests pass)
- [x] GherkinEditor imports ViewPlugin, Decoration, hoverTooltip, RangeSetBuilder, matchesCatalog
- [x] GherkinEditor contains cm-unknown-step class, "Step not in catalog — click to propose", onProposeStep prop with ref pattern
- [x] ProposeStepModal exports `export function ProposeStepModal`, contains "Propose step", "Select area…", imports Select from @/components/ui/select, renders errorText as text-destructive
- [x] _content.tsx imports ProposeStepModal, passes onProposeStep= to GherkinEditor, exposes catalogAreas
- [x] StepDetailModal STATUS_STYLES contains `proposed: 'bg-[#F97316] text-white border-transparent'`
- [x] StepCatalog STATUS_STYLES contains same proposed entry
- [x] StepBrowser renders orange "Proposed" span when step.status === 'proposed'
- [x] `npx vitest run __tests__/lib/catalog-match.test.ts` — 8/8 pass
- [x] `npx tsc --noEmit` — exits 0
- [x] No new lint errors introduced (pre-existing errors in electron/ and FileSidebar.tsx unchanged)

---

## Test Results

```
__tests__/lib/catalog-match.test.ts  — 8 tests PASS
Total new tests: 8 PASS, 0 FAIL
```

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Vitest config include pattern mismatch (same as Plan 01)**
- **Found during:** Task 1 (RED phase)
- **Issue:** Plan specified test file at `src/lib/catalog-match.test.ts`, but the project vitest.config.ts only includes `__tests__/**/*.test.ts`. Tests at `src/**` are never executed.
- **Fix:** Created test in `__tests__/lib/catalog-match.test.ts`. Vitest config unchanged.
- **Files modified:** `__tests__/lib/catalog-match.test.ts` (created), removed placeholder `src/lib/catalog-match.test.ts`

---

## Known Stubs

None. All code paths are fully implemented:
- `matchesCatalog` is functional (not a stub)
- `unknownStepPlugin` decorates dynamically from the catalog ref
- `ProposeStepModal` calls `onSubmit(area)` which is wired to `doProposeSingle` in `_content.tsx`
- `doProposeSingle` calls the real `/api/catalog/propose` endpoint (implemented in Plan 02)
- Proposed badge renders for `status === 'proposed'` which is produced by the Plan 01/02 pipeline

---

## Threat Flags

No new threat surface beyond the plan's `<threat_model>`:
- T-999.12-10: matchesCatalog try/catch preserves safe behavior on invalid regex
- T-999.12-11: ProposeStepModal only collects; server-side validation enforced in Plan 02 propose route
- T-999.12-12: tooltip uses `textContent` (not innerHTML); badges use React children (auto-escaped)

---

## Self-Check: PASSED

| Item | Status |
|------|--------|
| web-ui/src/lib/catalog-match.ts | FOUND |
| web-ui/__tests__/lib/catalog-match.test.ts | FOUND |
| web-ui/src/components/GherkinEditor.tsx | FOUND (cm-unknown-step, onProposeStep) |
| web-ui/src/components/ProposeStepModal.tsx | FOUND |
| web-ui/src/app/editor/_content.tsx | FOUND (catalogAreas, proposeModal, onProposeStep=) |
| web-ui/src/components/StepBrowser.tsx | FOUND (Proposed badge) |
| web-ui/src/components/StepCatalog.tsx | FOUND (proposed in STATUS_STYLES) |
| web-ui/src/components/StepDetailModal.tsx | FOUND (proposed in STATUS_STYLES) |
| commit e0a9da7 (Task 1) | FOUND |
| commit a9eb979 (Task 2) | FOUND |
| commit 0c43b4c (Task 3) | FOUND |
| commit a8a2edb (Task 4) | FOUND |
