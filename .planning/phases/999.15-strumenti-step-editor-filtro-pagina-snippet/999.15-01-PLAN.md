---
phase: 999.15-strumenti-step-editor-filtro-pagina-snippet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/src/components/StepBrowser.tsx
  - web-ui/src/lib/i18n.ts
autonomous: true
requirements: [STEP-FILTER-01]
must_haves:
  truths:
    - "Nel pannello Steps dell'editor, sotto la search bar, c'è una riga di chip per filtrare gli step del catalogo per pagina (#PAGINA)"
    - "I valori pagina derivano dal campo page degli step del catalogo (CatalogStep.page); compaiono solo le pagine effettivamente presenti"
    - "Selezionando una pagina, la lista step mostra solo gli step con quel page; il filtro si combina con ricerca, area e keyword esistenti"
    - "Esiste un chip 'Tutte' per azzerare il filtro pagina; il filtro pagina non rompe i filtri esistenti"
  artifacts:
    - path: "web-ui/src/components/StepBrowser.tsx"
      provides: "Filtro pagina (chip) sotto la search bar, combinato con i filtri esistenti"
      contains: "pageFilter"
  key_links: []
---

<objective>
Aggiungere nel pannello Steps dell'editor, **sotto la search bar**, un filtro per **pagina `#PAGINA`**: chip (come i filtri area/keyword già esistenti) che filtrano gli step del catalogo per schermata, usando il campo `page` già presente su `CatalogStep`.

Purpose: il QA pensa per schermate (LOGIN, MFA, ORDER…). Poter filtrare il catalogo per pagina rende immediato trovare "tutti gli step della pagina LOGIN" e chiude il cerchio con la pagina `/tags`. È il backlog 999.10, ora naturale perché gli step hanno già `page`.

Output: stato `pageFilter` in StepBrowser, riga chip pagine sotto la search bar, integrazione con il filtro `filtered` esistente, chiavi i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
web-ui/src/lib/types.ts: `CatalogStep` ha `page?: string` (riga 15).

web-ui/src/components/StepBrowser.tsx (stato attuale):
- stati filtro: `query`, `areaFilter`, `keywordFilter`; lista derivata `filtered` (useMemo su steps + i 3 filtri).
- `areas`/`areaCounts` useMemo. Pattern chip area: riga ~204-232. Pattern chip keyword: ~236-270.
- search Input: ~273-283. La lista `<ul>` segue subito dopo (~285).
- `filtered` già include `(s.page ?? '').toLowerCase().includes(q)` nella ricerca testuale.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filtro pagina nello StepBrowser</name>
  <files>web-ui/src/components/StepBrowser.tsx, web-ui/src/lib/i18n.ts</files>
  <read_first>
    - web-ui/src/components/StepBrowser.tsx (stati, filtered useMemo, pattern chip area/keyword, posizione search bar)
    - web-ui/src/lib/i18n.ts (sezione stepBrowser)
  </read_first>
  <action>
**StepBrowser.tsx**:
1. Aggiungi stato `const [pageFilter, setPageFilter] = useState<string | null>(null);`
2. Deriva le pagine disponibili: `const pages = useMemo(() => [...new Set(steps.map(s => s.page).filter(Boolean) as string[])].sort(), [steps]);` (con conteggi opzionali tipo `areaCounts`).
3. Integra nel `filtered` useMemo: dopo i filtri area/keyword, `if (pageFilter) list = list.filter(s => s.page === pageFilter);`. Aggiungi `pageFilter` alle deps del useMemo e al reset di `activeIndex` (l'effetto su `[query, areaFilter, keywordFilter]` → aggiungi `pageFilter`).
4. **Sotto la search bar** (subito dopo il blocco `<div className="p-2 pt-1"><Input .../></div>`, PRIMA della `<ul>`), aggiungi una riga di chip pagina SOLO se `pages.length > 0`: un chip "Tutte" (`t.stepBrowser.allPages`) che fa `setPageFilter(null)`, poi un chip per ogni pagina (stesso stile dei chip area: `rounded-full border text-[10px]`, attivo = teal). Click su una pagina già attiva → azzera (toggle). Mostra la riga in un contenitore con `flex flex-wrap gap-1 px-2 pb-1`.
5. (Opzionale) Se la pagina selezionata non ha più risultati per via degli altri filtri, va bene mostrare "nessun risultato" esistente.

**i18n.ts**: aggiungi in `stepBrowser` (en/it) la chiave `allPages` (en: "All pages" / it: "Tutte le pagine"). Se vuoi un'etichetta per la riga, aggiungi `pagesLabel` (en: "Page" / it: "Pagina") — opzionale.
  </action>
  <verify>
    <automated>cd web-ui && npx tsc --noEmit 2>&1 | grep -iE "StepBrowser|i18n" || echo "OK: nessun errore tsc"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pageFilter" web-ui/src/components/StepBrowser.tsx` mostra stato + uso nel filtro + chip
    - I chip pagina sono renderizzati SOTTO la search Input (non sopra, dove stanno area/keyword)
    - `pages` deriva da `steps.map(s => s.page)` filtrando i vuoti
    - Il filtro pagina si combina con query/area/keyword nel `filtered` useMemo
    - i18n: `allPages` in en e it
    - `cd web-ui && npx tsc --noEmit` senza nuovi errori
  </acceptance_criteria>
  <done>Il pannello Steps filtra per pagina #PAGINA tramite chip sotto la search bar, combinandosi coi filtri esistenti.</done>
</task>

</tasks>

<verification>
- `cd web-ui && npx tsc --noEmit` passa
- I chip pagina compaiono sotto la search bar e filtrano gli step per `page`
- Combinazione con ricerca/area/keyword funziona
</verification>

<success_criteria>
- Filtro per pagina #PAGINA disponibile sotto la search bar dello StepBrowser
- Usa il campo page esistente, niente nuove API
</success_criteria>

<output>
After completion, create `.planning/phases/999.15-strumenti-step-editor-filtro-pagina-snippet/999.15-01-SUMMARY.md`
</output>
