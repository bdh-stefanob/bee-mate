import { NextResponse } from 'next/server';

// STUB: implementazione completa in plan 03 (wave 3).
// Questa route verrà sostituita con execSync di import-scenarios.ts.
export async function POST() {
  return NextResponse.json(
    { ok: false, error: 'not implemented (plan 03)' },
    { status: 501 }
  );
}
